# despejo da tela renderizada, regerado por render_dump.js a cada execucao dos
# portoes: 10 MB derivados do payload + do app.js. O que vale versionar e o
# codigo que o produz, nao o produto.
#
# Fica AQUI e nao no .gitignore da raiz porque o portao ISOLATION exige que esta
# missao nao toque em nada fora de v1/ e pilot-label-intelligence/ — e ele tem
# razao: foi ele que pegou a versao anterior desta linha.
RENDER-DUMP.html
