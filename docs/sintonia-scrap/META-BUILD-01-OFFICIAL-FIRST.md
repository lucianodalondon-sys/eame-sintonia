# META-BUILD-01 — A FUNDAÇÃO OFICIAL DA META, E NENHUMA PROMESSA

```
MEDIDO_EM            = 2026-09-12
META_REAL_REQUESTS   = 0
APIFY_RUNS           = 0
PAID_RUNS            = 0
COST_USD             = 0
CREDENCIAL_USADA     = NENHUMA
PROBES EXECUTADOS    = 0
```

> **EVIDÊNCIA DE MISSÃO, NÃO BÍBLIA.** Não promove capacidade nenhuma, não
> autoriza coleta nenhuma e não substitui contrato nenhum. É o registo de uma
> fundação construída **offline** sobre o que a
> [`META-DEEP-STUDY-V1.md`](META-DEEP-STUDY-V1.md) mediu.

**O que esta missão fez, numa frase:**

```
DEU DONO À MAIOR ROTA OFICIAL DA FAMÍLIA META, E RECUSOU-SE A DIZER QUE ELA
ESTÁ PRONTA.
```

---

## A · O ESTUDO, LOCALIZADO NO GIT

A missão proibiu confiar no relato do chat. Os três artefatos foram procurados
por varredura de **todos** os ramos remotos, e existem:

```
META_DEEP_GIT_STATE = PROVEN
META_DEEP_BRANCH    = origin/claude/wonderful-hamilton-m50ahv
META_DEEP_BASE_HEAD = 5bcc44f54afefba44bfef9d6b1d2cd713b33f369
META_DEEP_FINAL_HEAD= 3c417267c5b21d73b3748d9eb6c2064f7bc7ac9c
```

| artefato | `BLOB_SHA` |
|---|---|
| `META-DEEP-STUDY-V1.md` | `af742dfb26716f3b4395957fe3026ab374cf4ef5` |
| `META-ROUTE-MATRIX-V1.json` | `4923c38f5456db567da534bff92020eac9b649cf` |
| `META-COMPETITOR-COVERAGE-V1.md` | `1c2f9d5cf99368ecd273cb4630a0d4b6281a9090` |

### Os onze achados, conferidos NO ARTEFATO

Cada um foi procurado no ficheiro, não na memória. **Onze suportados, zero
`NOT_PROVEN`** — e as citações literais que os sustentam:

```
ACTORS_REVIEWED       = 47   (18 IG · 15 FB · 7 Ad Library · 8 Threads)
REPOS_REVIEWED        = 28
COMMERCIAL_EU_HISTORY = 1 ANO a contar da última impressão
«MEDIDO em 7 posts das 5 contas do lote, deslogado: 18 de 31 comentários…»
```

### E o HEAD do SCRAP tinha andado

```
enquanto o estudo corria, a linha canônica avançou SEIS commits
  claude/sintonia-scrap-first-paid-route-c10-8b  5bcc44f5 -> cfbb80c2
```

A C10.8B-LIVE correu a primeira rota paga **real** e rebaixou
`apify:transcricao` para `PARTIAL` porque ela não entregou. Esta missão nasce
sobre esse HEAD, por merge — **nunca por rebase**, que reescreveria história de
outra frente.

```
CONSTRUIR SOBRE O HEAD QUE O ESTUDO MEDIU SERIA CONSTRUIR SOBRE UMA ÁRVORE
QUE JÁ NÃO EXISTE.
```

---

## B · KNOW-HOW

```
HEAD_BEFORE    = b7f96ef1b9cf1f5838db3687b6ecd30a9aebfd9a  (§85)
HEAD_AFTER     = 9bdcff05b… (§88, o ramo canônico andou)  +  §89 desta missão
KNOW_HOW_DELTA = ATUALIZADO
```

### E a secção mudou de número, porque o número já tinha dono

Esta missão escreveu a sua lei como `§86`. Enquanto ela corria, o ramo canônico
escreveu **a sua própria `§86`** — sobre linhagem de observação, outra coisa — e
seguiu para `§87` e `§88`.

```
NÃO ERA CONTEÚDO DUPLICADO. ERA NÚMERO DUPLICADO, QUE É PIOR:
duas secções diferentes a atender pelo mesmo nome.
```

As duas medições são válidas e não se sobrepõem. O número canônico ficou com
quem chegou primeiro ao ficheiro; a desta missão renumerou para **`§89`**, com
as subsecções junto.

```
QUEM CHEGA DEPOIS AO FICHEIRO DE OUTRO NÃO LEVA O NÚMERO QUE JÁ ESTAVA LÁ.
```

O estudo fechou com `KNOW_HOW_DELTA = NENHUM` porque o ficheiro canônico não
existia na sua linhagem. A cura não foi copiar a lei: foi **trazer a lei
canônica para onde a medição está**, por merge do ramo de know-how — 17
documentos, zero ficheiros de `coleta/`, `leis/` ou `guarda/`.

```
UMA LEI EM DOIS SÍTIOS DIVERGE, E A PARTIR DAÍ NENHUMA DAS DUAS VALE.
```

Antes de escrever, varreu-se o ficheiro: `Ad Library` 0 ocorrências, `branded`
0, `18/31` 0, `COMMENT_COUNT` 0, `OFFICIAL-FIRST` 0. **Nenhum dos quatro estava
registado.**

---

## C · O CENSO, NO HEAD FUNCIONAL REAL

Medido executando o próprio código, **depois** do merge — não assumido a partir
do estudo, porque outra frente tinha avançado.

```
INSTAGRAM_CAPABILITIES = 7   (4 com caminho · 3 sem)
FACEBOOK_CAPABILITIES  = 4   (0 com caminho)
THREADS_CAPABILITIES   = 0
META_CAPABILITIES      = 11  (antes desta missão)

AD_LIBRARY_BEFORE      = DECLARED=NO · REGISTERED=NO · WIRED=NO
BRANDED_CONTENT_BEFORE = DECLARED=NO · REGISTERED=NO · WIRED=NO
```

Varredura do código do SCRAP por `ads_archive`, `ad_library`, `branded_content`,
`advertis`, `partnership`: **zero ocorrências**. As seis de `anuncio` são todas
sobre preço anunciado ou sobre a camada de leitura — nenhuma é capacidade de
aquisição.

---

## D · AS CAPACIDADES NOVAS

```
AD_LIBRARY      = meta.ads.search              -> SEARCH_ADS
BRANDED_CONTENT = meta.branded_content.search  -> SEARCH_BRANDED_CONTENT
```

### `META` não é um guarda-chuva

Instagram, Facebook e Threads **continuam plataformas próprias**. Nada deles se
move.

```
META = A SUPERFÍCIE DE TRANSPARÊNCIA PÚBLICA. ONDE O OBJETO VIVE.
`publisher_platforms` = ONDE ELE APARECE, e é um CAMPO.
```

Um anúncio da Ad Library aparece em Facebook, Instagram, Threads ou nas quatro
ao mesmo tempo. Declará-lo como observação de Instagram mentiria sobre metade
dos casos; como de Facebook, sobre a outra metade.

### E não é um segundo runtime

```
scrap_executor.COLLECT -> social_rotas -> scrap_registo -> adaptador_meta -> rota oficial
```

Não existe `meta_executor.py`, `meta_runtime.py`, `meta_orchestrator.py` nem
`meta_scrap_v2.py` — e há prova automática que reprova se algum aparecer.

**O pedido completo foi corrido por essa porta, offline:**

```
objetos                  = 3
CONTENT_TYPE             = ADVERTISEMENT
RESULT                   = OK
EXECUTION_MODE           = TRIAL
CAPABILITY_STATE_BEFORE  = NOT_EXECUTED
CAPABILITY_STATE_AFTER   = NOT_EXECUTED      ← a prova que interessa
COST_USD                 = 0.0
URL chamada              = https://graph.facebook.com/v21.0/ads_archive?ad_reached_countries=["IT"]…
```

```
UM ENSAIO QUE DEVOLVE OBJETOS NÃO PROMOVE ESTADO NENHUM.
```

### Duas espécies novas, porque colapsá-las custa caro

`social_envelope.CONTENT_TYPES` tinha sete palavras e nenhuma servia. Emitir um
anúncio como `POST` teria apagado a distinção na origem.

```
ADVERTISEMENT   != ORGANIC_POST
BRANDED_CONTENT != ORGANIC_POST
BRAND_PARTNER   != POST_AUTHOR
```

A terceira é a mais fácil de errar: no branded content o **autor** é o criador e
o **pagador** é a marca. São pessoas diferentes, e o `SOURCE_ACCOUNT` fica com o
criador enquanto a marca vive em `partners`.

### Os campos são os que a evidência suporta, e mais nenhum

| grupo | campos | nota |
|---|---|---|
| **anúncio, universal** | 13 | `id` é o nome canônico — **não** `ad_archive_id`, que é o nome do endpoint interno |
| **anúncio, só-UE** | 7 | `eu_total_reach` (inteiro exacto), `beneficiary_payers`, segmentação, quebra por idade×género |
| **anúncio, RECUSADOS** | 7 | `spend`, `impressions`, `currency`… — só-político, e sempre em FAIXA |
| **branded content** | 5 | `creation_date`, `creator`, `partners`, `type`, `url` |
| **branded, ausentes** | 5 | legenda, mídia, país, métricas, spend — declarados como `NOT_RETURNED_BY_SOURCE` |

```
META AD != SALES.  META AD != MARKET SHARE.  META AD != CAMPAIGN SUCCESS.
CAMPO AUSENTE É `UNKNOWN`, NUNCA `0` E NUNCA `false`.
```

---

## E · ACESSO — DINHEIRO E CREDENCIAL SÃO DOIS EIXOS

`decisao()` responde «esta porta é **permitida**?» e responde bem. Ninguém
respondia «esta porta **abre hoje**?».

Medido nesta própria matriz, e é o achado desta fase:

```
META/SEARCH_ADS  →  DECISAO = ALLOWED   e   ESTADO = CREDENTIAL_MISSING
```

As duas frases estão certas. Lidas juntas por quem tem pressa, dizem «pode ir»
sobre uma porta que não abre. Nasce `prontidao()`, com cinco eixos que não são
sinónimos:

```
FINANCIAL_COST · ACCESS_CREDENTIAL · APP_REVIEW · PERMISSION · POLICY
```

### O resultado, medido em toda a matriz

```
ZERO_DOLLAR_BUT_CREDENTIAL_GATED = 9 rotas
  META       SEARCH_ADS               · SEARCH_BRANDED_CONTENT
  THREADS    SEARCH_KEYWORD           · SEARCH_HASHTAG
  FACEBOOK   DISCOVER_ACCOUNT · FETCH_POST · FETCH_PROFILE · FETCH_VIDEO_METADATA
  INSTAGRAM  FETCH_PROFILE

READY_NOW = 5 rotas
  MASTODON SEARCH_HASHTAG · BLUESKY DISCOVER_ACCOUNT · TELEGRAM INCREMENTAL
  INSTAGRAM INCREMENTAL · INSTAGRAM FETCH_POST
```

```
NOVE ROTAS DESTA CASA CUSTAM ZERO DÓLARES E ESTÃO TRANCADAS.
USD_COST = 0 NÃO SIGNIFICA EXECUTÁVEL AGORA — e é mais perigoso que um preço
alto, porque o número zero convida a chamar-lhe «grátis» e a dá-la por pronta.
```

---

## F · DELTA

```
SUPPORTED = SIM
CONTRACT  = SINCE · UNTIL · LAST_SEEN_TIME · KNOWN_IDS · MAX_ITEMS
```

Vocabulário **fechado**: nome inventado levanta `JanelaInvalida`. Nenhum
checkpoint global nasce aqui — `coleta_checkpoint.conteudo_persistido()`
continua a ser o dono de «o que é conhecido», e o adaptador só recebe o conjunto
pronto.

```
O CALLER FORNECE JANELA E ESTADO. O SCRAP RESPEITA.
```

**Provado offline:**

```
RUN 1                        → AD-A, AD-B, AD-C
RUN 2 (fonte devolve 5)      → AD-D, AD-E
SKIPPED_ALREADY_KNOWN        = 3
WINDOW_ENFORCED_BY           = SOURCE | LOCAL     ← dito, nunca deduzido
KNOWN_IDS_SOURCE             = DB | CALLER | NOT_ASKED
TRUNCATED_BY_MAX_ITEMS       = declarado
```

O terceiro campo é o que impede o buraco silencioso: **conjunto vazio não é
«nada conhecido»** — é indistinguível de «não perguntei».

E o porquê de isto ser requisito e não optimização:

```
A JANELA COMERCIAL NA UE É DE UM ANO. O QUE NÃO FOR COLHIDO ENQUANTO ESTÁ LÁ
DESAPARECE E NÃO VOLTA.
```

---

## G · COMENTÁRIOS

```
OLD_CLAIM         = «a rota grátis dá o NÚMERO de comentários, nunca o TEXTO»
                    motivo canônico: FREE_ROUTE_INSUFFICIENT_CAPABILITY
                    evidência citada: `scripts/instagram_janela.py`

MEASURED_EVIDENCE = o caminho citado NÃO EXISTE (o ficheiro vive em `coleta/`),
                    e o ficheiro certo mede o contrário:
                    «18 de 31 comentários declarados saíram COM TEXTO — 58%»
                    em 7 posts de 5 contas, deslogado.

NEW_STATE         = rota grátis: registada, PERMITIDA=NAO, ROUTE_NOT_ALLOWED
                    rota Apify: PROVED -> POSSIBLE_NOT_PROVED (zero runs)
                    motivo canônico: ROUTE_NOT_ALLOWED
```

```
COMMENT_COUNT != COMMENT_TEXT — continua verdade.
E 18/31 TAMBÉM NÃO É 31/31 — por isso nada virou PROVED.
```

O motivo do gasto muda de sítio, e a diferença é cara: «a rota grátis **não
sabe**» resolve-se com dinheiro; «a rota grátis **não pode**» não.

**A decisão de rota NÃO mudou** — `INSTAGRAM/FETCH_COMMENTS` continua a apontar
para a rota paga. O que mudou foi o motivo e o estado, que agora são verdade.

### E o mesmo defeito estava ao lado

`apify:instagram-scraper` em `FETCH_POST` também estava `PROVED`, também com
zero runs, também citando o caminho inexistente. Rebaixado.

```
DOIS `PROVED` SEM UM ÚNICO RUN, E OS DOIS ERAM NOSSOS.
UM `PROVED` QUE CITA O FICHEIRO DA OUTRA ROTA NÃO PROVA ESTA.
```

### O validador desta casa afirmava o erro que existe para apanhar

`gap_apify()` dizia «APIFY DISPENSÁVEL — rota livre cobre
`graph:/{page-id}/posts`» sobre uma rota que exige PPCA e que a casa não tem. E
deduzia o motivo canônico **do nome da plataforma**.

```
ANTES  FACEBOOK FETCH_POST  APIFY DISPENSÁVEL   rota livre cobre: graph:/{page-id}/posts
AGORA  FACEBOOK FETCH_POST  ROTA LIVRE TRANCADA graph:/{page-id}/posts existe e NÃO abre:
                                                CREDENTIAL_MISSING · ZERO_DOLLAR_BUT_CREDENTIAL_GATED
```

```
UMA ROTA QUE NÃO ABRE NÃO COBRE NADA.
O MOTIVO ESTÁ ESCRITO NA ROTA, NÃO NO NOME DA PLATAFORMA.
```

---

## H · ÁUDIO

```
OLD_SCOPE    = implícito: «o Instagram suporta áudio-only»
PROVEN_SCOPE = PER_ITEM — UM Reel, cujo manifesto DASH expôs faixa separada
GLOBALIZED   = NO
```

A prova da C10 **não foi rebaixada**: `instagram.reel.audio` continua `PROVEN`.
O que nasce é `ESCOPO_DA_PROVA`, que viaja ao lado do estado em vez de ser
deduzido dele.

```
pode_generalizar('instagram.reel.audio', 'ITEM')      -> True
pode_generalizar('instagram.reel.audio', 'ROUTE')     -> False
pode_generalizar('instagram.reel.audio', 'PLATFORM')  -> False
```

```
AUDIO-ONLY É PROPRIEDADE DO ITEM, NÃO DA PLATAFORMA. UM REEL NÃO É UM LOTE.
```

---

## I · PROVIDER RESIDUAL — E A COLUNA QUE É OBRIGATÓRIA

```
SE NÃO HOUVER MOTIVO COMPROVADO NA ÚLTIMA COLUNA, NÃO SE CHAMA APIFY.
```

| capability | official route | public route | local replacement | credential required | paid gap | apify candidate | **why paid still needed** |
|---|---|---|---|---|---|---|---|
| `META/SEARCH_ADS` | `ads_archive` | — | — | identidade confirmada | **NENHUM** | — | **nenhum — a rota oficial é grátis e cobre a Itália** |
| `META/SEARCH_BRANDED_CONTENT` | `branded_content_search` | — | — | **NÃO DOCUMENTADA** | **NENHUM** | — | **nenhum** |
| `THREADS/SEARCH_KEYWORD` | `keyword_search` | — | — | App Review | **NENHUM** | — | **nenhum — pagar seria comprar a volta a uma aprovação** |
| `INSTAGRAM/FETCH_COMMENTS` | **não existe em nenhum nível** | 58% medido, não permitido | — | — | **REAL** | `apify/instagram-comment-scraper` | a edge oficial exige token de quem criou a mídia; a pública é recusada por política. `FREE_ROUTE_UNAVAILABLE` |
| `FACEBOOK/FETCH_REELS` | **não existe** (`video_reels` não lê) | — | — | — | **REAL** | `apify/facebook-reels-scraper` | nenhuma rota oficial em nenhum nível de aprovação |
| `FACEBOOK/FETCH_POST` | `/{page-id}/feed` | — | — | PPCA | **condicional** | `apify/facebook-posts-scraper` | `AUTHORIZATION_BLOCK` — e **desaparece no dia em que o PPCA sair** |
| `INSTAGRAM/FETCH_TRANSCRIPT` | — | — | **ASR local, provado** | — | **NENHUM** | — | **nenhum — e nem Apify é permitida aqui** |
| OCR · PDF · keyframes · idioma | — | — | **local** | — | **NENHUM** | — | **nenhum — os bytes já estão na nossa mão** |

```
APIFY = RESIDUAL GAP PROVIDER.  NÃO  APIFY = META ENGINE.
```

E a escada que o roteador já aplica, por prioridade de classe:

```
0 PUBLIC_NATIVE · 1 OFFICIAL_API_FREE · 2 DIRECT_HTTP · 3 PUBLIC_BROWSER
4 LOCAL_EXECUTOR · 5 LOCAL_SESSION · 6 OFFICIAL_API_PAID · 7 APIFY
```

`APIFY` é o número mais alto da tabela, e há prova que reprova se deixar de ser.

---

## J · EXECUÇÃO REAL

```
META_REQUESTS = 0
APIFY_RUNS    = 0
PAID_RUNS     = 0
COST_USD      = 0
```

Toda a prova corre contra `tests/fixtures/META-BUILD-01/`, com o transporte
substituído no boundary externo. **Nenhum byte veio da Meta**: as fixtures foram
escritas a partir da documentação primária citada no estudo.

```
UMA FIXTURE ESCRITA A PARTIR DA DOC PROVA A NOSSA FIAÇÃO, NUNCA O
COMPORTAMENTO DA PLATAFORMA. As duas coisas precisam de provas diferentes, e
a segunda é o probe que esta missão NÃO executou.
```

---

## K · PROBES PREPARADOS — NENHUM EXECUTADO

| campo | **PROBE_1** | **PROBE_2** | **PROBE_3** |
|---|---|---|---|
| `CAPABILITY` | `meta.ads.search` | `meta.branded_content.search` | `threads` (perfil público) |
| `OFFICIAL_ENDPOINT_FAMILY` | `graph:/ads_archive` | `graph:/branded_content_search` | `threads:/profile_lookup` |
| `TARGET` | `page_id=1741459832625091` (BASF IT, do lote congelado) | `ig_username=bayer_italia` | um handle do lote |
| `CREDENTIAL_REQUIRED` | confirmação de identidade + app + token | **NÃO DOCUMENTADA** | App Review |
| `APP_PERMISSION` | não documentada para este nó | não documentada | `threads_profile_discovery` |
| `REQUEST_COUNT` | **1** | **1** | **1** |
| `COST_USD` | 0 | 0 | 0 |
| `WHAT_IT_PROVES` | que a rota devolve anúncio comercial italiano de concorrente real, e quais campos de UE chegam | **qual token o nó aceita** — a pergunta que a doc não responde | se a descoberta de perfil de terceiro responde |
| `STOP_CONDITION` | primeira resposta, com ou sem dados; **ou** qualquer erro. Nunca paginar | primeira resposta ou primeiro erro | primeira resposta ou primeiro erro |

```
UM CONCEITO POR PROBE. Misturar «a rota responde?» com «quantos itens tem?»
dá um probe que não responde a nenhuma das duas.
```

Os três já têm tecto de rede e tecto de gasto no executor — `teto_de_rede=` e
`teto_de_gasto=` — portanto o limite não é uma promessa escrita num documento.

```
UM TETO QUE VIVE NA PROVA MEDE A PROVA.
```

---

## L · RED TEAM E MUTAÇÃO

```
ATAQUES   = 35   (os 30 pedidos + 5 do caminho canônico)
RESULT    = PASS
MUTANTES  = 12
SURVIVORS = 0
```

Os cinco acrescentados provam o que a FASE 4 exige e nenhum dos 30 media: que o
pedido atravessa o **executor canônico**, que um ensaio não promove estado, que
a URL chamada é a oficial, que nenhum ficheiro `meta_*` nasceu, e que o custo é
zero.

---

## M · REGRESSÃO

```
TESTS_BEFORE  = 107 ficheiros · 95 PASS · 12 FAIL
TESTS_AFTER   = 108 ficheiros · 96 PASS · 12 FAIL
NEW_FAILURES  = 0
```

As doze falhas são **as mesmas doze** de antes, medidas por diferença de
conjuntos e não por contagem.

```
DOZE ANTES E DOZE DEPOIS NÃO PROVA QUE SÃO AS MESMAS DOZE. A comparação é
de nomes, não de número.
```

---

## N · O QUE MUDOU

```
1  a Meta ganhou DONO: duas capacidades canônicas, com adaptador e política
2  `META` entrou como SUPERFÍCIE DE TRANSPARÊNCIA, não como guarda-chuva
3  `ADVERTISEMENT` e `BRANDED_CONTENT` entraram no vocabulário do envelope
4  `prontidao()` separou dinheiro de credencial — 9 rotas a custo zero e trancadas
5  `gap_apify()` deixou de chamar «dispensável» uma rota que não abre
6  o motivo canônico do gasto passou a ser LIDO da rota, não deduzido da plataforma
7  dois `PROVED` sem run foram rebaixados
8  o contrato de janela nasceu, fechado, e provado com delta offline
9  `ESCOPO_DA_PROVA` cercou a generalização sem tocar na prova
10 a §89 entrou no know-how canônico, que passou a viver nesta linhagem
```

## O · QUAL A PROVA

```
· os três artefatos do estudo, por BLOB_SHA em commit remoto
· o censo, executando `scrap_capacidades` + `scrap_registo` + `CHECK`
· a ausência de ads/branded, por varredura do código
· o delta, por duas corridas offline com o mesmo adaptador
· o caminho canônico, por `COLLECT` real com transporte substituído
· 35 ataques e 12 mutantes, todos a custo zero
· a regressão, por diferença de conjuntos contra o baseline
```

## P · O QUE NÃO MUDOU

```
· nenhuma capacidade foi promovida — as duas novas nascem NOT_EXECUTED
· nenhuma rota foi executada contra a Meta
· a decisão de rota de INSTAGRAM/FETCH_COMMENTS continua a mesma
· a recusa de INSTAGRAM/FETCH_TRANSCRIPT continua de pé
· Collection global, Admission, Source Relevance, Intelligence, Portal e LIVE
  não foram tocados
· nenhum segundo runtime nasceu
```

## Q · O QUE CONTINUA DESCONHECIDO

```
· que token o nó `branded_content_search` aceita — a doc não o diz
· o rate limit da Ad Library — o erro 613 existe, o número não
· se a confirmação de identidade é concedida a esta casa
· se o PPCA ainda é concedido em 2026
· a profundidade máxima do `business_discovery`
· quantos Reels do lote têm faixa DASH de áudio separada
· o TTL da URL assinada da CDN — a resposta é ler o `oe`, não decorar
```

## R · RISCO RESTANTE — E UM DELES É UMA PERGUNTA PARA GENTE

**1 · `INSTAGRAM/INCREMENTAL` e `INSTAGRAM/FETCH_POST` aparecem `READY_NOW`.**

A `prontidao()` diz a verdade sobre o que está escrito: a rota
`instagram_janela.py` está `PERMITIDA=CONDICIONAL`, `ESTADO=PROVED`, sem
credencial em falta. Mas o estudo mediu que os **ToS §3.2 da Meta** dizem, por
escrito, que estar deslogado **não é defesa** — e essa rota lê `instagram.com`
deslogada.

```
A C10.5D RECUSOU A ROTA DE AQUISIÇÃO DO REEL. NINGUÉM DECIDIU SOBRE A JANELA.
```

Esta missão **não** mudou essa linha, e a razão é disciplina, não esquecimento:
mudá-la é decisão de política, e política é de gente. Fica declarado, com o
número ao lado, para que a decisão seja tomada e não herdada por omissão.

**2 · `meta.ads.search` tem rota ligada e estado que não promete.** Isso é
correcto hoje e é frágil amanhã: no dia em que alguém promover o estado sem
correr a rota, a fiação já está lá para obedecer. A prova `M12` existe
exactamente para reprovar esse dia.

**3 · As fixtures foram escritas a partir da documentação.** Elas provam a nossa
fiação. Se a Meta devolver forma diferente do que documenta, o adaptador quebra
no primeiro probe — e é para isso que o probe serve.

---

## T · A SEGUNDA VOLTA — O DEFEITO QUE A PRÓPRIA PROVA ENCONTROU

A retomada mandou provar que *«uma rota `ALLOWED` mas sem credencial retorna
estado de credencial/readiness, e NÃO chama Apify»*. Fui medir, e a medição
encontrou um defeito meu.

### O que estava errado

```
sr._executar(platform='META', capability='SEARCH_ADS')
  →  ESTADO = ROUTE_NOT_ALLOWED
  →  IDAS À REDE = 1   ·   https://graph.facebook.com/robots.txt
```

Duas coisas erradas numa só chamada:

1. **saiu para a rede real**, a um host da plataforma, por uma rota que não
   tinha como ser executada;
2. devolveu `ROUTE_NOT_ALLOWED` — «eu podia e decidi não» — quando a verdade
   era «falta-me a chave».

```
NÃO SE BATE À PORTA DE QUEM NÃO SE TEM CHAVE.
```

### ⚠️ E ISSO CUSTOU UMA REQUISIÇÃO REAL A UM HOST DA META

Registado sem arredondar, porque esta casa já pagou para aprender que uma
missão que mede o próprio excesso e se dá `PASS` transforma o portão num
comentário:

```
META_REAL_REQUEST_ATTEMPTS (nesta sessão) = 1
  host      graph.facebook.com/robots.txt
  quando    ao medir o comportamento da rota, ANTES de existir a trava
  resposta  robots.txt ilegível — nenhuma permissão foi afirmada
  API       nenhuma chamada · credencial NENHUMA · dados NENHUNS · custo 0
```

Não foi chamada de API, não usou credencial e não trouxe dado. Foi o portão de
transporte a fazer o que faz sempre: ler o `robots.txt` do host antes de
qualquer pedido. Mas a rota **não devia ter chegado ao portão de transporte**, e
é isso que a trava corrige.

```
DEPOIS DA TRAVA: IDAS À REDE = 0, medido com o boundary instrumentado.
```

### As três vezes que pus a trava no sítio errado

| tentativa | o que li | o que partiu | a lição |
|---|---|---|---|
| **1ª** | `escolhida['ESTADO']` da matriz | três provas do YouTube | `ESTADO DECLARADO NA MATRIZ != CREDENCIAL PRESENTE NO AMBIENTE` — o YouTube lê a chave do ambiente e pode tê-la com a matriz a dizer que falta |
| **2ª** | a sonda, mas **antes** da trava do gasto | a prova de que rota paga sem motivo é recusada | `RECUSAR GASTAR NÃO DEPENDE DO AMBIENTE. FALTAR A CHAVE DEPENDE.` A recusa mais forte fala primeiro |
| **3ª** | a sonda vetava o **ensaio** | a lei da C10.7 | `UM ENSAIO QUE SÓ PODE CORRER QUANDO JÁ SE SABE O RESULTADO NÃO É UM ENSAIO` |

A terceira foi a mais instrutiva. O ensaio existe **para descobrir**, e
«descobri que me falta a chave» **é uma medição** — custa zero e não toca na
rede. Em modo `NORMAL` a sonda continua a vetar, e tem de continuar: ali a
pergunta é «posso colher agora?», e a resposta é não.

### O dono certo já existia

`scrap_registo` tem a **sonda gratuita** — *«lê configuração, nunca chama
rota»* — e o `CHECK` já a consultava. A trava passou a perguntar-lhe em vez de
interpretar a matriz. Capacidade sem sonda não é barrada: quem não declarou como
se mede não passa a ser medido por palpite meu.

```
ONE CONCEPT → ONE OWNER. A pergunta «tenho a chave?» já tinha dono.
```

### E a prova do caminho canônico foi corrigida, não removida

Ela afirmava que o `COLLECT` devolvia três objetos. Depois da trava devolve
**zero**, e está certo: a rota está `CREDENTIAL_MISSING` e a trava para antes do
adaptador.

```
UMA PROVA QUE PRECISA DO DEFEITO PARA PASSAR É UMA PROVA DO DEFEITO.
```

A fiação até ao adaptador passou a provar-se pelo **registo**, que é o dono do
mapa `PLATAFORMA/CAPACIDADE → ADAPTADOR` — e o documento diz, onde antes não
dizia, que isso **não** é a prova do caminho inteiro.

### O estado final das duas capacidades

```
meta.ads.search              NORMAL = CAPABILITY_STATE_PROMISES_NOTHING
                             TRIAL  = TRIAL_ELIGIBLE
                             READY  = ZERO_DOLLAR_BUT_CREDENTIAL_GATED
meta.branded_content.search  idem
```

```
ATAQUES 42 · MUTANTES 12 · SURVIVORS 0
NEW_FAILURES 0 — baseline 97/109 com 12 falhas, depois 97/109 com AS MESMAS 12
```

---

## S · VEREDITO

```
META_OFFICIAL_FIRST = PASS

META_DEEP_GIT_STATE = PROVEN
KNOW_HOW_DELTA      = ATUALIZADO
AD_LIBRARY          = DECLARED · REGISTERED · WIRED · NOT_EXECUTED
BRANDED_CONTENT     = DECLARED · REGISTERED · WIRED · NOT_EXECUTED
SEGUNDO RUNTIME     = NENHUM
ADS != ORGANIC      = provado
BRANDED != ORGANIC  = provado
DELTA               = representado e provado offline
CREDENCIAL != DINHEIRO = separados, 9 rotas trancadas a custo zero
APIFY DEFAULT       = NAO
ATAQUES 42 · MUTANTES 12 · SURVIVORS 0 · NEW_FAILURES 0
APIFY_RUNS 0 · PAID_RUNS 0 · COST_USD 0
META_REAL_REQUEST_ATTEMPTS = 1 (robots.txt, antes da trava; zero depois dela)
```

```
A CASA DEIXOU DE NÃO TER PALAVRA PARA ANÚNCIO. E CONTINUA A NÃO TER PROVA
DE QUE CONSEGUE LÊ-LO — QUE É EXACTAMENTE O QUE UM PROBE SERVE PARA DESCOBRIR,
E O QUE ESTA MISSÃO NÃO EXECUTOU.
```

**HARD STOP.**
