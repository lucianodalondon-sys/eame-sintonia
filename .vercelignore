# O que nunca deve sair deste repositorio para um URL publico.
#
# A integracao Git implanta a PARTIR DA RAIZ do repositorio. Sem este ficheiro,
# tudo o que esta commitado fica publicamente descarregavel — inclusive o
# arquivo de investigacao, os pacotes canonicos e o codigo de auditoria.
# `outputDirectory` no vercel.json ja restringe o que e SERVIDO; isto restringe
# o que e sequer ENVIADO. Duas fechaduras, porque o deploy e publico e
# irreversivel.

# Segredos. Nao existem no tree, e continuam a nao poder aparecer.
.env
.env.*
.vercel

# Pacotes canonicos, investigacao e dados internos. Nada disto e conteudo de
# site: e a materia-prima de onde o site foi construido.
/build
/data
/docs
/handoff
/research
/prototype
/supabase

# Ferramentas: cadeia de ingestao, auditoria, testes, CI.
/scripts
/tests
/.github

# O PORTAO DA BUILD TEM DE CHEGAR A BUILD.
#
# `/italia-portale/audit` inteiro estava excluido, e `prebuild` corre
# `node italia-portale/audit/build-gate.mjs`. O ficheiro nunca chegava ao
# contentor: MODULE_NOT_FOUND, e o deployment morria antes de existir.
#
#     UM PORTAO OBRIGATORIO QUE O EMPACOTAMENTO APAGA NAO E UM PORTAO.
#
# Sobe so a CLOSURE MINIMA — medida seguindo os imports, nao adivinhada: o
# portao, o modulo que ele importa, a biblioteca desse modulo e o contrato que
# os dois leem. Mais nada de audit/ viaja.
#
# Subir NAO e servir: `outputDirectory` em vercel.json e
# `italia-portale/client`, e so o que la esta e publicado. Estes quatro
# ficheiros existem durante a build e desaparecem com ela.
#
# A negacao tem de vir depois de `audit/*`, nunca depois de `audit`: um
# directorio excluido nao se percorre, e `!` dentro dele nao teria efeito.
# Por isso `lib/` e re-incluida antes de se excluir o seu conteudo.
/italia-portale/audit/*
!/italia-portale/audit/build-gate.mjs
!/italia-portale/audit/ingestion-provenance.mjs
!/italia-portale/audit/CANONICAL-PACKAGE-CONTRACT.json
!/italia-portale/audit/lib/
/italia-portale/audit/lib/*
!/italia-portale/audit/lib/harness.mjs
!/italia-portale/audit/lib/pacote.mjs
/italia-portale/BASELINE

# Documentacao. Nada no portal liga a um .md; servi-los so alarga o publico.
*.md
**/*.md

# Configuracao que viajou com a pasta do cliente. A raiz e agora a autoridade
# de hosting: estes ficheiros nao sao lidos pelo Vercel e nao sao conteudo de
# site — servi-los so conta a quem olha como o deploy esta montado.
/italia-portale/client/vercel.json
/italia-portale/client/.vercelignore
/italia-portale/client/_ds/adama-brandwell/_adherence.oxlintrc.json
/italia-portale/client/.gitignore
/italia-portale/client/_ds/adama-brandwell/_ds_manifest.json
/italia-portale/client/_ds/adama-brandwell/readme.md
/italia-portale/client/vendor/LEGGIMI-VENDOR.md
