
====================================================================================================
DOMAIN: ARCHIVE
CONFIDENCE: high  |  realSource: window.ITALY_APP_MODEL.collections.archive (740 records, provenance REAL_DERIVED), built in italy-app-model.js lines 266-276 as an index over scienceRecords (88, from IG.SCIENCE), marketObservations (77, from IG.MARKET), competitorActivities (503, from IG.COMP_ACTIVITIES), voices (17, from IG.VOICES), events (18, from IG.EVENTS), news (8, from IG.NEWS) and windows (29, from ITALY_CANONICAL.windows). Measured: 740 rows, 740 unique ids, 0 duplicate ids, 0 null ids; provenance is REAL_SOURCE (711) or CANONICAL (29) — zero synthetic rows.
SUMMARY: The Archive screen (portale.html markup 2186-2232) is a paginated table of "intelligence captures": date, type, item title + crop/region subtitle, source, and a "CONNECTED TO" column, plus a text query, four dropdown filters (type / crop / region / company), nine coloured type chips with counts, and a right-hand drawer with a summary paragraph, crop/issue/region tags and up to six cross-links (opportunity, future signal, ADAMA product, competitor, source, research topic). Today every fact on that screen comes from window.ITALY_DEMO.ARCHIVE: 448 rows, of which 420 are procedurally generated in italy-demo-data.js lines 522-538 (type, source route, company, product, caseObj, signal and an English prose summary are all synthesised from a modulo over the demo CASES/SIGNALS arrays) and only 28 are real (news + bulletins + science re-shaped at lines 539-543). The screen also feeds the nav badge (line 2608, K.archive = 448), the Data-State panel Archive row (line 2530), the Source-detail "LATEST CAPTURES · N IN ARCHIVE" block (line 3368, markup 2311-2312) and the global search ARCHIVE group (line 3383). The real replacement, APP.collections.archive, already exists and holds 740 rows derived from the normalized collections, but it is currently dead code — declared as A_ARCHIVE0 at line 2467 and never read.

-- MODEL CHANGES --
  * archive :: Fix six measured field-mapping bugs in the push() calls at italy-app-model.js lines 269-275. They silently null or corrupt fields the Archive screen renders. These are not new facts — the data is already in ITALY_INGEST 
      fields: date (SCIENCE), title (EVENT), url (EVENT), source (NEWS), url (NEWS), crop (MARKET), crop (COMPETITOR)
      derivation: SCIENCE date: line 269 passes r.date||r.year, but scienceRecords maps date from r.DATE||r.date and IG.SCIENCE has neither — the real field is PUBLISHED_AT (measured 88/88 non-null, e.g. '2024-04-01'). Result today: 88/88 SCIENCE rows have date null. Fix scienceRecords to read S(r.PUBLISHED_AT). — EVENT title: line 273 passes e.name, mapped from e.NAME||e.name; IG.EVENTS field is EVENT (measured 18/18 non-null, e.g. 'EIMA International 2026'). Result today: 18/18 EVENT rows have title null. — EVENT url: mapped from e.URL||e.url; the real field is OFFICIAL_URL (18/18). Also line 273 passes e.location as the SOURCE column, so the source reads 'BolognaFiere, Bologna' / 'Piemonte'; use ORGANIZER 
  * archive :: The view needs a stable source join. Today archive.source is a free-text display label mixing four different vocabularies (institution names like 'University of Verona', registry codes like 'IT-SRC-AGRIFOOD', platform na
      fields: sourceId, sourceName
      derivation: sourceId, derived only from real per-record source fields, never guessed: SCIENCE -> r.SOURCE_ID (measured: all 88 = 'IT-SRC-OPENALEX'); MARKET -> m.SOURCE_ID (all 77 = 'IT-SRC-AGRIFOOD'); VOICE -> v.SOURCE_ID (all 17 = 'IT-SRC-YOUTUBE'); COMPETITOR -> from a.platform, META_ADS_LIBRARY (414) -> 'IT-SRC-META', YOUTUBE (89) -> 'IT-SRC-YOUTUBE' (both platforms are registered sources in IG.SOURCES); NEWS -> exact match of n.PUBLISHER against IG.SOURCES[].NAME, which resolves 6 of 8 ('AgroNotizie (Image Line)'->IT-SRC-PROBE-003, 'ADAMA Italia'x2->IT-SRC-PROBE-020, 'Bayer Crop Science Italia'->IT-SRC-PROBE-019, "L'Informatore Agrario"->IT-SRC-PROBE-002, 'Terra e Vita (Edagricole)'->IT-SRC-PROBE-00
  * archive :: The 'CONNECTED TO' column, the drawer link list and the aCase/aCompany filters need explicit relation keys instead of the demo's caseObj/signal/product/company object graph.
      fields: company, legacyCaseId, competitorProducts
      derivation: company = a.company for COMPETITOR rows only (measured 503/503 non-null, 11 distinct: BASF 138, FMC 102, Corteva 94, Bayer 86, Syngenta 55, UPL 13, Sipcam 6, Certis 4, Gowan 2, Sumitomo 2, Nufarm 1); null for all other kinds. legacyCaseId = w.LEGACY_CASE_ID for WINDOW rows only (measured 29/29 non-null, exactly one row per IT-OPP-001..029); null for all other kinds — no other collection carries a case link, and IG.OPPORTUNITIES/IG.FUTURE_SIGNALS SOURCE_IDS are source-registry ids, measured 0 of 15 matching any archive record id. competitorProducts = a.products for COMPETITOR (102/503 non-empty) — these are COMPETITOR products, never ADAMA products.
  * archive :: Dates arrive in five incompatible shapes and 219 rows have none. Presentation may sort and compute days from supplied dates (§7) but must not invent them, and the list currently has no sort at all (it is in push order, k
      fields: date, dateISO, dateState, daysFromRef
      derivation: Keep date as the raw upstream string for display. Add dateISO = the value only when it already parses as YYYY-MM-DD, plus MARKET's DD/MM/YYYY (40 rows) converted mechanically. Measured shapes over 740: ISO 457, DD/MM/YYYY 40 (IG.MARKET.PUBLICATION_DATE), literal 'NAO SEI' 18 (17 VOICE + 1), range '2026-11-10 a 2026-11-14' 5 (EVENT), '2026-08/09' 1, null 219. After the SCIENCE PUBLISHED_AT fix above, null drops to 131 and dateISO coverage rises to 545/740 = 73.6%. dateState = 'EXACT' | 'RANGE' | 'PERIOD' | 'UNKNOWN' (the 18 'NAO SEI' rows are UNKNOWN, not a date). daysFromRef = AM.daysFrom(dateISO) using APP.referenceDate 2026-09-02 only — no new Date().
  * archive :: Vocabulary honesty for the crop filter. The 47 distinct crop values are five mutually unintelligible vocabularies and must not be presented as one dropdown of crop keys.
      fields: crop, cropVocab
      derivation: cropVocab = 'CANONICAL' for the 29 WINDOW rows (English canonical names: Grapevine, Maize, Olive, Sugar Beet, Apple, Wheat, Durum Wheat, Tomato, Rice, Soybean), 'UPPER_CODE' for SCIENCE/VOICE/NEWS (OLIVE, VINE, DURUM_WHEAT, MAIS, MAIZE, CEREAL...), 'LATIN' for the COMPETITOR rows whose crops[0] is a binomial (Vitis vinifera 42, Zea mays 59, Olea europaea 12, Solanum lycopersicum 4, Helianthus annuus 5, Malus domestica 3, Beta vulgaris 1, Oryza sativa 1, Citrus 5), 'GENERIC_IT' for the COMPETITOR rows whose crops[0] is a generic Italian word (colture 29, cereali 19, frutta 3), and null where crop is null (401/740 non-null, so 339 rows have no crop at all). The engineer must NOT map these onto
  * archive :: Presentation metadata only (§4). Legacy typeColor is measured to be the single constant '#B1A9A7' on all 448 demo rows, so nothing informational is lost by moving colour under .ui.
      fields: ui.color, ui.order
      derivation: A hardcoded per-kind map in the model: { COMPETITOR, SCIENCE, MARKET, WINDOW, EVENT, VOICE, NEWS } -> { color, order }. Not derived from any record; pure layout. This is the one archive field allowed to be authored rather than ingested.
  * archive :: Optional but cheap insurance for id collisions in the next intelligence package.
      fields: recordKey
      derivation: recordKey = kind + ':' + id. Today ids are already globally unique (measured 740 unique of 740, 0 duplicates, 0 nulls), so this is not required for correctness now — but the archive is a union of seven independently-numbered upstream sequences and nothing upstream guarantees that stays true. Use recordKey for state.archiveId; keep .id for display and deep links.

-- FIELDS THAT DIE (13) --
  [REMOVE_SUBCOMPONENT] region @ portale.html 3318 (aRegion filter clause), markup 2195 (region <select>), markup :: This is the biggest loss and it must not be softened. Measured: 0 of 740 real archive rows carry a region, and only 29 (3.9%) can get one at all — the canonical WINDOW rows, whose REGION is one of 9 Italian regions. Ever
  [REMOVE_SUBCOMPONENT] summary @ markup 2222 (drawer body paragraph), 3318 (text-query match string) :: No real equivalent exists. The 420 demo summaries are template prose generated at italy-demo-data.js line 536 ('Regional bulletin item reporting X situation on Y in Z, with crop stage and monitoring guidance.') — pure in
  [REMOVE_SUBCOMPONENT] product (ADAMA product on a record) @ 3337 (drawer link kind 'ADAMA PRODUCT'), 3318 (text-query match string), 3383 (s :: 0 of 740 real archive rows carry an ADAMA product. The demo attaches one to 116 of 448 rows by picking the case's primary product (italy-demo-data.js line 528). Nothing in IG.SCIENCE, IG.MARKET, IG.VOICES, IG.NEWS, IG.EV
  [REMOVE_SUBCOMPONENT] signal (link from a record to a future signal) @ 3336 (drawer link kind 'FUTURE SIGNAL'), 3329 (linkLabel fallback 'Signal · issu :: 0 of 740. The demo assigns a signal to every fifth row by modulo (italy-demo-data.js line 523, useSig = i % 5 === 4) — 84 of 448 rows. Reality has 3 FUTURE_SIGNALS and their SOURCE_IDS are source-registry codes (IT-SRC-O
  [SHOW_UNKNOWN] caseObj (link from a record to an opportunity) @ 3318 (aCase filter clause), 3329 (linkLabel/linkColor for the CONNECTED TO colum :: Survives on 29/740 = 3.9%, and only through ITALY_CANONICAL.windows[].LEGACY_CASE_ID — exactly one row per IT-OPP-001..029. The demo attaches a case to 351 of 448 rows. Consequence to state plainly: the 'CONNECTED TO' co
  [DERIVABLE_FROM_REAL] typeColor (per-type colour on the record) @ markup 2209 (row type dot), markup 2218 (drawer border), markup 2219 (drawer typ :: Not a loss. Measured, all 448 demo rows carry the single value '#B1A9A7' — it is a constant, not information. Move it to record.ui.color as an authored per-kind map (§4 explicitly allows this).
  [DERIVABLE_FROM_REAL] dateFull (long localized date) @ markup 2221 (drawer date line '{{ dr.dateFull }} · {{ dr.source }}') :: Derivable only for the rows that actually have a parseable date — 545/740 = 73.6% after the SCIENCE PUBLISHED_AT fix, 457/740 before it. The other 195 must render the dateState label (UNKNOWN / RANGE / PERIOD), not a fab
  [DERIVABLE_FROM_REAL] days (age in days) @ italy-demo-data.js 538/545 (the only sort key the archive has ever had) :: Derivable as daysFromRef from APP.referenceDate for the 545 rows with a parseable date; 195 rows can never be sorted by date. Note the real collection has NO sort at all today — it is in push order (88 SCIENCE, then 77 M
  [REMOVE_SUBCOMPONENT] sourceRoute / isDemo / the 'DEMONSTRATION RECORD' source label @ 3322-3324 (archDeco sets isDemo, sourceL, sourceRouteL), i18n keys PROV.DEMO_REC :: This whole apparatus exists only to label the 420 fabricated rows. With synthetic rows at 0 it becomes dead code. Remove isDemo/sourceL/sourceRouteL from archDeco and read a.source directly. Leave the PROV.DEMO_RECORD an
  [SHOW_UNKNOWN] type (the 9 legacy archive types) @ 3331 (hardcoded archiveTypes array), 3332 (chips), markup 2193 (type <select>),  :: The 9-value vocabulary ('Field bulletin', 'Regulatory record', 'Label', 'Scientific paper', 'Researcher signal', 'Technical article', 'Competitor activity', 'Producer-organization update', 'Monitoring observation') does 
  [DERIVABLE_FROM_REAL] company options @ markup 2196 (company <select> bound to the shared {{ companies }} prop) :: The filter itself survives — 503/740 = 68.0% of rows carry a company. But the option list is currently the competitor-domain prop built from D.COMPANIES (6 names: BASF, Bayer, Corteva, FMC, Syngenta, UPL) while the real 
  [DERIVABLE_FROM_REAL] crop options @ markup 2194 (crop <select> bound to the shared {{ cropOptions }} prop, built at  :: Silent breakage if left as is. cropOptions holds the 10 English canonical crop names from the demo cases; against the real archive those match only the 29 WINDOW rows, so picking any crop would appear to reduce a 740-row
  [REMOVE_SUBCOMPONENT] region options @ markup 2195 (bound to the shared {{ regionOptions }} prop) :: Consequence of region dying. The <select> element goes away entirely; do not leave a disabled or empty dropdown.

-- PLAN --
  1. Step 1 — Fix the model's field-mapping bugs first, in italy-app-model.js, before touching any view. In scienceRecords (line 179) read S(r.PUBLISHED_AT) for date. In events (line 215) read e.EVENT for name and e.OFFICIAL_URL for url, and add location: S(e.LOCATION) and organizer: S(e.ORGANIZER). In news (line 221) read n.PUBLISHER for outlet and n.SOURCE_URL for url. Verify with the node recipe that SCIENCE date goes 0/88 -> 88/88, EVENT title 0/18 -> 18/18, EVENT url 0/18 -> 18/18, NEWS source 0/8 -> 8/8, NEWS url 0/8 -> 8/8.
  2. Step 2 — In the archive builder (italy-app-model.js lines 266-276), stop passing m.product as the MARKET crop; pass null and add a separate series field carrying m.product. Verify the archive crop tally loses BLTPAN|PAN, MAI|FEED, DUR|UNKNOWN and the six olive-oil grade strings (77 rows affected, 12 bogus crop values removed).
  3. Step 3 — Extend the push() signature to carry the new fields: sourceId, sourceName, company, legacyCaseId, competitorProducts, dateISO, dateState, daysFromRef, cropVocab, series, evidence, ui. Derive each exactly as specified in modelChanges — every value must name its IG field, and anything with no IG field stays null. Re-measure: sourceId coverage must land on 691/740, company on 503/740, legacyCaseId on 29/740.
  4. Step 4 — Add the authored per-kind ui map (color + order) for the 7 kinds under record.ui. This is the only authored data allowed in the archive (§4). Do not derive it from ITALY_DEMO.
  5. Step 5 — Add recordKey = kind + ':' + id and expose archive.byKey for O(1) drawer lookup. Ids are measured unique today (740/740), so this is defensive, not corrective.
  6. Step 6 — In portale.html, reuse the already-declared but dead A_ARCHIVE0 at line 2467 (rename to AR) as the single source for the whole domain.
  7. Step 7 — Replace line 2530 (Data-State) with APP0.archive.real / APP0.archive.demo. Expect the panel to change from 'real 28 / demo 420' to 'real 740 / demo 0'.
  8. Step 8 — Replace line 2608 K.archive with AM.counts.archive (448 -> 740).
  9. Step 9 — Rewrite line 3318: filter on a.kind (not a.type), a.crop, a.company, a.legacyCaseId, a.sourceId; DELETE the aRegion clause; change the text-query string from (title + summary + source + product) to (title + source + crop + issue + company). Add the explicit sort (dated newest-first via daysFromRef, undated last grouped by kind) — the real collection has no sort at all today.
  10. Step 10 — Delete the hardcoded archiveTypes array at line 3331 and rewrite line 3332 to derive kinds and counts from AR. Remove the unguarded .find(...).typeColor — it is a latent throw and the colour now comes from the ui map.
  11. Step 11 — Rewrite archDeco (3320-3329): drop isDemo/sourceL/sourceRouteL/descriptor (all demo-only), drop the title re-composition at 3326 (real titles are publisher-authored strings and must not be split on ' · ' and re-translated — §11), add dateLabel from date/dateState, keep typeL/cropL/issueL, and redefine linkLabel/linkColor per fieldsThatDie.
  12. Step 12 — Rewrite the drawer at 3333-3341: lookup by recordKey, delete the ADAMA PRODUCT, FUTURE SIGNAL and RESEARCH TOPIC link entries (the last one is the D.SCI_THEMES read at line 3340), keep SOURCE (join on sourceId) and COMPETITOR, keep OPPORTUNITY only where legacyCaseId is non-null, and replace dr.summary with the per-kind evidence block.
  13. Step 13 — Rewrite line 3368: srItems = AR.filter(a => a.sourceId === sr0.sourceId). Coordinate with the SOURCES domain owner — sources.id is demo-polluted for 6 of 31 rows (SRC-15, SRC-36, SRC-12, SRC-25, SRC-13, SRC-22) and must not be the join key.
  14. Step 14 — Rewrite line 3383 (global search ARCHIVE group): drop a.region and a.product from the match string, add a.issue and a.company, and give the label a kind fallback for the 18 title-null rows.
  15. Step 15 — Update renderVals' exported props (3449-3452): add archiveCropOptions and archiveCompanyOptions built from the archive itself; remove aRegion/setARegion from the export; remove aRegion from hasArchiveFilters (3451), clearArchive (3451) and archiveWith (2422); remove aRegion from state (2382).
  16. Step 16 — Apply the markup edits listed in markupImpact: delete the region <select> (2195), rebind the crop and company selects (2194, 2196), rebind typeColor -> ui.color (2209, 2218, 2219), fix both row subtitles (2210, 2312), replace the drawer summary block (2222), make the tag row (2223) render only non-empty tags, and reword arcSub in italy-i18n.js.
  17. Step 17 — Add it/en ARCHTYPES entries for the 7 real kinds (en.ARCHTYPES is measured empty today, so English currently falls through to raw keys) and add labels for the dateState tokens.
  18. Step 18 — Re-measure and record the honest post-migration numbers on screen: 740 rows, 7 type chips, crop filter usable on 401 rows across 5 vocabularies, company filter on 503, source filter on 691, case link on 29, region filter gone.

-- RISKS --
  ! The archive count on the nav badge goes UP (448 -> 740) while the screen gets visibly thinner: no region column content, no summary paragraph on 323 rows, an empty CONNECTED TO cell on 711 rows. Someone will read that as a regression. It is the opposite — 420 of the old 448 rows were fabricated. Say so before the review, not after.
  ! The 29 WINDOW rows are the ONLY rows with a region and the ONLY rows with a case link, and they are also the least document-like rows in the set: ITALY_CANONICAL.windows[].PROVENANCE is measured 'EXPECTED_NORM', they have no url (29/29 null), and their source reads the literal 'CANONICAL'. Any feature rebuilt on them (a region facet, a per-case evidence list) would be resting an observational clai
  ! The crop filter is a correctness trap. If line 2194 keeps reading the shared {{ cropOptions }} prop, the UI will silently look fine and return at most 5 rows for any crop — a wrong-looking-right failure, the worst kind. This must be caught in the same commit that touches the model, not later.
  ! COMPETITOR rows are 503 of 740 = 68% of the archive. Sorting by date newest-first will make the first pages almost entirely Meta ad records, and the '2026-01-30'-style start dates are ad campaign starts, not publication dates of an observation. Do not let the date column imply 'when this was observed'. Also §9: an archive row must not inherit REACHED_IN_ITALY framing from its geoClass.
  ! archive.source is a display label mixing four vocabularies; if the engineer wires the aSource filter to it instead of the new sourceId, the filter will half-work (it will match 'YOUTUBE' across both VOICE and COMPETITOR rows but split 'University of Verona' from 'IT-SRC-OPENALEX' even though both are OpenAlex science). Join on sourceId only.
  ! The source-detail cross-link depends on a collection owned by another domain. sources.id is demo-polluted for 6 of 31 rows. If the SOURCES owner does not expose a clean sourceId, the 'LATEST CAPTURES · N IN ARCHIVE' block will show 0 for those 6 sources and no one will notice.
  ! 18 rows carry the literal string 'NAO SEI' in the date field and 17 of them are VOICES (all 17 IG.VOICES.DATE are 'NAO SEI'). If any code path does new Date(a.date) or a string sort, these will land somewhere arbitrary. Handle dateState explicitly.
  ! Line 3332's unguarded D.ARCHIVE.find(a => a.type === t).typeColor throws the moment a listed type has zero rows. A naive port that swaps D.ARCHIVE for the real collection while keeping the hardcoded 9-type list at line 3331 will crash the Archive view immediately, because 5 of those 9 types have zero real rows.
  ! The title re-composition at line 3326 splits the title on ' · ' and translates each part through T.ARCHTYPES/T.CROPS/T.ISSUES. That was safe on generated titles. On real titles it is not — it would chop publisher headlines and scientific paper titles (e.g. the Xylella fastidiosa paper title) at arbitrary separators and risks truncating a taxonomic name, which §11 forbids. This code must be deleted

-- OPEN QUESTIONS --
  ? Should the 29 WINDOW rows (upstream PROVENANCE 'EXPECTED_NORM', no url, source literal 'CANONICAL') be in the archive at all? Excluding them gives a cleaner 711-row archive of genuinely captured artefacts — but then legacyCaseId and region drop to 0/711 and the 'VIEW IN ARCHIVE →' link from every opportunity detail mus
  ? Two of the 8 NEWS publishers do not resolve to a source-registry id because of name drift ('Agronotizie' vs 'AgroNotizie (Image Line)'; 'Consorzio Fitosanitario di Modena' vs 'Consorzio Fitosanitario Provinciale di Modena'). Should these be normalized upstream in the ingest, or left null? I did not fuzzy-match them — t
  ? The crop dimension has 47 distinct values across 5 incompatible vocabularies (canonical English, uppercase codes, Latin binomials, generic Italian words, olive-oil grades). Is normalizing them an upstream ingest job for the next package, or does the portal ship a visibly-grouped dropdown that admits the vocabularies ar
  ? Should COMPETITOR ad copy (IG.COMP_ACTIVITIES.text, 392/503) be shown verbatim in the drawer? It is public observed communication and §11 says do not translate it — but it is also marketing prose from a competitor rendered inside an ADAMA product, which may need a label rather than a design decision from me.
  ? The 'CONNECTED TO' column loses its meaning once opportunity links cover only 3.9% of rows. Does the product owner want the column repurposed (source / company / kind-appropriate relation) or emptied and the grid narrowed? §12 says preserve component proportions, so I propose repurposing, but this is a product call.
  ? italy-app-model.js does not read window.ITALY_LABEL_VERDICTS at all. That is out of my domain, but if a future archive kind carries label-audit records this is where they would land — flagging it so it is not forgotten.

====================================================================================================
DOMAIN: RESEARCHERS / PEOPLE / TSR (Sources → People directory, Person detail, Science screen "RESEARCHERS" + "INSTITUTIONS" cards, Theme detail "KEY RESEARCHERS", Field Sales "FIELD SALES REPRESENTATIVES" card)
CONFIDENCE: high  |  realSource: APP.collections.people — a NEW merged collection over IG.RESEARCHERS[60] ∪ IG.PEOPLE[15], joined on accent-folded PERSON name (9 overlaps measured: Sattin, Scarabel, Panozzo, Loddo, Marzachì/Marzachi, Galetto, Angelini, Filippin, Bosco) → 66 distinct people. Plus APP.collections.researchers rebuilt with correct field names (IG.RESEARCHERS, 60). Plus IG.THEMES[5] for theme labels, INSTITUTIONS_TOP and the real works/authors counts. Provenance for both: REAL_SOURCE, SOURCE_ID = IT-SRC-OPENALEX (whose own LIMITATIONS field states "afiliacao e do AUTOR, nao do estudo"). D.TSR stays where it belong
SUMMARY: Today the portal shows a directory of 39 "people monitored" (Sources → People), a per-person detail page with connected opportunities, connected future signals, publications and a 5-row dated "SOURCE HISTORY", a "RESEARCHERS · 14" card plus an "INSTITUTIONS · 12" card on the Science screen, a "KEY RESEARCHERS" panel on each theme, and a "FIELD SALES REPRESENTATIVES · 7" card on the Field Sales screen. Every single one of those facts comes from window.ITALY_DEMO. D.PEOPLE (39) = 14 profiles whose NAMES come from the older italy-real-intelligence.js fixture but whose observation dates, related cases, signals and topics are invented (lastDays = 20 + i*3), plus 18 fully synthetic "Demo profile · <role>" records built from a seeded PRNG, plus the 7 TSR demo reps concatenated in. The real backing exists and is unused: IG.RESEARCHERS (60, all fields 60/60 populated: PERSON, ORCID, OPENALEX_ID, INSTITUTIONS, THEME, WORKS_IN_SCOPE, LAST_ACTIVITY, IDENTITY_STATUS) and IG.PEOPLE (15, all fields 15/15, with separate IDENTITY_EVIDENCE and ROLE_EVIDENCE). italy-app-model.js already builds collections.researchers from IG.RESEARCHERS but the field names are wrong, so 60/60 records come out with org null, url null, crops [], issues [], and role = the literal Portuguese string "NÃO SEI — o índice não declara papel"; the model does not expose IG.PEOPLE at all.

-- MODEL CHANGES --
  * people (NEW) :: There is no real people directory in the contract today. IG.PEOPLE is not exposed at all and IG.RESEARCHERS comes out empty-fielded. The Sources → People screen, the person detail, the search PEOPLE group and the 'People
      fields: id — RESEARCHERS.ID (IT-PER-001..060) preferred on merge; PEOPLE.ID (IT-PER-061..075) otherwise. Keep alsoIds:[] for the merged pair so old links resolve., name — PERSON verbatim, accents preserved, never translated (PRODUCT LAW §11)., category — CATEGORY. Measured union: RESEARCHER 62, INSTITUTIONAL_EXPERT 2, COMPANY_PERSON 1, CREATOR 1., roleCat — presentation label mapped 1:1 from category: RES
      derivation: Union of IG.RESEARCHERS[60] and IG.PEOPLE[15], keyed on PERSON normalized with .normalize('NFD').replace(/[̀-ͯ]/g,'').toLowerCase(). 66 distinct records. Every string passes through UNK(v) = /^N[ÃA]O SEI/i.test(v) ? null : S(v) BEFORE anything else — 60/60 ROLE, 60/60 FACT_REGION and 6/60 ORCID carry that literal marker today.
  * people.categories / people.institutions (NEW derived indexes on the collection) :: So the view stops hand-writing a 9-item pCats literal and stops reading D.INSTITUTIONS.
      fields: categories: [{ key, label, count }], institutions: [{ name, researchers }] — 36 entries, sorted desc. NO 'type' field: the demo's National research body / University / Research foundation classification is a guess from a name prefix and does not exist upstream., nonItalianNote: the theme recorte returned non-Italian affiliations (measured: All-Russian Institute of Plant Protection 2, State Educati
      derivation: categories = distinct roleCat over the 66 merged records, ordered by count desc. institutions = flatten IG.RESEARCHERS[*].INSTITUTIONS into {name, researchers} counted; measured 36 distinct, top: Institute for Sustainable Plant Protection 15, National Research Council 14, University of Turin 9, University of Perugia 8, University of Bologna 5, Cereal Research Centre 4, University of Pavia 4.
  * researchers (REBUILD of the existing broken mapper) :: italy-app-model.js:188 reads NAME/ORGANIZATION/INSTITUTION/CROPS/ISSUES/SOURCE_URL — none of which exist in IG.RESEARCHERS. Result measured on all 60: org null, url null, crops [], issues [], role = 'NÃO SEI — o índice n
      fields: id, name, institutions, orgLabel, orcid, openalexId, theme, themeLabel, worksInScope, lastActivity, daysFromRef, identityState, role=null, factRegion=null, crops=[] (see fieldsThatDie), issues=[] (see fieldsThatDie), sourceId, provenance
      derivation: Same field list as `people` above, restricted to IG.RESEARCHERS. Keep the collection name so nothing else breaks; it becomes a filtered view of `people`.
  * publicationsForPerson(person) (NEW helper, expected to return [] almost always) :: The person detail has a 'PUBLICATIONS · TOPIC X' panel that today lists D.SCI_THEMES records — papers this person did not write. If the panel is kept it must join on real authorship.
      fields: title (SCIENCE.TITLE), publishedAt (PUBLISHED_AT), venue (VENUE), doi (DOI), sourceUrl (SOURCE_URL), materialType (MATERIAL_TYPE), Returns [] for 59 of 60 researchers. The panel must be OMITTED when empty, not filled from anywhere else.
      derivation: Join IG.RESEARCHERS.ORCID (bare id after stripping the orcid.org prefix) to IG.SCIENCE.ORCID. MEASURED: 88/88 SCIENCE records have an ORCID, but only 1 of 60 researchers matches any of them (25 works for that one person). Exact-name join: also 1/60. The join is real but almost empty.
  * dataState row (portale.html:2526-2533) :: The provenance panel currently has no People/Researchers row at all, so the reader cannot see that this layer just went from 0 real to 66 real.
      fields: { layer: 'Persone / Ricercatori', real: 66, derived: 0, demo: 0, note: 'OpenAlex + evidenza pubblica · affiliazione = autore, non studio' }
      derivation: real: AM.collections.people.count (66); derived 0; demo 0. And the existing Field Sales row keeps the 7 TSR on the demo side of the line.

-- FIELDS THAT DIE (17) --
  [REMOVE_SUBCOMPONENT] pr.history — the 5-row 'RECENT SIGNALS · SOURCE HISTORY' list @ portale.html:3372 (built), portale.html:2345 (rendered) :: Five dates and five sentences generated from one integer. No ingested collection contains any per-person observation timeline. There is exactly ONE real temporal fact about a person: RESEARCHERS.LAST_ACTIVITY, a single d
  [REMOVE_SUBCOMPONENT] pr.related / p.relatedCount — 'CONNECTED OPPORTUNITY CASES' and the 'N opportunities' counter @ portale.html:3310 (related: p.related.map), 2332, 2255 :: The demo builds this with CASES.filter(x => x.crop === r.crop).slice(0,3) — a person is attached to an opportunity purely because a fixture gave them a crop string. There is no person→opportunity edge anywhere: IG.OPPORT
  [REMOVE_SUBCOMPONENT] pr.signals / p.signalCount — 'CONNECTED FUTURE SIGNALS' and the 'Connected signals' KPI @ portale.html:3310 (signalCount), 2324, 2335, 2255 :: Same fabrication, over D.SIGNALS. IG.FUTURE_SIGNALS[3] has WHO_IS_TALKING as free text, not a person id — it cannot be joined to a person record without inventing the join. Delete the card and the header KPI tile.
  [SHOW_UNKNOWN] p.crops / p.issues / p.cropLabel / p.issueLabel — the crop and issue chips on every person card and on the person detail @ portale.html:3310, 3311 (p.crops[0] inside bizWhy), 3371, 2251, 2330 :: Measured: crops [] and issues [] for 60/60 real researchers. What DOES exist is THEME, one of five codes (VINE_FLAVESCENCE, DURUM_FUSARIUM, OLIVE_BACTROCERA, MAIZE_BORER_DIABROTICA, WEED_HERBICIDE_RESISTANCE), 12 researc
  [SHOW_UNKNOWN] p.region / p.regionShort — 'Region' on the person card, and the region in the person header line @ portale.html:3310 (regionShort), 3371, 2251, 2323 :: FACT_REGION is the literal 'NÃO SEI — a afiliação é do AUTOR, não do estudo' in 60/60 records. The source's own LIMITATIONS field says the same thing. Render 'NON OSSERVABILE DA FONTI ESTERNE'. The demo's dodge — 'Affili
  [REMOVE_SUBCOMPONENT] p.recentTopic — the free-text 'WHAT THEY TALK / RESEARCH ABOUT' paragraph, and the one-line summary on every directory card @ portale.html:3310, 3371, 3372 (reused as history row 0), 2252, 2330 :: For the 18 synthetic profiles it is a template string ('<issue> in <region> · <signal>'); for the 14 name-real ones it is a hand-written focus sentence in the old real-intelligence fixture. Nothing equivalent exists in I
  [DERIVABLE_FROM_REAL] p.last / p.lastDays — 'Last observed' on the header, the trailing timestamp on every directory card, on the science RESEARCHERS card and on the theme KEY RESEARCHERS rows @ portale.html:3310, 3371, 3372, 2080, 2173, 2255, 2325 :: Derivable but the MEANING changes and the label must change with it. Demo lastDays is invented (20 + i*3 for the real-named ones, seeded PRNG for the rest). Real LAST_ACTIVITY exists 60/60 — but it is the date of the las
  [REMOVE_SUBCOMPONENT] p.platform — 'Platform' on every directory card and in the person header @ portale.html:2251, 2323 :: A demo lookup table (PLATFORMS by role: 'LinkedIn · field notes', 'YouTube · Instagram', 'Technical bulletins'). No researcher record has a platform. For the single CREATOR (Riccardo Castaldi) a channel may be recoverabl
  [SHOW_UNKNOWN] p.identityLabel — the 'real identity' / 'demo profile' badge @ portale.html:3310, 2250 :: Replace with identityState, which is more honest and three-valued: ORCID_PRESENT_NOT_RESOLVED_HERE (54), NO_ORCID_IN_SOURCE (6), or evidence-backed via IG.PEOPLE.IDENTITY_EVIDENCE. 'Real identity' overstates an ORCID tha
  [REMOVE_SUBCOMPONENT] p.role as an enum ('Researcher', 'Agronomist', 'Technical advisor', 'Influencer / creator', 'Field expert', 'Producer voice', 'Institutional expert') and the 9-chip pCats filter row @ portale.html:3311 (p.role === 'Researcher'), 3359 (pCats literal), 3360, 3371 (i :: Five of the nine chips have zero real members: AGRONOMISTS / ENGINEERS, TECHNICAL ADVISORS, FIELD EXPERTS, PRODUCER / FARM VOICES, TECHNICAL SALES REPRESENTATIVES. Real CATEGORY values are only RESEARCHER (62), INSTITUTI
  [REMOVE_SUBCOMPONENT] pr.themeRecords — the 'PUBLICATIONS · TOPIC X' list on the person detail @ portale.html:3371, 2341 :: Today it lists the theme's demo records, i.e. papers the person did not write. The real ORCID join to IG.SCIENCE resolves for 1 researcher out of 60 (25 works). Keep the panel ONLY when publicationsForPerson() is non-emp
  [REMOVE_SUBCOMPONENT] researchers[].bizWhy / bizUse / bizNote — 'POTENTIAL EXPERT TO WATCH' and 'Relevant to <crop> · <issue> · technical watch and possible expert engagement' @ portale.html:3311, 2080 :: An invented commercial interpretation stacked on top of an invented crop. 'Potential expert to watch' is a Sintonia judgement no external source made, and it is asserted identically for all 8 rows, which makes it decorat
  [REMOVE_SUBCOMPONENT] institutions[].type ('National research body' / 'University' / 'Research foundation') and institutions[].records @ portale.html:3312, 2085 :: type is guessed from a name prefix (name.startsWith('CREA')||startsWith('CNR')) — not a fact. records counts demo D.RECORDS. The real replacement counts RESEARCHERS per institution (36 institutions), which is a different
  [SHOW_UNKNOWN] kpi.people = 39 and kpi.researchers = 14 @ portale.html:3363 (K.people), 2077 (kpi.researchers), italy-demo-data.js:776-777 :: Not merely wrong — 39 deliberately includes the 7 synthetic ADAMA reps (KPI.people += TSR.length). The real numbers are 66 people and 60 (or 62 with IG.PEOPLE's extra researchers) researchers. The nav badge at portale.ht
  [KEEP_AS_LABELLED_DEMO] D.TSR on the core People directory and in the People count @ italy-demo-data.js:784 (PEOPLE: PEOPLE.concat(TSR)), portale.html:3310, 3360, 33 :: VERDICT: Field Sales, not Researchers. D.TSR is 100% derived from D.FIELD_MESSAGES (italy-demo-data.js:620) and is provenance SYNTHETIC_DEMO. PRODUCT LAW §5 allows it on the labelled Field Sales screen and forbids it fro
  [DERIVABLE_FROM_REAL] person→theme edge feeding 'KEY RESEARCHERS' @ portale.html:3314, 2173 :: Derivable — RESEARCHERS.THEME gives 12 researchers per theme, real. But the word KEY is not derivable: nothing upstream ranks a researcher. Rank by WORKS_IN_SCOPE and rename the panel to say so, or the panel keeps making
  [SHOW_UNKNOWN] 'Italian researchers' framing of the 60 @ portale.html:2077 heading, 3311, 3359 pCats 'RESEARCHERS' :: The 60 come from a theme-scoped OpenAlex query, and the affiliations measurably include non-Italian institutions (All-Russian Institute of Plant Protection 2, Weinsberg viticulture institute 3). The heading must read 'ri

-- PLAN --
  1. 1. italy-app-model.js — add a shared sanitizer next to S()/N(): const UNK = (v) => { const s = S(v); return s && /^N[ÃA]O SEI/i.test(s) ? null : s; }. This is load-bearing: 60/60 ROLE, 60/60 FACT_REGION and 6/60 ORCID currently carry the literal 'NÃO SEI' marker and would otherwise print Portuguese debug text into an Italian UI.
  2. 2. italy-app-model.js:188-192 — rewrite the researchers mapper against the measured schema (PERSON, INSTITUTIONS array, ORCID, OPENALEX_ID, THEME, WORKS_IN_SCOPE, LAST_ACTIVITY, IDENTITY_STATUS, CATEGORY). Add themeLabel from IG.THEMES, daysFromRef via the existing daysFrom(), orgLabel from institutions.join(' · '). Verify org, orcid and worksInScope are now non-null 60/60 before going further.
  3. 3. italy-app-model.js — build the new merged `people` collection (66) over IG.RESEARCHERS ∪ IG.PEOPLE, keyed on accent-folded PERSON. Attach roleCat, role (5/66 non-null), roleEvidence, identityEvidence, identityState. Expose people.categories and people.institutions (36 entries, counting researchers not papers).
  4. 4. italy-app-model.js — add publicationsForPerson(person) joining bare ORCID to IG.SCIENCE.ORCID, and a hasPublications flag. Expect 1/60 true; do not add a name-similarity fallback.
  5. 5. italy-app-model.js:282-289 and 313 — register `people` in collections so it appears in provenanceSummary and counts. Replace the searchIndex researcher entry at line 303 with a people-based one indexing name, institutions, themeLabel and orcid.
  6. 6. portale.html:3310 — replace the D.PEOPLE deco with the AM.collections.people deco. Drop related/signalCount/cropLabel/issueLabel/regionShort/identityLabel from the deco; keep only go, initials and the ui colour.
  7. 7. portale.html:3311 — rebuild `researchers` from AM.collections.researchers sorted by worksInScope desc, slice(0,8). Delete bizWhy and bizUse.
  8. 8. portale.html:3312 — replace D.INSTITUTIONS with AM.collections.people.institutions. Drop i.type and i.themeLabel.
  9. 9. portale.html:3314 — rebind th.people to AM.collections.researchers filtered on the upstream THEME code, sorted by worksInScope. This needs D.SCI_THEMES entries to carry the upstream code (IT-THEME-001..005) — add a small explicit map in the view, or resolve the open question below first.
  10. 10. portale.html:3370-3372 — rebuild pr0/pr from AM.collections.people. DELETE the history array and the D.TODAY / D.fmt reads inside it. DELETE isTsr and messages. Gate the publications panel on hasPublications.
  11. 11. portale.html:3359-3361 — drive pCats from AM.collections.people.categories; rebind peopleCatChips and visiblePeople.
  12. 12. portale.html:3363 and 3391 — replace K.people with AM.collections.people.count (66) and override kpi.researchers with AM.counts.researchers (60). Re-check the nav badge at 3387/2608, which sums K.orgs + K.people.
  13. 13. portale.html:3381 — rebind the search PEOPLE group to AM.collections.people.
  14. 14. portale.html:3129, 3406, 921-922 — leave D.TSR in place on Field Sales, but: remove the two routes that deep-link from core screens into the demo people category (portale.html:3114 and the goTsr at 3404), rename 'Marco R.' and 'Luca F.' in italy-demo-data.js:557-558 to the FIELD SALES DEMO · <REGION> pattern used by the other five, and add the DEMO badge to the card at 921.
  15. 15. Coordinate with the Competitor domain owner on portale.html:2599: the 8 type-PEOPLE demo activities call openPerson with demo P-* ids that will no longer resolve. Either drop the subtype or make the handler a no-op — do not leave a dangling person link.
  16. 16. Apply the markup edits listed in markupImpact, deleting the four dead cards (connected opportunities, connected signals, source history, TSR quotes) and shrinking the person-detail header from 3 KPI tiles to 2. Preserve card shells, radii, colours, avatar circles and grid proportions (PRODUCT LAW §12).
  17. 17. Add the 'Persone / Ricercatori' row to dataState at portale.html:2526-2533 (real 66, derived 0, demo 0) so the provenance panel reflects the change.
  18. 18. Add IT/EN strings for the new labels (tema monitorato, opere nel recorte, ultima attività registrata, affiliation caveat, identity-state values) to italy-i18n.js. Do NOT translate person names, institution names or the five Portuguese ROLE strings without a decision — see open questions.
  19. 19. Verify at runtime: AM.counts.people === 66, AM.counts.researchers === 60, zero occurrences of the substring 'NÃO SEI' or 'NAO SEI' in any rendered prop, and zero remaining reads of D.PEOPLE outside the Field Sales screen.

-- RISKS --
  ! The People screen gets visibly emptier and older. Today a card shows platform, region, crops, issues, a topic sentence, '3 opportunities · 3 signals' and a reassuring '16d ago'. After the migration it shows a name, institutions, one theme, a works count and a real LAST_ACTIVITY that for 31 of 60 people is more than two years old (measured against REF 2026-09-02: 19 within 12 months, 10 at 12-24 mo
  ! The person detail loses three of its five cards. Connected opportunities, connected signals and source history all go, and the header drops from three KPI tiles to two. There is a strong temptation to backfill with 'related by theme' opportunity links. Do not: the theme→crop→case chain is exactly the fabricated join being removed.
  ! Count changes are user-visible in two places at once: People monitored 39 → 66 and Researchers 14 → 60. Both go UP, which reads as good news, but the underlying screen gets thinner. Do not let the rising count be used to justify keeping the thick card layout.
  ! The theme→researcher join needs D.SCI_THEMES to map onto the five upstream THEME codes. If that map is written carelessly (for example by title-matching), a researcher can be attached to the wrong theme — recreating the exact class of invented edge being deleted. It should be an explicit, reviewed 5-row literal.
  ! Nine people exist twice (once in IG.RESEARCHERS, once in IG.PEOPLE) under differently-accented spellings, and one of them, Maurizio Sattin, appears as IT-PER-051 and IT-PER-061. A naive union yields 75 records and inflates every count by 9. The accent-folded merge must be tested explicitly.
  ! Six researchers carry the literal 'NÃO SEI' in the ORCID field itself. Building an href from that value produces a link to https://orcid.org/NÃO SEI. Guard on the 'https://orcid.org/' prefix, not on truthiness.
  ! The list is not all Italian. Measured affiliations include All-Russian Institute of Plant Protection (2) and a German viticulture institute (3). Any heading or export that calls these 'Italian researchers' is a new factual error introduced by the migration itself.
  ! LAST_ACTIVITY is the date of the last work indexed inside the monitored query recorte. If it is rendered with the existing ago() relative formatter next to a person's name it will read as 'we last saw this person N days ago', which is a field-observation claim. Render an absolute date with an explicit label.
  ! Removing D.TSR from the People directory breaks two existing deep links (portale.html:3114 and goTsr at 3404) that navigate to sourceGroup PEOPLE with peopleCat TECHNICAL SALES REPRESENTATIVES. Both land on an empty filtered screen if only the data is changed and the routes are left behind.
  ! The 8 competitor 'PEOPLE mention' activities call openPerson with demo P-* ids (measured 8/8 resolving today). They are another domain's records but they break in this domain's view; without coordination the person route will throw or fall back to people[0], silently showing the wrong human.
  ! IG.PEOPLE.ROLE and ROLE_EVIDENCE are written in Portuguese ('coordenador do GIRE', 'duas rotas independentes: video publico e indice bibliografico'). Shipping them verbatim into an Italian-default UI is wrong; machine-translating a provenance statement is also wrong. This needs a decision, not a default.

-- OPEN QUESTIONS --
  ? D.SCI_THEMES has no upstream THEME code. Should the 5-row map (grapevine-phytoplasma → VINE_FLAVESCENCE, etc.) be written in the view, or should the Science-domain owner add the code to the theme record so both domains read one key? These two domains collide here; whoever goes second must not write a second map.
  ? IG.PEOPLE.ROLE carries five real Portuguese role strings and IDENTITY_EVIDENCE / ROLE_EVIDENCE are whole Portuguese sentences. Translate to IT/EN, show verbatim with a 'source note' label, or omit until the upstream pack supplies IT? PRODUCT LAW §11 protects names and original public quotes, but these are Sintonia's ow
  ? Mirco Casagrandi is a real ADAMA Italia employee (Marketing Technical Manager) present as a REAL_SOURCE record with public evidence. Should ADAMA's own people appear in an external-intelligence directory at all, and if so under which category label? He is currently the only COMPANY_PERSON.
  ? Does the person detail survive as a route at all for the 61 people who have no publications, no region, no role and no observation history — or should the directory card simply not be clickable for them? A detail page with two facts on it may be worse than no detail page.
  ? Should the two non-Italian-affiliated clusters (All-Russian Institute of Plant Protection, Weinsberg) be filtered out of the researcher list, or kept with the honest 'monitored theme, not Italian scope' framing? Filtering changes 60 into a smaller number that no upstream field justifies.
  ? IG.THEMES carries AUTHORS_IT (334, 235, 223, 62, 22 — total 876) while only 60 researchers were ingested. Should the Science screen state that 60 is a top-N slice of a much larger author population? Showing 60 without that denominator invites reading it as the whole field.

====================================================================================================
DOMAIN: SCIENTIFIC INTELLIGENCE (themes, research records, institutions, researchers, resistance)
CONFIDENCE: high  |  realSource: APP.collections.scienceRecords (88, from IG.SCIENCE) + APP.collections.researchers (60, from IG.RESEARCHERS) + APP.collections.resistance (34, from IG.RESISTANCE) + a NEW APP.collections.scienceThemes projected from IG.THEMES (5). Institutions must be DERIVED, and there are two distinct and non-interchangeable derivations: (a) IG.SCIENCE[].INSTITUTION — 6 distinct values, the FIRST AUTHOR's affiliation on each of the 88 records: University of Verona 27, University of Turin 25, University of Milan 16, Institute of Sciences of Food Production 15, INRAE 3, Instituto de Agricultura Sostenible 2 (t
SUMMARY: Today the Science screen shows 13 "classified themes" (10 from D.SCI_THEMES + 3 from ITALY_SCIENCE.EXTRA_THEMES), a 36-row "RECENT RESEARCH" table, a 14-name "RESEARCHERS" panel, a 12-row "INSTITUTIONS · 12" panel, a GIRE resistance block, and a per-theme detail view (isTheme, markup 2157-2184). Only two of those blocks stand on real ingested data: the GIRE resistance block (portale.html 3244-3300 reads window.ITALY_INGEST.RESISTANCE directly, 34 cases, every field 34/34 populated) and 12 of the 36 research rows (which come from window.ITALY_REAL.SCIENCE, an older hand-curated fixture, not from IG). The other 24 research rows are procedurally generated in italy-demo-data.js:385-389 with seed() — fabricated year, institution, study type, descriptor, "why" prose and study location. The 12 institutions are a hardcoded name array (italy-demo-data.js:370) whose record counts are counted over those fabricated rows. The real backing is IG.THEMES (5), IG.SCIENCE (88), IG.RESEARCHERS (60) and IG.RESISTANCE (34); italy-app-model.js already exposes scienceRecords/researchers/resistance but three of its science field mappings are dead (measured 0/88 and 0/60) because it reads demo-era key names that the real schema does not have.

-- MODEL CHANGES --
  * scienceThemes (NEW) :: The screen's spine is a theme list. Today it is invented in the view from a fixture. IG.THEMES is real, small (5) and carries its own institution profile — the view must not have to know any of that.
      fields: key (IG.THEMES.ID, e.g. IT-THEME-001), theme (THEME token, e.g. VINE_FLAVESCENCE), title (a controlled display label per token — NOT a translation of the token, and NOT the demo t.title), query (QUERY verbatim — this is the honest definition of what 'works' counts), works (WORKS: 135/70/71/27/17), authorsIt (AUTHORS_IT: 334/235/223/62/22), authorsWithOrcid (AUTHORS_WITH_ORCID: 213/195/188/50/17), 
      derivation: 1:1 from IG.THEMES[5]. works/authors* copied verbatim from OpenAlex counts. recordCount computed by matching IG.SCIENCE CROP+ISSUE to the theme's tokens — MEASURED: VINE_FLAVESCENCE 43, DURUM_FUSARIUM 40, OLIVE_BACTROCERA 0, MAIZE_BORER_DIABROTICA 0, WEED_HERBICIDE_RESISTANCE 0; and 5 of the 88 records (IT-SCI-001/002 OLIVE/REPILO, IT-SCI-086/087 VINE/DOWNY_MILDEW, IT-SCI-088 CEREAL/SEPTORIA) match NO theme and must be reachable outside the theme grouping. There is NO trend field and none can be derived — AUTHORS_ACTIVE_SINCE_2024 is a headcount, not a movement.
  * scienceRecords (FIX + EXTEND) :: Three of its eleven current fields are measured dead (0/88) and seven real fields the source does supply are invisible to every screen, which is why the view fell back to the fixture.
      fields: publishedAt (PUBLISHED_AT, 88/88) replacing the dead date, year (first 4 chars of PUBLISHED_AT) replacing the dead year, author (AUTHOR, 88/88 — only 7 distinct values), orcid (ORCID, 88/88 — 7 distinct), doi (DOI, 86/88), venue (VENUE, 88/88 — 52 distinct), materialType (MATERIAL_TYPE: PEER_REVIEWED_PAPER 80, PREPRINT 6, OTHER_TECHNICAL_PUBLICATION 2), materialRole (MATERIAL_ROLE: DISEASE_PEST_MO
      derivation: All fields copied verbatim from IG.SCIENCE. themeKey is the only derived one and is null where no theme matches. Do not synthesize a region: COUNTRY_OF_FACT is 'IT' for all 88 and is a country, not a study location.
  * scienceInstitutions (NEW, derived) :: The screen has an INSTITUTIONS panel and a per-theme INSTITUTIONS chip row. Both must be derived, and they are derived from two DIFFERENT real sources with two different meanings that must not be blended.
      fields: name, recordCount (how many of the 88 records carry this as first-author affiliation), source: 'FIRST_AUTHOR_AFFILIATION', (separately) themeWorks map from IG.THEMES[].INSTITUTIONS_TOP
      derivation: Panel A (the INSTITUTIONS card, markup 2082-2087): group IG.SCIENCE by INSTITUTION. MEASURED, exactly 6: University of Verona 27, University of Turin 25, University of Milan 16, Institute of Sciences of Food Production 15, Institut National de Recherche pour l'Agriculture l'Alimentation et l'Environnement 3, Instituto de Agricultura Sostenible 2. Panel B (the per-theme chips, markup 2175): Object.keys(theme.institutionsTop), 11-12 per theme, 37 distinct across all five. There is NO institution 'type' field in either source — the demo's 'University / National research body / Research foundation' is a string-prefix guess and must not be reproduced.
  * researchers (FIX) :: org and url are measured 0/60 and role leaks a Portuguese research note to an Italian screen.
      fields: institutions (INSTITUTIONS array, 60/60; 36 distinct across the 60), orcid (60/60), openalexId (60/60), themeKey (THEME, 60/60 — exactly 12 researchers per theme, all five themes covered), worksInScope (WORKS_IN_SCOPE, 60/60, range 1-30), lastActivity (LAST_ACTIVITY, 60/60, range 2019-01-02 to 2026-07-30), identityState (IDENTITY_STATUS: ORCID_PRESENT_NOT_RESOLVED_HERE 54, NO_ORCID_IN_SOURCE 6), r
      derivation: Verbatim from IG.RESEARCHERS with a single NOT_OBSERVABLE guard on ROLE and FACT_REGION. daysSinceLastActivity, if a screen wants it, is computed by the existing AM.daysFrom against REF 2026-09-02 — no new clock.
  * searchIndex (EXTEND) :: Search at portale.html:3380 reads the fixture directly. The model already indexes resistance (line 304) but not science records or themes.
      fields: idx('science', r.id, r.title, [r.title, r.author, r.institution, r.venue, r.crop, r.issue], 'science'), idx('theme', t.key, t.title, [t.title, t.theme, t.cropToken, t.issueToken], 'theme')
      derivation: Straight from the two collections above; the view then reads AM.searchIndex only.

-- FIELDS THAT DIE (21) --
  [REMOVE_SUBCOMPONENT] theme.trend / trendColor / trendUpper @ portale.html 3305 (trendUpper), 3209 (sourceTrend), markup 2162 'Publication mov :: There is no trend, movement, growth or direction field anywhere in IG.THEMES. The five fields it does have (WORKS, AUTHORS_IT, AUTHORS_WITH_ORCID, AUTHORS_ACTIVE_SINCE_2024, INSTITUTIONS_TOP) are all point-in-time counts
  [REMOVE_SUBCOMPONENT] theme.cases / caseObjs / caseCount / opportunityLine @ portale.html 3220-3221, 3305, 3314; markup 1996, 2054, 2133, 2163, 2176-2177; an :: The theme-to-opportunity relationship exists in exactly one place: the hand-written `cases: ['IT-OPP-001', ...]` arrays in italy-demo-data.js:372-381. IG.SCIENCE has no case field, IG.THEMES has no case field, IG.OPPORTU
  [REMOVE_SUBCOMPONENT] record.related / record.openCase @ portale.html 3309, 3314; markup 2071, 2168 :: A per-record link to an opportunity case. In the fixture it is assigned by `t.caseObjs[i % length]` — literally a modulo. Nothing in IG.SCIENCE points at an opportunity.
  [REMOVE_SUBCOMPONENT] record.why @ portale.html markup 2068 :: Generated sentence: 'Connected because it informs <issue> timing or pressure for <crop> in Italy.' It asserts timing and pressure relevance — a scientific/agronomic conclusion invented by a template. Delete.
  [SHOW_UNKNOWN] record.location / locationShort ('study area') @ portale.html 3309, markup 2068 and 2168 :: IG.SCIENCE's only geography is COUNTRY_OF_FACT, which is 'IT' for all 88 records. There is no region, province or study area. The fixture generated one from the related case's region 45% of the time. Show 'Italia · area 
  [DERIVABLE_FROM_REAL] record.study (the STUDY vocabulary) @ portale.html 3309 via D.RECORDS; markup 2136 '{{ r.study }}' :: The demo's six-value list (Field trial / Population model / Review / Regional survey / Laboratory study / Decision-support evaluation) is invented, but the real corpus has a comparable pair: MATERIAL_TYPE (88/88) and MAT
  [REMOVE_SUBCOMPONENT] institution.type @ italy-demo-data.js 391; portale.html 3312; markup 2085 :: 'National research body' / 'Research foundation' / 'University' is decided in the fixture by `name.startsWith('CREA')`. No such classification exists in IG. Drop the second line of the row.
  [DERIVABLE_FROM_REAL] institutions list itself (12 names) @ italy-demo-data.js 370 and 391; portale.html 3312; markup 2083-2087 :: Derivable, but the honest result is much smaller and less Italian than the fixture: 6 institutions, not 12, and two of the six (INRAE, France; Instituto de Agricultura Sostenible, Spain) are not Italian at all. The panel
  [SHOW_UNKNOWN] the size of the research corpus itself @ markup 2063 '{{ kpi.records }} RECORDS' :: This is the most important honesty item on the screen and it is not a missing field, it is a misleading true one. The count 88 is real, but IG.SCIENCE has only 7 distinct AUTHOR values (Nicola Mori 27, Massimo Blandino 2
  [SHOW_UNKNOWN] per-theme records for 3 of the 5 real themes @ markup 2167-2168 'PUBLICATION ACTIVITY · REPRESENTATIVE RECORDS'; portale.html 3 :: MEASURED by joining CROP+ISSUE: VINE_FLAVESCENCE 43 records, DURUM_FUSARIUM 40, OLIVE_BACTROCERA 0, MAIZE_BORER_DIABROTICA 0, WEED_HERBICIDE_RESISTANCE 0. Three of the five theme detail pages will have an EMPTY publicati
  [REMOVE_SUBCOMPONENT] researcher.bizUse / bizWhy / bizNote @ portale.html 3311; markup 2079 :: 'POTENTIAL EXPERT TO WATCH' and 'Relevant to X · Y · technical watch and possible expert engagement' are a business judgement attached to a named real person, composed by the view from fixture crop/issue fields. Nothing 
  [SHOW_UNKNOWN] researcher.role @ italy-app-model.js 190 (role: S(r.ROLE)); portale.html 3311 filter p.role === 'R :: MEASURED 60/60 = 'NÃO SEI — o índice não declara papel'. That is the analyst's own Portuguese note meaning 'I don't know — the index does not declare a role'. It is currently carried through the app model unfiltered. It 
  [SHOW_UNKNOWN] researcher.region / factRegion @ IG.RESEARCHERS.FACT_REGION; demo equivalent at italy-demo-data.js 398 ('Affiliat :: MEASURED 60/60 = 'NÃO SEI — a afiliação é do AUTOR, não do estudo'. Same leak as role. There is no researcher region.
  [DERIVABLE_FROM_REAL] researcher.crops / researcher.issues @ italy-app-model.js 190; portale.html 3311 (p.crops[0], p.issues[0] feed bizWhy) :: MEASURED 0/60 in the model. The real link is THEME (60/60, exactly 12 per theme), from which a crop and issue token follow. Usable as a NAVIGATION label only — a researcher being in the VINE_FLAVESCENCE query is not a st
  [DERIVABLE_FROM_REAL] researcher.contentCount / lastDays / 'ago' relative dates @ italy-demo-data.js 398-403; markup 2079 '{{ p.last }}' :: The fixture's `20 + i * 3` days is invented. IG.RESEARCHERS.LAST_ACTIVITY is real and complete (60/60, 2019-01-02 to 2026-07-30). Compute any 'N giorni fa' with the existing AM.daysFrom against referenceDate 2026-09-02 —
  [REMOVE_SUBCOMPONENT] person -> publications join @ portale.html 3371 themeRecords; markup 2340 'PUBLICATIONS · TOPIC X' :: MEASURED: of the 7 distinct AUTHOR values in IG.SCIENCE, exactly ONE (Massimo Blandino) also appears in IG.RESEARCHERS.PERSON. So a truthful person->publications panel is empty for 59 of 60 researchers. The current scree
  [REMOVE_SUBCOMPONENT] theme.note @ italy-demo-data.js 372-381; markup 2160 :: Hand-written descriptive prose per theme ('Mycotoxin risk, flowering timing, fungicide efficacy'). IG.THEMES has QUERY instead, which is strictly better: it states exactly what was counted. Show the query, drop the prose
  [REMOVE_SUBCOMPONENT] theme.works for the 8 themes with no real counterpart @ markup 1994 and 2161 :: scaphoideus 66, xylella 52, maize-mycotoxins 41, resistance 24, ipm 19, cercospora-dss 18 — six publication volumes with no upstream theme. Plus durum-fusarium 78 (real: 70) and maize-borer 30 (real: 27), two numbers tha
  [KEEP_AS_LABELLED_DEMO] ITALY_SCIENCE.B entries for the 5 themes with no real counterpart @ italy-science-business.js 20-175 keyed by demo theme id; portale.html 3233 :: Of the 10 SB.B keys, 5 map to a real IG theme (grapevine-phytoplasma, durum-fusarium, olive-bactrocera, maize-borer, and weed-resistance via EXTRA_THEMES). The other 5 (scaphoideus, cercospora-dss, maize-mycotoxins, xyle
  [REMOVE_SUBCOMPONENT] sciWeed @ portale.html 3302, exported at 3447 :: Computed and exported but bound nowhere in the 51-2374 markup. Dead prop; delete while you are in there.
  [SHOW_UNKNOWN] i18n labels for the real crop/issue tokens @ portale.html 2480 cl(), 2487 il(); markup 1985, 2104, 2160 :: Not a data gap but it will look like one. T.CROPS and T.ISSUES are keyed on the demo's English labels ('Grapevine', 'Fusarium Head Blight'). The real tokens are VINE, DURUM_WHEAT, OLIVE, CEREAL and FLAVESCENCE, FUSARIUM,

-- PLAN --
  1. STEP 1 — italy-app-model.js, scienceRecords (lines 179-186). Replace the dead `date: S(r.DATE || r.date)` and `year: S(r.YEAR || r.year)` with `publishedAt: S(r.PUBLISHED_AT)` and `year: r.PUBLISHED_AT ? String(r.PUBLISHED_AT).slice(0,4) : null`. Delete `studyArea` and `affiliation` (both measured 0/88). Add author, orcid, doi, venue, materialType, materialRole, countryOfFact. Verify with the node recipe that publishedAt is 88/88 before moving on.
  2. STEP 2 — italy-app-model.js line 269. The archive index pushes `r.date || r.year` as the date for every science row; both are null today. Change to `r.publishedAt`. This silently fixes 88 archive rows in another domain — mention it to the archive owner rather than letting them rediscover it.
  3. STEP 3 — italy-app-model.js, researchers (lines 188-192). Add a `noteGuard = (v) => /N[ÃA]O SEI/i.test(String(v||'')) ? null : S(v)` helper and apply it to ROLE and FACT_REGION. Replace `org` with `institutions: r.INSTITUTIONS || []`. Replace `url` with `orcid: S(r.ORCID)` and `openalexId: S(r.OPENALEX_ID)`. Add themeKey, worksInScope, lastActivity, identityState. Delete the dead crops/issues.
  4. STEP 4 — italy-app-model.js, new `scienceThemes` collection from IG.THEMES (5). Include key, theme, title (from a controlled token->label map you author here, not in the view), query, works, authorsIt, authorsWithOrcid, authorsActiveSince2024, institutionsTop, cropToken, issueToken. Then backfill `themeKey` onto scienceRecords and compute recordCount / researcherCount per theme. Assert in a scratch node run that recordCount comes out 43/40/0/0/0 and researcherCount 12/12/12/12/12 — if it does not, the join is wrong.
  5. STEP 5 — italy-app-model.js, new derived `scienceInstitutions` (group IG.SCIENCE by INSTITUTION). Assert 6 entries with counts 27/25/16/15/3/2. Register both new collections in the `collections` object at line 282 so counts and provenanceSummary pick them up.
  6. STEP 6 — italy-app-model.js searchIndex (around line 298-304): add idx() entries for science records and science themes so the view can stop reading the fixture.
  7. STEP 7 — a token->label dictionary for it and en covering VINE, DURUM_WHEAT, OLIVE, CEREAL, MAIZE and FLAVESCENCE, FUSARIUM, REPILO, DOWNY_MILDEW, SEPTORIA, BACTROCERA, BORER, HERBICIDE_RESISTANCE. Without this the screen renders raw uppercase tokens.
  8. STEP 8 — portale.html 3200-3239, buildTheme() and sciAll. Drive sciAll from AM.collections.scienceThemes. Look SB.B up through an explicit legacyKey map and make every SB read null-safe. Move SB.EXTRA_THEMES out of sciAll into a separate `sciDeclaredNotIngested` prop. Delete works/trend fallbacks. Keep D.CAT but only under theme.ui.category.
  9. STEP 9 — portale.html 3197, winFor2(). Repoint from D.WINDOWS to APP.collections.windows and stop composing the state string: print w.status / w.statusReason verbatim, use w.daysToStart / w.daysToEnd only for the numeral.
  10. STEP 10 — portale.html 3216-3221. Replace the products/portfolioState/portfolioColor derivation (which reads the demo cases' product lists) with AM.strengthFor and the label-verdict strength classes; make the negative read NO_CONFIRMED_MATCH_CURRENT_READING. Delete opportunityLine, caseCount and caseObjs entirely.
  11. STEP 11 — portale.html 3309, `records`. Rebuild from AM.collections.scienceRecords sorted by publishedAt descending. Drop why, locationShort, related and openCase. Keep bizRel/bizColor only where a themeKey resolves to an SB.RECORD_REL entry, otherwise omit the cell.
  12. STEP 12 — portale.html 3311-3312. `researchers` from AM.collections.researchers (delete bizUse/bizWhy/bizNote); `institutions` from AM.collections.scienceInstitutions (delete i.type).
  13. STEP 13 — portale.html 3313-3314. Look th0 up by key with NO fallback to [0]; add a `hasTheme` prop and an empty state. Rebuild th.records, th.institutions (from institutionsTop), th.instCount and th.people from the model. Delete trendUpper, caseCount, caseObjs.
  14. STEP 14 — portale.html 2628 and 3340. Delete both fixture-driven theme<->case links. At 2628, either delete relatedThemes or rebuild it as a crop+issue navigation chip explicitly labelled as navigation.
  15. STEP 15 — portale.html 2893, 2898 and 3371. Repoint the two navigation fallbacks to the first real theme key. At 3371 make themeRecords an author-name match (it will be empty for 59 of 60 people) with an explicit empty state.
  16. STEP 16 — portale.html 3380. Replace the two fixture reads in search with AM.searchIndex.
  17. STEP 17 — portale.html 3446-3447 export line. Add scienceRecordCount, researcherCount and sciDeclaredNotIngested; delete sciWeed and sciHasWeed (bound nowhere).
  18. STEP 18 — markup 51-2374, apply every edit listed in markupImpact. Do not relayout: delete the tiles and columns named there and let the existing grid close up; do not add new cards to fill the space (Rule 3 and Rule 12).
  19. STEP 19 — verify nothing regressed in the GIRE block (portale.html 3244-3300, markup 2020-2045). It is already real and correct; the species-truncation guard and the four-digit year guard must be byte-identical after the change.
  20. STEP 20 — final check with the node recipe: sciTotal === 5, scienceRecordCount === 88, researcherCount === 60, institutions.length === 6, and grep portale.html for `D.SCI_THEMES`, `D.RECORDS` and `D.INSTITUTIONS` — the only remaining hits should be none.

-- RISKS --
  ! The screen gets visibly emptier and the numbers get visibly smaller: 13 themes -> 5, 12 institutions -> 6, 36 'records' -> 88 (this one grows), 14 researchers -> 60. Three of the five theme detail pages will have an empty publications list. Someone will read that as a regression. It is not; it is the first honest version of this screen.
  ! The 88-record corpus is 7 authors deep. If the count ships without that caption next to it, it will be quoted as 'Sintonia monitors 88 Italian studies' and there is no way to walk that back later. This is the highest-consequence item in the whole domain.
  ! Two of the six real institutions are not Italian (INRAE, France; Instituto de Agricultura Sostenible, Spain). Any header wording like 'Italian institutions' becomes false the moment the panel is repointed.
  ! IG.SCIENCE has no theme key. The record<->theme join is inferred from CROP+ISSUE and it leaves 5 of 88 records (IT-SCI-001, 002, 086, 087, 088) belonging to no theme. Those records must stay reachable in the flat table, or 5 real records silently vanish from the product.
  ! INSTITUTION on a record is the FIRST author's affiliation, and INSTITUTIONS_TOP on a theme is a works count from a different query. The two panels sit within 20 lines of each other in the markup (2082 and 2175) and mixing their denominators is the easiest mistake available here.
  ! IG.RESEARCHERS.ROLE and FACT_REGION are 60/60 Portuguese analyst notes ('NÃO SEI — ...'). They already flow through the app model unfiltered. Any view that binds AM researcher role today ships that note to the user; the guard must land in the model, not in one view.
  ! The theme<->opportunity link is deleted in three places (case detail 2628, archive detail 3340, theme detail 2176). If any of the three is missed, a fixture-only relationship keeps rendering while the other two disappear, which is worse than either state alone.
  ! T.CROPS / T.ISSUES do not have entries for a single real token. Repointing the view without the token->label map first will put VINE, DURUM_WHEAT and FLAVESCENCE on an Italian screen in raw uppercase.
  ! th0 falls back to sciDetailThemes[0]. Every existing link and bookmark to a demo theme id (8 of 10 ids disappear) will silently render a different theme's facts under the requested id unless STEP 13 lands with STEP 8.
  ! winFor2() (line 3197) reads D.WINDOWS and turns it into 'WINDOW OPEN · N days remaining'. It belongs to the windows domain but is invoked from buildTheme, so it can be missed by both owners. Coordinate this one line explicitly.
  ! ITALY_SCIENCE.B is authored interpretation, not a fixture, and the UI already labels it honestly. Deleting it wholesale would remove the one thing on the screen that connects science to business. Deleting only the 5 orphan keys is the right cut, and it is a judgement call worth confirming with the product owner.

-- OPEN QUESTIONS --
  ? ITALY_SCIENCE.B is Sintonia's own business interpretation, already labelled on screen as 'an interpretation layer over the science, not a scientific conclusion'. Does Rule 2 (a demo fixture may never supply a fact) apply to it? My reading: the rel/short/why/shows/notShows prose is interpretation, allowed, but ONLY over
  ? The three EXTRA_THEMES (herbicide resistance, weed management, biocontrol) declare works:null and 'not ingested' in their own text. Should they stay visible under a separate labelled heading (my recommendation, since they document a known gap), or be removed until data exists?
  ? IG.THEMES.WORKS (135/70/71/27/17) counts works matching an OpenAlex QUERY. IG.SCIENCE holds 88 harvested records. These are two different universes and the theme detail page will show both at once (works 71, records 0 for OLIVE_BACTROCERA). What exact wording distinguishes them on screen?
  ? Is there an upstream plan to add a THEME key to IG.SCIENCE? If yes, the CROP+ISSUE join in STEP 4 is temporary scaffolding and should be marked as such in a comment.
  ? The 'BUSINESS RELEVANCE' column of the records table (SB.RECORD_REL, keyed by demo themeId) can only resolve for the two themes that have records. Keep the column with blanks, or drop the column?
  ? Does any other domain already bind AM.collections.researchers[].role or .org? If so the Portuguese note may already be on screen somewhere I did not look, and the model-side guard becomes urgent rather than tidy.
  ? window.ITALY_REAL.SCIENCE (12) and .RESEARCHERS (14) are an earlier hand-curated real fixture with fields IG does not have (kind, what, region, focus, note). Is it superseded by IG entirely, or is it a second real source that should be merged? I assumed superseded; it is the one assumption in this spec I could not sett

====================================================================================================
DOMAIN: SOURCES / EVENTS / NEWS / CHANNELS
CONFIDENCE: high  |  realSource: window.ITALY_APP_MODEL.collections.sources (31, from IG.SOURCES), .events (18, from IG.EVENTS), .news (8, from IG.NEWS), .channels (30, from IG.CHANNELS). Cross-links: IG.OPPORTUNITIES[3].SOURCE_IDS and IG.FUTURE_SIGNALS[3].SOURCE_IDS are the ONLY records that name their sources (ITALY_CANONICAL.windows carry SOURCE_IDS on 0/29). Per-source backing volume is derivable from SOURCE_ID on SCIENCE/MARKET/VOICES/RESISTANCE/THEMES/CROP_WINDOWS/RESEARCHERS.
SUMMARY: Today the Sources screen (portale.html 2235-2314) shows a 7-tile KPI strip, 8 group chips, a 53-row source registry table (name, type, "what Sintonia obtains", frequency, coverage, latest, "N cases", health), a news feed with TODAY/7/30-day chips, and a source-detail page with a CROPS/TOPICS chip list, a related-opportunity list and a "LATEST CAPTURES IN ARCHIVE" list. Event Watch (1748-1782, 1848-1890) shows an upcoming-events rail, event cards with countdown/bucket/exhibitor status/crop chips/competitor participation summary, and an event detail page with a full 6-competitor participation table, an EVENT PROFILE programme, related cases and a BEFORE/DURING/AFTER activity story. Every one of those facts today comes from window.ITALY_DEMO: D.SOURCES (53 hand-written entries with cov/topics/what/freq/health/lastDays/related), D.EVENTS (6 entries with city/region/dates/startDays/crops/exhibitorStatus/program/participation for BASF, Bayer, Corteva, FMC, Syngenta, UPL) and D.NEWS (= window.ITALY_REAL.NEWS, 9 hand-curated Sep-2026 items with days/use/editorial/originating). The real backing is window.ITALY_INGEST: SOURCES[31], EVENTS[18], NEWS[8], CHANNELS[30] — a different, smaller and differently-shaped reality that the app model already normalizes, but with four field-name bugs that make the real data arrive as null.

-- MODEL CHANGES --
  * collections.sources :: Four of the seven current fields are wrong or empty, and the view has to invent the rest. group is null on 31/31 because IG.SOURCES has no GROUP field. The demo id join must go. The view should receive one flat, honest r
      fields: id — s.ID (stable upstream key; replaces the demo SRC-nn id for the 6 name-colliding records), sourceId — s.SOURCE_ID || s.ID (SOURCE_ID present on only 11/31; ID is the primary key), name — S(s.NAME) [31/31], type — S(s.TYPE) [31/31; 12 distinct values: OFFICIAL 5, FIELD 6, TECHNICAL_MEDIA 4, COMPETITOR 4, COOPERATIVE 3, RESEARCH 2, RESEARCH_INSTITUTION 2, MARKET 1, COMPANY 1, PEOPLE 1, PRODUCER_
      derivation: 
  * collections.events :: name is null on 18/18 (reads e.NAME, real field is e.EVENT) and url is null on 18/18 (reads e.URL, real field is e.OFFICIAL_URL). Both bugs are live today and already poison the archive index. Everything the Event Watch 
      fields: id — e.ID (drop the deByName demo join), name — S(e.EVENT) [18/18] — FIXES the null, date — S(e.DATE) [18/18], format 'YYYY-MM-DD' or 'YYYY-MM-DD a YYYY-MM-DD', startDate / endDate — split on ' a '; endDate = startDate for the 13 single-day records, daysToStart — daysFrom(startDate) against AM.REF 2026-09-02. MEASURED: only 2 of 18 are positive (IT-EVT-001 +69, IT-EVT-005 +221); 16 are past, locat
      derivation: 
  * collections.news :: outlet is null on 8/8 (reads n.OUTLET/n.SOURCE, real field is n.PUBLISHER) and url is null on 8/8 (reads n.URL, real field is n.SOURCE_URL). The feed also needs the content-kind badge, because 3 of 8 real items are ADAMA
      fields: id — n.ID, title — S(n.TITLE) [8/8], publisher — S(n.PUBLISHER) [8/8] — FIXES the null outlet, publisherSourceId — exact case-insensitive NAME match against the source registry. MEASURED 6/8 resolve (AgroNotizie→IT-SRC-PROBE-003, ADAMA Italia→IT-SRC-PROBE-020 x2, Bayer→IT-SRC-PROBE-019, L'Informatore Agrario→IT-SRC-PROBE-002, Terra e Vita→IT-SRC-PROBE-001); 'Agronotizie' (casing variant) and 'Cons
      derivation: 
  * collections.channels :: url is null on 30/30 (reads c.URL, real field is c.CHANNEL_URL) and platform is null on 30/30 (no PLATFORM field exists). No screen currently binds channels, so nothing visibly breaks — but the archive/search layer will 
      fields: id — c.ID, name — S(c.CHANNEL) [30/30], url — S(c.CHANNEL_URL) [30/30] — FIXES the null, platform — derived from the URL host; MEASURED 30/30 are youtube.com, so 'YouTube' is a real derivation, not an assumption, identityState — S(c.IDENTITY_STATE) [30/30, ALL 'NOT_PROVED' — no channel is a proven identity and no screen may present one as a named person], contentTypeExample — S(c.CONTENT_TYPE_EXAM
      derivation: 
  * collections.archive :: Lines 273-274 of italy-app-model.js push EVENT and NEWS rows using the broken field names, so 18 archive rows have title null and 8 have source null today.
      fields: EVENT push: title must become e.name (works once the EVENT-field fix lands); source stays e.location; url becomes e.url, NEWS push: source must become n.publisher; url becomes n.url, Add sourceId to every push so the source-detail 'LATEST CAPTURES' list can be rebuilt from real data instead of D.ARCHIVE
      derivation: 

-- FIELDS THAT DIE (28) --
  [REMOVE_SUBCOMPONENT] source.topics[] @ portale.html 2627, 2667, 3384; markup 2304 ('CROPS / TOPICS' card) :: There is no crop/topic field on IG.SOURCES in any form. This is a pure fixture invention and it is load-bearing in three places: the case evidence panel, the crop-window coverage KPI and global search. The whole CROPS/TO
  [REMOVE_SUBCOMPONENT] source.cov (crop-and-region coverage used as a matching key) @ portale.html 2627, 2667, 3367 (sr.cases), 3384 :: Two different things are hiding in one field. As a LABEL it maps to GEOGRAPHY (31/31) — see the DERIVABLE entry below. As a MATCHING KEY ('does this source cover Veneto and Grapevine?') it dies completely: GEOGRAPHY is '
  [DERIVABLE_FROM_REAL] source.cov (as a displayed coverage label) @ markup 2283, 2301 :: IG.SOURCES.GEOGRAPHY, 31/31 filled. But the column header must change from COVERAGE to GEOGRAFIA: 'IT' means the source is Italian, not that it covers all Italian crops.
  [DERIVABLE_FROM_REAL] source.what @ markup 2282, 2298 :: IG.SOURCES.ROLE, 27/31 non-empty and one of those is the literal 'NÃO SEI' → 26/31 informative, 5/31 must render UNKNOWN. ROLE is terser than the demo prose ('registro nacional de produto e rotulo autorizado' vs a full s
  [SHOW_UNKNOWN] source.freq @ markup 2284, 2300 :: MEASURED: only 11 of 31 sources carry a human cadence (continua, semanal, anual, irregular...). 13 say 'DATED' and 3 'NO_DATE_FOUND' — those are crawler status tokens, not frequencies — and 4 are null. So 20 of 31 rows m
  [DERIVABLE_FROM_REAL] source.health / healthColor / healthShort @ portale.html 3352; markup 2287, 2302 :: IG.SOURCES.ACCESS_STATUS, 31/31: GREEN 26, BLOCKED 3, PARTIAL 1, NOT_REACHED 1. Honest and better than the demo's 'Available / Intermittent / Alternative route needed'. Note 3 of the 4 non-green are competitor and ADAMA 
  [SHOW_UNKNOWN] source.last / source.lastDays @ markup 2286, 2302 :: The demo values are seeded random numbers (1 + floor(seed(k*19)*12) days ago) — pure invention. LATEST_OBSERVATION is present 31/31 but 9 records store a raw Python list literal, several truncated mid-token ("['Sat, 29 A
  [DERIVABLE_FROM_REAL] source.related ('N cases') @ markup 2288, 2305 :: Replaceable by a real backedRecords count over SOURCE_ID, but the engineer must accept the shape of the answer: OPENALEX 158, AGRIFOOD 77, GIRE 34, YOUTUBE 17, MINISTERO 6, PIEMONTE 4, MODENA 2, CELLAR 1 — and 23 of 31 s
  [REMOVE_SUBCOMPONENT] source.cases[] (related opportunities on the source page) @ portale.html 3369; markup 2306 :: Built from cov+topics against demo CASES. The only real reverse-link is IG.OPPORTUNITIES[3].SOURCE_IDS, which names 3 sources total (MINISTERO, PIEMONTE, OPENALEX). 28 of 31 source pages would show an empty card.
  [REMOVE_SUBCOMPONENT] source.items / itemCount ('LATEST CAPTURES · N IN ARCHIVE') @ portale.html 3368; markup 2309-2311 :: Reads D.ARCHIVE (420 generated rows) filtered by demo sourceId. The real archive index in italy-app-model.js does not carry a sourceId at all. Showing generated captures under a real institution's masthead is exactly the
  [DERIVABLE_FROM_REAL] source.group (the 8 UI groups) and the 'EVENTS & TRADE FAIRS' chip @ portale.html 3152-3155, 3352; markup 2242 :: GROUP is null on 31/31. A ui-layer bucket can be derived from TYPE (12 values), but it yields 6 groups, not 8: 'PEOPLE' survives as one source (IT-SRC-YOUTUBE) and 'EVENTS & TRADE FAIRS' has NO real source behind it and 
  [SHOW_UNKNOWN] event.city / event.region @ portale.html 2599; markup 1752, 1776, 1852 :: IG.EVENTS has only LOCATION, one free-text string mixing venue and city ('BolognaFiere, Bologna', but also just 'Piemonte' or 'Veneto' for the 13 field-day records). Do not split it with a regex to resurrect the two-fiel
  [SHOW_UNKNOWN] event.crops[] @ portale.html 3182; markup 1779, 1860 :: Real CROP_RELEVANCE is a single token, not a list, and 5 of 18 are 'NAO SEI'. The chip row becomes one chip or 'NON DICHIARATO'. It also means the evd0.crops.some(...) matching at line 3182 has nothing to match on.
  [REMOVE_SUBCOMPONENT] event.participation / partRows / confirmed / historical / statusLabel / partSummary / confirmedLabel / confirmedCount @ portale.html 3157 (co.eventRows), 3173 (evDeco), 3183; markup 1752, 1781, 1786,  :: THE BIGGEST LOSS IN THIS DOMAIN. The demo asserts a participation state for six competitors at six events — 36 facts, all invented, including the 'HISTORICAL PARTICIPANT' and 'MONITORING' states. MEASURED reality across 
  [REMOVE_SUBCOMPONENT] event.program @ markup 1860 ('EVENT PROFILE') :: No field. NOTE exists on 7/18 but it is edition/biennial trivia ('117a edicao, tema Full Innovation'), not a programme. Bind NOTE with an honest label or delete the card; do not relabel NOTE as 'programme'.
  [SHOW_UNKNOWN] event.exhibitorStatus @ portale.html 3173; markup 1778, 1855 :: EXHIBITOR_LIST_STATE is filled 18/18 but only ONE record is informative ('161 marcas, segundo a UIV'). 13 say 'NAO APLICAVEL' and 4 'NAO CONSULTADA'. The evDeco branch that reads statusLabel from exhibitorStatus.startsWi
  [DERIVABLE_FROM_REAL] event.site @ markup 1852 :: OFFICIAL_URL, 18/18. Currently arrives null in the model because it reads e.URL — a one-word fix.
  [DERIVABLE_FROM_REAL] event.startDays (as stored) @ portale.html 3174, 3175, 3182, 3183; markup 1752, 1774 :: The stored demo value violates §6 (baked against D.TODAY). daysFrom(startDate) against AM.REF is legitimate presentation math (§7). But the consequence must be stated: only 2 of 18 real events are in the future, so the U
  [REMOVE_SUBCOMPONENT] event.cases[] and event.story (BEFORE / DURING / AFTER) @ portale.html 3182-3184; markup 1861, 1866 :: Both depend on a.eventId linking a competitor activity to an event. No record in COMP_ACTIVITIES[503] carries an event id, and evd0.crops/evd0.region do not exist. The event-detail right column loses its whole narrative 
  [REMOVE_SUBCOMPONENT] activity.eventDates / activity.eventCity @ portale.html 2599; markup 1725, 1828, 1884, 1929 :: Four identical blocks. Real competitor activities have type PAID/ORGANIC/etc with no EVENT type and no event link, so the sc-if never fires with real data — but leaving it means the first upstream record that ever sets a
  [REMOVE_SUBCOMPONENT] news.days and the TODAY / LAST 7 DAYS / LAST 30 DAYS chips @ portale.html 3349-3351; markup 2260 :: MEASURED: the nearest real news item is 91 days old; the set spans -91 to -2016 days and 2 of 8 have no parseable date. All three chips read 0 and the default 30-day filter empties the feed. A recency control over 8 reco
  [REMOVE_SUBCOMPONENT] news.region @ markup 2265 (region chip) :: MEASURED 0 of 8: every real news record has REGION = 'NAO SEI'. Region is an explicitly forbidden demo field. Delete the chip rather than print NON SEI eight times.
  [REMOVE_SUBCOMPONENT] news.originating @ markup 2266 ('ORIGINATING SOURCE') :: No equivalent. CONTENT_KIND_MEANING answers a different question (who wrote it) not this one (who the story came from). Do not repurpose it under the old label.
  [DERIVABLE_FROM_REAL] news.editorial @ markup 2266 ('EDITORIAL TYPE') :: CONTENT_KIND + CONTENT_KIND_MEANING, 8/8. And this one gets BETTER with real data: 3 of 8 items are ADAMA Italia publishing about its own products and 1 is Bayer (COMPANY_PROVIDED), 1 is BRANDED_CONTENT, only 4 are EDITO
  [REMOVE_SUBCOMPONENT] news.use[] and the n.cta / n.go routing @ portale.html 3351; markup 2271 :: 'RELATED OPPORTUNITY →' / 'CROP WINDOWS →' / 'FUTURE RADAR →' are routing claims that assert an article supports a case. No real field carries it. Replace the CTA with the real SOURCE_URL.
  [REMOVE_SUBCOMPONENT] the hardcoded '2026' and 'real' suffix on every news card @ markup 2263 ('{{ n.date }} 2026 · real') :: Two of the eight real items are from 2021 and 2022 and one has no date at all. The template would print '2021-02-24 2026'. The bare word 'real' must also go — provenance belongs in the data-state panel, not as decoration
  [DERIVABLE_FROM_REAL] K.orgs / K.official / K.research / K.field / K.media / K.market / K.people (D.KPI, italy-demo-data.js 776) @ portale.html 2549 (const K = D.KPI), 2608 (nav badge K.orgs + K.people), 3362-33 :: Strictly these are D.KPI reads, not D.SOURCES reads, but they are counts OF the demo registry and they are published as headline numbers on my screen and in the nav badge. 'Organizations monitored' currently says 53; the
  [REMOVE_SUBCOMPONENT] the demo-name join that assigns record ids (italy-app-model.js 203, 214) @ italy-app-model.js 203-206 and 214-217 :: Not a display field but the most dangerous item here. Six real sources currently carry a DEMO id because their name string-matched the fixture (IT-SRC-OPENALEX renders as SRC-15, IT-SRC-EUROSTAT as SRC-12, and four more)

-- PLAN --
  1. STEP 1 (italy-app-model.js, lines 201-226) — fix the four live null bugs and delete the demo joins in one edit. Delete dsByName (203) and deByName (214) entirely. sources: id = s.ID, sourceId = s.SOURCE_ID || s.ID, drop ui and drop group, add role/country/frequency/frequencyKnown/accessStatus/limitations/latestObservation. events: name = S(e.EVENT) (was S(e.NAME) → null 18/18), url = S(e.OFFICIAL_URL) (was S(e.URL) → null 18/18), add startDate/endDate split on ' a ', daysToStart, sector, cropRelevance, organizer, exhibitorListState, timeState, confirmedParticipation, participationLaw, note. ne
  2. STEP 2 (italy-app-model.js) — add sources[].backedRecords by counting SOURCE_ID across SCIENCE, MARKET, VOICES, RESISTANCE, THEMES, CROP_WINDOWS and RESEARCHERS. Expected result, verify it: OPENALEX 158, AGRIFOOD 77, GIRE 34, YOUTUBE 17, MINISTERO 6, PIEMONTE 4, MODENA 2, CELLAR 1, everything else 0 (23 of 31). Also add sources[].uiGroup as a pure presentation bucket over TYPE, and note that IT-SRC-REGIONAL is referenced by 4 CROP_WINDOWS but is MISSING from the registry — surface it as a data gap, do not invent the row.
  3. STEP 3 (italy-app-model.js, lines 273-274) — fix the archive pushes now that the fields exist: EVENT rows get e.name (18 rows currently have title null) and e.url; NEWS rows get n.publisher (8 rows currently have source null) and n.url. Add sourceId to every push so step 8 has something to join on.
  4. STEP 4 (portale.html 3352, 3367, 3384) — switch the source registry, source detail and search to AM.collections.sources. Replace the || D.SOURCES[0] fallback with an explicit not-found state. openSource() now receives IT-SRC-* ids; check every caller (2627, 2893 srcGo, 3339 archive link, 3384) and confirm none still passes an SRC-nn id.
  5. STEP 5 (portale.html markup 2277-2288 and 2296-2311) — rebind the registry table and source-detail page. DELETE the CROPS / TOPICS card (2304), the RELATED OPPORTUNITIES card (2305-2307) and the LATEST CAPTURES card (2309-2311). Rename the columns: WHAT SINTONIA OBTAINS→RUOLO, COVERAGE→GEOGRAFIA, RELATED→RECORD COLLEGATI, HEALTH→ACCESSO. Add LIMITAZIONI. Make FREQUENCY able to render NON DICHIARATA for 20 of 31 rows.
  6. STEP 6 (portale.html 3362-3365, 3152-3155, 2608) — recompute the 7 KPI tiles and the group chips from real TYPE counts, drop the 'EVENTS & TRADE FAIRS' chip (no real source of that kind), remove the '+ D.NEWS.length + ' real items'' string, and recompute the sources nav badge at 2608 so it agrees with the table.
  7. STEP 7 (portale.html 3349-3351 and markup 2259-2272) — delete newsPeriods / newsPeriodChips / newsLimit and markup line 2260. Point newsItems at AM.collections.news with no filter. In the card: publisher, real date with no hardcoded 2026 and no 'real' badge, delete the region chip, delete originating, show contentKind + contentKindMeaning as a visible badge (4 of 8 items are COMPANY_PROVIDED or BRANDED_CONTENT), replace the CTA with the SOURCE_URL.
  8. STEP 8 (portale.html 2627 and markup 481) — rebuild cs.sourceRows from the case's upstream SOURCE_IDS. Verify the expected result: 3 sources on each of IT-OPP-* upstream-real cases, empty on the other 26, because 0 of 29 canonical windows carry SOURCE_IDS. Add the empty state.
  9. STEP 9 (portale.html 2667) — rewrite hasSource against real GEOGRAPHY, then LOOK AT THE NUMBER before shipping: only 2 of 31 sources name a region, so the crop-window GAPS KPI at 2913 will jump. If that number cannot be defended, delete the GAPS tile rather than keep the demo computation.
  10. STEP 10 (portale.html 3173-3175, 3181-3184 and markup 1748-1782, 1848-1890) — the events rewrite, and the biggest deletion. Point upcomingEvents/eventCards/evd0 at AM.collections.events. Delete from evDeco: confirmedLabel, statusLabel, statusColor, partSummary. Delete from the markup: the partRows table (1859), the EVENT PROFILE programme (1860), related cases (1861), the BEFORE/DURING/AFTER story (1866) and related activities (1869-1871). Collapse city+region to location. Render participationLaw verbatim under whatever participation survives.
  11. STEP 11 (portale.html 3157) — replace co.eventRows with a filter over confirmedParticipation. Confirm the outcome: BASF, Bayer, Corteva, FMC and UPL show an empty card; only Syngenta has one real row (Enovitis in Campo 2026). Do NOT emit 'NOT YET KNOWN' rows to fill the card.
  12. STEP 12 (portale.html 2599 and markup 1725, 1828, 1884, 1929) — delete eventDates, eventCity and partColor from actDeco and delete the four identical a.isEvent blocks.
  13. STEP 13 — regression sweep: grep portale.html for D.SOURCES, D.EVENTS and D.NEWS and confirm zero remain (D.GROUP_COLOR at 2904 stays, VISUAL_ONLY). Then re-run the node harness and assert AM.collections.events.records.every(e => e.name), .news.every(n => n.publisher && n.url), .channels.every(c => c.url), and that no source id starts with 'SRC-'.

-- RISKS --
  ! The Sources screen gets visibly emptier and there is no way around it. The registry drops 53→31 rows, the source-detail page loses three of its five cards, the news feed loses its period filter and four card fields, the UPCOMING EVENTS rail drops 5→2, and the competitor participation table disappears. PRODUCT LAW §3 says a smaller truthful screen beats a dense fake one, but someone will read this 
  ! The 'RECORD COLLEGATI' column will be 0 for 23 of 31 sources. That is the honest state (two thirds of the registry is a listening list that has not yet fed a record), but a column of zeros invites someone to 'fix' it by re-deriving from names or topics. It must not be.
  ! IT-SRC-REGIONAL is referenced as the SOURCE_ID of 4 CROP_WINDOWS records but does not exist in the 31-row registry. Any reverse lookup will silently drop those 4. Surface it as a gap; do not create a placeholder row.
  ! News publisher→source linking is name-string matching and it is fragile: 6 of 8 resolve, 2 fail ('Agronotizie' is a casing variant of IT-SRC-PROBE-003, and 'Consorzio Fitosanitario di Modena' is IT-SRC-MODENA under a shorter name). Fuzzy-matching those two would be inventing a link. Leave them unlinked and ask upstream for a PUBLISHER_SOURCE_ID.
  ! LATEST_OBSERVATION is filled 31/31 but 9 values are raw Python list literals, several truncated mid-string. If the template binds the raw field the registry will show "['Sat, 29 Aug 2026 07:30:57 +0000', 'Fri, 28 Aug 2026 16:57:" in a table cell. Parse defensively and fall back to NON DISPONIBILE.
  ! FREQUENCY mixes two vocabularies — human cadences (continua, semanal, anual) on 11 records and crawler tokens (DATED, NO_DATE_FOUND) on 16. Anyone reading it as one enum will publish 'DATED' as a frequency on 13 sources.
  ! 3 of the 8 real news items are ADAMA Italia publishing about its own products and 1 is Bayer. If the COMPANY_PROVIDED badge is not rendered, the screen presents ADAMA's own marketing as independent Italian trade news — a worse failure than showing nothing.
  ! The 14 real events with 'CONFIRMADO' ADAMA participation are ADAMA's own field days, not competitor intelligence. If Event Watch keeps its competitor framing while showing ADAMA-only participation, the reader will misread ADAMA's own calendar as competitor movement. Consider labelling the two groups explicitly.
  ! Steps 4 and 5 must land together. Switching visibleSources to real ids (step 4) while the source-detail page still reads D.SOURCES (step 5) leaves every row clicking through to a dead page, and the || D.SOURCES[0] fallback would hide that by rendering Regione Veneto for every click.
  ! D.ARCHIVE rows carry demo sourceIds (SRC-nn) and the archive detail at line 3339 links { kind: 'SOURCE', go: openSource(dr0.sourceId) }. That belongs to the archive domain, but it breaks the moment my step 4 lands. Coordinate with whoever owns the archive migration.
  ! collections.channels (30 records) is not bound by any markup in portale.html — the field fixes in step 1 are invisible today. Do not treat 'no visible change' as 'not applied'.

-- OPEN QUESTIONS --
  ? The real EVENTS registry is 14 ADAMA field days plus 4 trade fairs, and CONFIRMED_PARTICIPATION is 14x ADAMA and 1x SYNGENTA. Is 'Event Watch' still a competitor-intelligence screen, or has it become ADAMA's own event calendar with one competitor observation? The framing decision is a product call, not an engineering o
  ? collections.channels (30 real YouTube channels, all IDENTITY_STATE = NOT_PROVED, each with an example video and view count) is fully normalized and rendered nowhere. Should it surface — and if so, under Sources or under Voices? PRODUCT LAW §12 says do not redesign, so this needs an explicit decision before anyone build
  ? 0 of 29 ITALY_CANONICAL windows carry SOURCE_IDS, and only 3 opportunities and 3 future signals do. Is upstream going to populate window SOURCE_IDS in the next package? If yes, the 'SOURCES BEHIND THIS CASE' panel should be built now with an empty state; if no, it should be deleted.
  ? IG.SOURCES has no GROUP field, so the 8 UI group chips must be re-derived from 12 TYPE values into 6 buckets. Should upstream own that mapping (a GROUP column in the next package) rather than the view inventing a presentation taxonomy?
  ? IG.NEWS[8] and window.ITALY_REAL.NEWS[9] are two different, non-overlapping real news sets — the first ADAMA/AgroNotizie-centric from the ingest, the second hand-curated Sep-2026 harvest-timing items sitting outside the contract. Should ITALY_REAL.NEWS be ingested into IG.NEWS, or retired? Right now the portal shows on
  ? ROLE carries two incompatible vocabularies: descriptive Portuguese prose on the 11 curated sources ('registro nacional de produto e rotulo autorizado') and enum-like tokens on the 20 PROBE sources ('MARKET_AND_POLICY_SIGNAL', 'FIELD_OR_TECHNICAL_SIGNAL', one 'NÃO SEI'). Which is the intended shape, and should the RUOLO
  ? Language: the real registry is written in Portuguese ('registro nacional...', 'sondada em 2026-08-30'), the event states in Portuguese ('CONFIRMADO', 'PASSADO', 'NAO SEI'), while PRODUCT LAW §11 makes Italian the default UI language. Does the view translate these upstream state tokens, or does upstream re-emit them in 

====================================================================================================
DOMAIN: NAV COUNTERS + DATA STATE / PROVENANCE PANEL + NOTIFICATIONS
CONFIDENCE: high  |  realSource: APP = window.ITALY_APP_MODEL. Nav and KPI counts come from APP.counts / APP.collections (windows 29 · competitorActivities 503 · scienceRecords 88 · archive 740 · sources 31 · researchers 60 · voices 17 · products 166 · marketObservations 77 · resistance 34 · channels 30 · events 18 · news 8 · futureSignals 3 · upstreamOpportunities 3). Window status buckets come from APP.collections.windows.records[].status (upstream CURRENT_STATUS). Verified portfolio matches come from window.ITALY_LABEL_VERDICTS (VERIFIED 12 / NOT_FOUND 7) — which italy-app-model.js does NOT read yet. Regulatory use rows co
SUMMARY: Today the chrome of the portal — left nav counts, the sidebar "today" box, the 8 radar KPI cards, the 20-tile region map, the 7 source tiles, the transparency ("DEMO ENVIRONMENT") panel and the notification bell — is almost entirely fed by one object, `D.KPI` (aliased `K` at portale.html:2549), plus `D.WINDOW_KPI`, `D.FIELD_KPI`, `D.REGION_STATS`, `D.WHAT_CHANGED` and `D.NOTIFICATIONS`. Those are computed inside italy-demo-data.js from the demo fixtures (29 demo CASES, 72 demo ACTIVITIES, 56 demo SIGNALS, 53 demo SOURCES, 448-row demo ARCHIVE, 36 demo RECORDS, 39 demo PEOPLE), so every badge is a fixture number even when a real one exists. Measured, the gap is large and always in the same direction: competitors nav says 72, real is 503; science says 36, real is 88; archive says 448, real is 740; sources says 92, real is 31; "competitor movements · 30d" says 43, real is 11; the transparency panel claims 244 real / 491 demo when the real application model holds 1038 real / 100 demo records. Four numbers are honest by luck (29 windows, 6 window-open, 2 next-cycle, 5 date-unknown) because the demo CASES are a 1:1 shadow of the 29 canonical windows. The transparency panel itself is the worst offender in principle: it is the screen that promises to tell the truth about the data, and it reads the fixture (`D.ACTIVITIES`, `D.ARCHIVE`, `D.FIELD_MESSAGES`) to do it. `D.NOTIFICATIONS` is 

-- MODEL CHANGES --
  * provenanceSummary :: The transparency panel must be able to render itself from the model with no fixture reads and no per-row hand-maintenance. Today the summary has total/real/demo but no 'derived' split, so a REAL_DERIVED collection (archi
      fields: layer (existing), provenance (existing), total (existing), real (existing) — redefine as records whose provOf is CANONICAL or REAL_SOURCE, derived — NEW: records whose provOf === 'REAL_DERIVED'. Derivation: collections[k].records.filter(r => provOf(r, collections[k].provenance) === P.REAL_DERIVED).length. Measured result today: archive 740, every other layer 0., demo (existing) — records whose pro
      derivation: All fields derive from the existing collections map at italy-app-model.js:282-289 using the existing provOf/isDemo classifiers (lines 45-52). No new external data. Add a top-level provenanceTotals = { real, derived, demo, indexRows } computed over provenanceSummary.filter(r => !r.isIndex), plus indexRows summed over the isIndex rows. Measured today: real 1038 · derived 29 · demo 100 · indexRows 740.
  * sources :: The seven source-group tiles (portale.html:3363-3365) and the group chips (3346) filter on a 'group' name, but IG.SOURCES has no GROUP field — it has TYPE. The model already maps s.GROUP || s.group, which is null for all
      fields: group — derive from IG.SOURCES.TYPE with an explicit, auditable map: OFFICIAL→OFFICIAL (5); RESEARCH, RESEARCH_INSTITUTION→RESEARCH (4); FIELD, COOPERATIVE, PRODUCER_ORG→FIELD (10); TECHNICAL_MEDIA→TECHNICAL_MEDIA (4); MARKET, COMPANY, COMPETITOR→MARKET (6); PEOPLE→PEOPLE (1); ADAMA→OWN (1). Total reconciles to 31., accessStatus — S(s.ACCESS_STATUS). Measured: GREEN 26, PARTIAL 1, BLOCKED 3, NOT_R
      derivation: Pure rename/mapping over the existing IG.SOURCES read at italy-app-model.js:204-209. The TYPE→group map must be written literally in the model file so an auditor can check it against the 31 records; no fuzzy matching.
  * people :: There is no people collection at all, so the 'People monitored' tile (3363) has nowhere to go but the fixture's 39. IG.PEOPLE[15] is a real, evidence-bearing list (each record carries IDENTITY_EVIDENCE and ROLE_EVIDENCE)
      fields: id (ID), person (PERSON) — never translated, category (CATEGORY). Measured: RESEARCHER 11, INSTITUTIONAL_EXPERT 2, COMPANY_PERSON 1, CREATOR 1., organization (ORGANIZATION), role (ROLE), identityEvidence (IDENTITY_EVIDENCE), roleEvidence (ROLE_EVIDENCE), country (COUNTRY), provenance: provOf(p, REAL_SOURCE)
      derivation: coll(IG.PEOPLE.map(...), P.REAL_SOURCE, 'people with documented public identity and role'). Count 15. Do NOT merge with researchers: 8 of the 60 researcher names also appear here, so a naive sum would double count; the union of names is 66. If a combined figure is ever needed, expose it as a separate distinctPeople = 66 with the dedupe rule stated.
  * labelVerdicts :: italy-app-model.js does not read window.ITALY_LABEL_VERDICTS at all, so the 'verified portfolio matches' KPI (portale.html:2558) has no honest source and currently reads 13 off the demo cases instead of the audit's 12. T
      fields: verified — array of { crop, issue, product } from LV.VERIFIED. Count 12., notFound — array of { crop, issue, product } from LV.NOT_FOUND. Count 7., verifiedCount 12 · notFoundCount 7 · assessedCount 19, strength — LV.STRENGTH map (VERIFIED_LABEL_MATCH / RELATED_PORTFOLIO / LABEL_CHECK_NEEDED / NO_CONFIRMED_MATCH_CURRENT_READING with colour and rank), so the views stop hardcoding strength colours, 
      derivation: Direct read of window.ITALY_LABEL_VERDICTS, provenance CANONICAL. The absenceRule string must be surfaced next to any 'not found' figure so the screen never reads as 'ADAMA has no product' (§10).
  * competitorActivities :: Two recency counters are rendered (the 'movements · 30d' KPI card and, if ever revived, the 7-day strip). Each view currently re-derives its own window off the fixture. One projection, computed once against the single re
      fields: recent30 — NEW top-level number on APP: records with daysFromRef !== null && daysFromRef <= 0 && daysFromRef >= -30. Measured 11., recent7 — same at 7 days. Measured 1., undatedCount — records with no startDate. Measured 89 (all ORGANIC_VIDEO), i.e. 17.7% of the corpus can never enter a recency count and the screen must say so., italyReachCount — records with geoClass REACHED_IN_ITALY. Measured 41
      derivation: Filters over the existing competitorActivities.records built at italy-app-model.js:132-148, using the existing daysFrom() against REF = 2026-09-02. No new source.
  * windows :: The four radar status cards and the nav/sidebar window counts all need the same status tally; each screen recomputing it off a different array is exactly how the 11 cross-screen conflicts happened before.
      fields: statusCounts — NEW map on APP: { WINDOW_CLOSED: 16, WINDOW_OPEN: 6, DATE_UNKNOWN: 5, NEXT_CYCLE: 2 }, dateStateCounts — { EXPECTED_NORM: 24, DATE_TO_CONFIRM: 5 }, regionCounts — { Veneto: 6, Puglia: 6, 'Emilia-Romagna': 4, Lombardia: 3, Piemonte: 3, Sicilia: 3, Toscana: 2, 'Friuli-Venezia Giulia': 1, 'Trentino-Alto Adige': 1 } — 9 regions, sums to 29
      derivation: Tallies over windows.records[].status / .dateState / .region, which come straight from canonical CURRENT_STATUS / DATE_STATE / REGION. Nothing computed, nothing inferred.
  * regulatoryLinks :: The 'links' KPI card (2559) shows 80 fixture links. The only real relationship table is IG.LINKS (219 label use rows), which the model never exposes, so the card has no honest replacement today.
      fields: count 219, byCrop / byProduct tallies, evidence, labelUrl, prov passed through per row so any drill-down stays traceable
      derivation: coll(IG.LINKS.map(...), P.REAL_SOURCE, 'righe d'uso lette dalle etichette autorizzate'). This is a DIFFERENT fact from 'portfolio positions assessed' (19) and from 'verified matches' (12); whichever the card keeps, its label must change to match.

-- FIELDS THAT DIE (12) --
  [REMOVE_SUBCOMPONENT] Notification feed (6 alerts) @ portale.html markup 144-157 (bell, green unread dot, dropdown, sc-for notifs); s :: Every one of the six asserts a fact the real data does not contain — a field message from Friuli 2 minutes ago, a dealer conversation adding context to Flavescenza Dorata, '14 days remaining' on a corn borer window, a BA
  [REMOVE_SUBCOMPONENT] Region tile / region rank 'signals' count (+N signals) @ portale.html markup 264 ('+{{ r.signals }} signals'); script 2564-2565; italy-de :: Counts D.SIGNALS, the 56 DEMO_SCENARIO records, per region — Piemonte 9, Emilia-Romagna 8, Veneto 7. There are exactly 3 real future signals and not one of them has an Italian region: their REGION values are 'NAO SEI — o
  [DERIVABLE_FROM_REAL] 'Competitor movements · 30 days' = 43 @ portale.html 2560 (KPI card 8), 3391→markup 88 (sidebar kpi.movements) :: Derivable but the value collapses: 43 → 11. Two honesty conditions travel with it. First, 89 of the 503 real activities (17.7%) have no start date at all — every ORGANIC_VIDEO record — so they can never enter a recency c
  [REMOVE_SUBCOMPONENT] 'Product links' = 80 @ portale.html 2559 (KPI card 7) :: There is no real 80. The number is the total of productLinks hung on the 29 demo CASES. Upstream, all 29 canonical windows carry PRODUCT_MATCHES = [] (measured — the array is empty on every record), and the label audit a
  [DERIVABLE_FROM_REAL] 'Verified label matches' = 13 @ portale.html 2558 (KPI card 6) :: 13 → 12. Small in absolute terms, large in kind: this is the headline claim about ADAMA's portfolio fit, and it is currently one higher than the audit supports because it counts demo case-links rather than the audit's 12
  [SHOW_UNKNOWN] 'People monitored' = 39 @ portale.html 3363 (source tile 2); also folded into the sources nav badge at 260 :: 39 = 32 demo people + 7 invented Technical Sales Representatives. The TSRs are fictional ADAMA staff — internal, not externally observable, and forbidden from the core by §1. Real people split into two non-equivalent set
  [REMOVE_SUBCOMPONENT] Six agronomic timing buckets (OPEN NOW / NEXT 30 / 31-60 / 61-90 / 3-6 MONTHS / NEXT CYCLE) @ portale.html 2874-2875 (WK, windowBuckets), exported at 3403 as windowBuckets an :: Dead and false. Grep proves no markup consumes windowBuckets or windowKpi anywhere in lines 51-2374. The numbers are invented besides: 21 windows 'in the next 30 days' from a demo day-count, against a canonical reality o
  [REMOVE_SUBCOMPONENT] 'What changed · last 7 days' strip (18 items) @ portale.html 3441 (whatChanged export); italy-demo-data.js:470 :: Also dead — 'whatChanged' appears exactly once in the entire file, in the export, with no markup binding. Its claim of 18 competitor items in 7 days is contradicted by the real corpus, which yields exactly 1. If a recenc
  [REMOVE_SUBCOMPONENT] Sidebar 'windows' counter (second row of the today box) @ portale.html markup 87 ({{ kpi.windows }}), fed by italy-demo-data.js:770 (CASES :: It is the same number as the row above it. D.KPI.windows and D.KPI.windowOpen are both 'cases whose window is open' and both measure 6, shown under two different labels so the box looks fuller than it is. Either delete t
  [DERIVABLE_FROM_REAL] Source-group tiles: official 16, research 10, media 8 @ portale.html 3364-3365 :: Derivable, and every one shrinks: official 16→5, research 10→4, media 8→4, organizations 53→31. Only field & producer goes up, 8→10. Two real source types (PEOPLE 1, ADAMA/own channel 1) have no tile, so the seven tiles 
  [DERIVABLE_FROM_REAL] Transparency panel totals: 244 real / 491 demo @ portale.html 2526-2534, markup 122-137 :: The single most misleading number in this domain, and it is misleading in the product's favour once fixed: rebuilt from APP.provenanceSummary the same panel reads 1038 real / 29 derived / 100 demo, plus a 740-row archive
  [DERIVABLE_FROM_REAL] 'News & trade media · N real items' = 9 @ portale.html 3365 (D.NEWS.length inside the tile label) :: 9 → 8, and the word 'real' needs qualifying. D.NEWS is ITALY_REAL.NEWS (9 curated items living inside the demo bundle); the ingested registry IG.NEWS has 8. Of those 8: 5 are dated in 2026, 2 are from 2021 and 2022, and 

-- PLAN --
  1. 1. italy-app-model.js — add the labelVerdicts collection: read window.ITALY_LABEL_VERDICTS (currently never read) into collections.labelVerdicts with verified 12 / notFound 7 / assessed 19, the STRENGTH map, and the ABSENCE_RULE string. Provenance CANONICAL.
  2. 2. italy-app-model.js — add the people collection from IG.PEOPLE (15 records: id, person, category, organization, role, identityEvidence, roleEvidence, country). Do not merge with researchers; if a combined figure is needed expose distinctPeople = 66 with the dedupe rule written in a comment.
  3. 3. italy-app-model.js — add the derived source group. Extend the sources mapping (currently lines 204-209) with a literal TYPE→group map: OFFICIAL→OFFICIAL, RESEARCH|RESEARCH_INSTITUTION→RESEARCH, FIELD|COOPERATIVE|PRODUCER_ORG→FIELD, TECHNICAL_MEDIA→TECHNICAL_MEDIA, MARKET|COMPANY|COMPETITOR→MARKET, PEOPLE→PEOPLE, ADAMA→OWN. Also pass through accessStatus, latestObservation, limitations, country. Assert in a comment that the groups sum to 31.
  4. 4. italy-app-model.js — add regulatoryLinks = coll(IG.LINKS, REAL_SOURCE) (219 rows) so the 'links' KPI has a real target if it is kept.
  5. 5. italy-app-model.js — add the tallies: windows.statusCounts, windows.dateStateCounts, windows.regionCounts; competitorActivities recent30 (11), recent7 (1), undatedCount (89), italyReachCount (414). All computed once, against the single REF.
  6. 6. italy-app-model.js — extend provenanceSummary with derived, isIndex and label per layer, and add provenanceTotals { real: 1038, derived: 29, demo: 100, indexRows: 740 } computed over the non-index rows. Update APP.counts to include the new collections.
  7. 7. portale.html:2549 — replace 'const K = D.KPI' with the model-backed K object (see the replacement expression in occurrences). Do not delete D.KPI from italy-demo-data.js yet; other domains still read it.
  8. 8. portale.html:2552-2560 — repoint the 8 KPI cards. Cards 1-5 keep their values (29 / 6 / 2 / 5 / 16) with honest sources; card 6 becomes 12; card 8 becomes 11. Decide card 7: delete it, or rebind to regulatoryLinks 219 AND rewrite T.kpiLinks / T.kpiLinksSub in italy-i18n.js so the label matches the new meaning.
  9. 9. portale.html:2564-2565 — introduce REGION_GRID (geometry only, from D.REGIONS) and rebuild regionTiles/regionRank on windows.regionCounts. Drop the signals field entirely.
  10. 10. portale.html markup 259-265 — delete the '+N signals' span (line 264) and rebind r.cases → r.windows (line 263).
  11. 11. portale.html:2608 — repoint navDef: radar APP.counts.windows, windows APP.counts.windows, competitors APP.counts.competitorActivities, science APP.counts.scienceRecords, archive APP.counts.archive, sources APP.counts.sources. Leave future/voices/portfolio as they already are.
  12. 12. portale.html:2625 — repoint the case mini-map at REGION_GRID (no visible change; single geometry source).
  13. 13. portale.html:2874-2875 and 3403 — delete WK, windowBuckets, and the windowBuckets / windowKpi entries in the export. Verify with grep that neither name survives.
  14. 14. portale.html:2524-2535 — rebuild the transparency panel from APP.provenanceSummary and APP.provenanceTotals. Render ~15 layer rows plus a separate archive-index footnote; exclude the index row from the totals; add the overlap note for products/regulatory/commercial. Delete the local IG and cnt helpers.
  15. 15. italy-i18n.js — add IT/EN labels for the new dataState layers and rewrite t.dsNote to describe a mostly-real model with the overlap caveat. If KPI card 7 changed meaning, rewrite kpiLinks/kpiLinksSub too.
  16. 16. portale.html:3363-3365 — rebuild sourceKpis on the derived source groups (31 / 5 / 4 / 10 / 4 / 6), split People into 'ruolo documentato 15' and 'ricercatori 60', add the 'altre rotte 2' tile so the tiles reconcile to 31, and change the media label suffix to APP.counts.news (8).
  17. 17. portale.html:3391 — export the rebuilt K and change todayLabel to D.fmt(AM.REF).
  18. 18. portale.html:3407 and markup 144-157 — delete the notifications feed: the notifs prop, the showNotifs state, the bell, the green unread dot and the dropdown.
  19. 19. portale.html:3441 — delete 'whatChanged: D.WHAT_CHANGED' from the export.
  20. 20. portale.html:3123-3124 — leave the Field Sales KPIs on the fixture, but confirm the screen carries a visible 'dati simulati' label and that none of these numbers reaches dataState's real column or any core nav badge.
  21. 21. Verification pass — grep portale.html for 'D.KPI', 'D.WINDOW_KPI', 'D.REGION_STATS', 'D.NOTIFICATIONS', 'D.WHAT_CHANGED': the only survivors should be D.REGION_STATS/D.REGIONS used purely as grid geometry and D.FIELD_KPI in the labelled demo. Then load the page and read off every badge against the expected-value table in this spec.

-- RISKS --
  ! The transparency panel's layers overlap. products (166) already contains regulatory (163) and commercial (44); archive (740) re-indexes science, market, competitor, voices, events, news and windows. A naive sum of all 20 model layers reads ~1800 and would be a new, larger lie than the one being fixed. The panel must either use a curated non-overlapping row set or mark every contained row explicitl
  ! Four radar numbers do not move (29 / 6 / 2 / 5 / 16) because the 29 demo CASES are a 1:1 shadow of the 29 canonical windows. It will look as though nothing was fixed. It was: the source changed from a fixture to the canonical contract, so the next data load moves them. Say this explicitly in the handoff or someone will 'revert the no-op'.
  ! Nav badges move in both directions and one is a 7x jump (competitors 72→503). Anyone who has memorised the demo numbers will read the change as a bug. Publish the before/after table with the change.
  ! Repointing the source group chips (portale.html:3346) at the model will empty the source LIST unless the derived group field lands first — AM sources currently have group: null for all 31 records, so every group filter returns zero. Order matters: model change first, view change second.
  ! The news period chips (portale.html:3348) filter on a .days field that exists only on the demo news objects. The real IG.NEWS records have DATE strings only — including one literal 'NAO SEI' and two items from 2021/2022. Repointing news at the model without adding a daysFrom(DATE) projection makes those chips read 0.
  ! Deleting the notification bell removes the only 'live' affordance in the header. Expect pushback that the portal now looks static. It is static: one reference date, no stream. The honest answer is that the bell was simulating a service that does not exist.
  ! 89 of 503 competitor activities (17.7%) have no start date, all of them ORGANIC_VIDEO. Any recency counter silently drops them. If the caveat is not rendered, the 30-day figure of 11 quietly implies the other 492 were older, which is not what the data says.
  ! AM.products[].links — the source of the current 13 verified matches — are themselves built from D.CASES inside italy-app-model.js (lines 102-105). Fixing the KPI to read ITALY_LABEL_VERDICTS does not fix the underlying link graph, which is still fixture-fed. That belongs to the portfolio domain but will contradict this domain's 12 if it is not aligned.
  ! APP.counts.news is 8 while D.NEWS is 9 (ITALY_REAL.NEWS). Two news corpora are live simultaneously. Whichever the sources screen ends up rendering, the tile label and the list must come from the same one.
  ! italy-i18n.js needs roughly nine new layer-label keys plus a rewritten t.dsNote. Missing keys fall back to the English literal in the || expressions, which will silently ship English strings into the Italian default (§11).

-- OPEN QUESTIONS --
  ? KPI card 7 ('links', currently 80): delete it, or keep the slot and rebind to the 219 real label use rows with a new label? These are different facts and the card cannot straddle them. Product decision, not an engineering one.
  ? Should the notification bell be deleted outright, or kept inert with a single 'NESSUN SERVIZIO DI NOTIFICA COLLEGATO' row? Deleting is the honest minimum; keeping it inert preserves the header rhythm at the cost of a control that does nothing.
  ? Does the transparency panel show all ~15 model layers, or a curated subset? All 15 is more complete but forces overlap notes on 4 rows; a subset is cleaner but is itself an editorial choice the panel should disclose.
  ? 'People monitored': one tile (66 distinct names, dedupe rule stated) or two (15 with documented role · 60 researchers in scope)? Two is more truthful — the sets are not the same kind of evidence — but adds a tile to a row already at 7.
  ? The sources nav badge: 31 (sources only) or 31 + people? The screen shows both sources and people behind that one nav item, so 31 slightly under-describes it. Recommend 31 with the people tiles carrying their own numbers.
  ? Canonical windows are placed in the 'derived' column with the note 'expected agronomic norms'. Measured, 24 of 29 carry DATE_STATE = EXPECTED_NORM and 5 DATE_TO_CONFIRM, so 'derived' is defensible — but they are the CANONICAL layer, the highest precedence in the contract. Real, derived, or split 5/24? Needs an upstream
  ? Should the 740-row archive index appear in the panel at all? It is real work and real traceability, but as a number in a real/derived/demo table it invites double counting.

====================================================================================================
DOMAIN: MARKET PULSE
CONFIDENCE: high  |  realSource: APP.collections.marketObservations (77 records, all provenance REAL_SOURCE, all sourceId IT-SRC-AGRIFOOD = "European Commission — Agri-food Data Portal", FREQUENCY weekly, LATEST_OBSERVATION 2026-08-23) is the only real backing for prices. Coverage measured by crop: Wheat 13 obs / 13 piazze / all current / latest period end 2026-08-23; Maize 11 / 11 / all current / 2026-08-23; Durum Wheat 8 / 8 / 7 current / 2026-08-09; Barley 8 / 8 / all current / 2026-08-09 (no crop tab exists for it); Olive 36 / 13 piazze / 26 current + 10 dead series / 2026-08-02; Grapevine 1 record, series stopped in 2025
SUMMARY: Today the Market Pulse screen is driven end-to-end by window.ITALY_MARKET (italy-market-pulse.js, 596 lines), which is NOT ingested data: it is a hand-authored editorial fixture. It carries no record id, no SOURCE_ID and no PROVENANCE on any of its 8 crop objects, and it attributes its numbers to ISMEA, CUN Grano Duro, BMTI/Unioncamere, ICQRF, Agrofarma-Federchimica, Baltic Exchange, JRC MARS and Veneto Agricoltura — measured: NONE of those appear in IG.SOURCES (31 registered sources), and 20 of its 23 chart values do not exist anywhere in the 77 real market observations (the 3 that coincide numerically — 245, 4.55, 250 — share no id and sit on unrelated products). From that fixture the screen renders a market temperature verdict (PRESSURED / BALANCED / MIXED SIGNALS), a Sintonia "reading", drivers, a PAST-TODAY-OUTLOOK trajectory, a 3-6 month outlook, ISMEA farmer confidence (-1.4), a Baltic Dry input-cost card, production/stocks/trade, per-piazza tables, "what changed", commentary, and an industry tab of Agrofarma sector figures. The only real backing is APP.collections.marketObservations: 77 EU Commission Agri-food Data Portal weekly price rows (IT-SRC-AGRIFOOD, GEOGRAPHY uniformly "IT — praca nomeada"), covering Wheat 13, Maize 11, Durum 8, Barley 8, Olive 36, Grapevine 1 — and ZERO for Tomato, Sugar Beet and Apple, which are three of the eight crop tabs. Five legacy D.* re

-- MODEL CHANGES --
  * marketObservations (extend the existing 77-record projection in italy-app-model.js, lines 151-160) :: The view currently has to parse Italian/EU date strings and guess which crop a product code means. Push that into the model so the screen only reads fields. Also, 12 of 77 records are dead series carrying an explicit war
      fields: cropKey — mapped from GROUP + PRODUCT with an explicit, declared lookup: GROUP 'OLIVE_OIL' -> 'Olive'; GROUP 'WINE' -> 'Grapevine'; PRODUCT 'MAI|FEED' or 'Feed maize' -> 'Maize'; 'BLTPAN|PAN' or 'Breadmaking common wheat' -> 'Wheat'; 'DUR|UNKNOWN' or 'Durum wheat' -> 'Durum Wheat'; 'ORGFOUR|FEED' or 'Feed barley' -> 'Barley'. One record has PRODUCT null (IT-MKT-077, the wine row) and must resolve 
      derivation: 
  * marketByCrop (NEW projection, one row per crop key) :: So the screen can stop asking a fixture 'what is the state of this market' and start asking the model 'what did we actually observe for this crop'. This is the object that replaces mc.temp, mc.reading, X.fresh, X.traj, m
      fields: cropKey and cropName, hasData — false for Tomato, Sugar Beet and Apple (0 records each), which is the single most important field on this screen, observationCount — measured: Wheat 13, Maize 11, Durum Wheat 8, Barley 8, Olive 36, Grapevine 1, currentCount / stoppedCount — Wheat 13/0, Maize 11/0, Durum 7/1, Barley 8/0, Olive 26/10, Grapevine 0/1, piazzaCount — distinct MARKET values: Wheat 13, Maiz
      derivation: 
  * portfolioLinksByCrop (NEW projection over IG.LINKS, 219 records) :: The green ADAMA panel currently gets its product list from D.CASES[].products. Give it a regulatory-fact source instead so the view can stop touching the demo fixture. Every IG.LINKS row carries evidence 'REGULATORY_FACT
      fields: cropKey — mapped from IG.LINKS.crop tokens (MAIZE, DURUM_WHEAT, COMMON_WHEAT, WHEAT_GENERIC, OLIVE, GRAPEVINE, TOMATO, SUGARBEET, APPLE, BARLEY, RICE, SORGHUM, SOYBEAN, POTATO, TRITICALE), products — distinct product names. Measured distinct counts: Maize 3, Durum Wheat 3 (incl. WHEAT_GENERIC), Wheat 5, Olive 4, Grapevine 1, Tomato 7, Sugar Beet 3, Apple 2, linkCount — row counts: Maize 8, Durum W
      derivation: 
  * marketSummaries (NEW, thin projection over IG.MARKET_SUMMARIES, 5 rows) :: There is upstream written market analysis for MAIS, FRUMENTO DURO, OLIVO/OLIO DI OLIVA, VITE/VINO and RISO, but only the file manifest (CROP, FILE, CHARS) is in the client bundle — the .md bodies are not. The screen shou
      fields: crop, file, chars, loaded:false, note 'analisi scritta esistente a monte, non caricata in questo pacchetto'
      derivation: 

-- FIELDS THAT DIE (30) --
  [REMOVE_SUBCOMPONENT] temp — the market temperature verdict (SUPPORTIVE / BALANCED / PRESSURED / MIXED SIGNALS / COOLING / TIGHTENING / VOLATILE) @ italy-market-pulse.js per crop; portale.html 2917, 2920, 2951, 2955, 2972, 2979, :: There is no derivation from 77 weekly price rows to a market verdict, and inventing one would be a Sintonia interpretation presented as an observation. The hero must carry a coverage line instead. This is the single bigg
  [REMOVE_SUBCOMPONENT] reading — the 3-5 sentence narrative per crop @ italy-market-pulse.js 49, 101, 178, 217, 290, 360, 385, 410; consumed via mp (sp :: Contains hard facts with no record: '+6.1% EUR238.56/t Sep-2025', 'CUN range of EUR231-236/t', '~153,000 t stocks +33%'. Not salvageable.
  [REMOVE_SUBCOMPONENT] drivers[] — the 'WHY SINTONIA READS IT THIS WAY' tiles @ markup 980-988; built at 2952 :: 6 tiles per crop of arrow + claim + value, all fixture. Nothing in the real data produces a driver.
  [REMOVE_SUBCOMPONENT] current[] — PRICE MOMENTUM / PRODUCTION / SUPPLY / STOCKS / IMPORT COMPETITION / GROWER ECONOMICS / FARMER CONFIDENCE @ built at 2953, 2980-2983; rendered at markup 1020, 1045 and again in csMarket at :: Verdict strings ('FIRM vs LAST YEAR', 'BELOW STATED COST OF PRODUCTION'). changeVsPrev and changeVsYearAgo exist per record (72/77 and 67/77) but they are per-piazza percentages, not a crop-level momentum state.
  [REMOVE_SUBCOMPONENT] outlook[] and outlookNote — the 'NEXT 3-6 MONTHS' cards @ markup 1131-1144; built at 2954 :: Forward-looking claims (JRC MARS routes, 'NORTH-AMERICA DEPENDENT', 'REBALANCING PHASE'). No forward-looking source is ingested at all.
  [REMOVE_SUBCOMPONENT] traj — PAST / TODAY / OUTLOOK trajectory strip @ markup 1116-1125; built at 2972 from MK.EXTRA[key].traj :: Three invented states per crop plus a nowLine map ('WEAK MOMENTUM', 'SIGNALS POINT BOTH WAYS'). Nothing in the record set supports past-present-future framing.
  [REMOVE_SUBCOMPONENT] forces[] — 'WHAT IS DRIVING THE MARKET' @ markup 1148-1165; built at 2970-2971 from MK.EXTRA[key].forces :: Up to 5 cards per crop, each with an explanatory sentence and a source line ('Baltic Exchange, 3 Aug 2026', 'ICQRF Frantoio Italia', 'MASAF decree, 16 Jan 2026'). None of those sources is registered.
  [REMOVE_SUBCOMPONENT] price.headline / product / geo / period / published / cadence @ markup 1023-1024, 1039; mp.price at 2956 :: Replace with a real record, not a repair: pick the most recent current-series observation for the crop and print its priceRaw, unit, market, product and referencePeriod. Note the real GEOGRAPHY field is the same literal 
  [REMOVE_SUBCOMPONENT] price.points / price.series / chartNote — the multi-series price chart @ markup 1169-1186; built at 2927-2931 :: Measured: 20 of 23 values do not exist in the real data. Rebuild as a per-piazza bar comparison inside ONE product definition and ONE unit, or drop the card for crops where that leaves fewer than 2 bars (Grapevine leaves
  [REMOVE_SUBCOMPONENT] spark — the sparkline trend @ markup 1025-1034; built at 2932-2944 :: Requires 3+ points in one series. Real data has exactly one ingested observation per piazza per product, so zero crops qualify. It cannot be rebuilt without a new ingestion.
  [REMOVE_SUBCOMPONENT] production[] — Production / Area / Yield / Stocks tonnages @ markup 1042-1058; built at 2959, 2981 :: No production, area, yield or stock array exists in ITALY_INGEST. The 'PRODUCTION · YIELD · STOCKS' card has no real content and the whole card must go.
  [REMOVE_SUBCOMPONENT] trade + flow — SUPPLY & TRADE card with PRODUCTION/IMPORTS/EXPORTS/STOCKS bars @ markup 1060-1077; built at 2959, 2974-2978 :: The flow bars are already 0% for imports and exports on every crop; with production and stocks also gone, the card is four empty bars. Remove it.
  [REMOVE_SUBCOMPONENT] confidence — ISMEA Indice del Clima di Fiducia (-1.4, Q1 2025, under-40 +2.7 / over-40 -2.2) @ italy-market-pulse.js 13-26; markup 1079-1095; built at 2960 :: ISMEA is not among the 31 registered sources and no confidence record exists. The entire FARMER SENTIMENT card dies.
  [REMOVE_SUBCOMPONENT] inputs — INPUT COST PRESSURE incl. the Baltic Dry Index 2,843 'OBSERVED' value @ italy-market-pulse.js 35-40; markup 1097-1110; built at 2961 :: Most dangerous single item on the screen: it is explicitly flagged state:'OBSERVED' and renders a precise number and date, so a reader cannot tell it apart from a real observation.
  [REMOVE_SUBCOMPONENT] weather @ markup 1138-1142; built at 2962 :: JRC MARS is not a registered source; no crop-condition record exists.
  [REMOVE_SUBCOMPONENT] commentary @ markup 1258-1262; built at 2962 :: Explicitly a 'Sintonia reading' of sources we never ingested.
  [REMOVE_SUBCOMPONENT] changed[] — the WHAT CHANGED dated log @ markup 1250-1257; built at 2963 :: Dates and events with no record ids. There is no ingested change log for market data.
  [REMOVE_SUBCOMPONENT] contextIt — Italy macro strip (value added +0.6%, exports +3%, imports +6.6%, consumer confidence 92.6) @ italy-market-pulse.js 28-33; markup 1270-1273; built at 2962 :: ISMEA and ISTAT attributions with no records. ISTAT IS a registered source (IT-SRC-ISTAT) but contributes 0 of the 77 market rows.
  [REMOVE_SUBCOMPONENT] CP_MARKET.metrics — 9 Agrofarma-Federchimica industry figures @ italy-market-pulse.js 436-446; markup 1290-1299; built at 2995 :: Sector turnover ~EUR1bn, +2% value / +1% volume, R&D >EUR30m, ~2,000 employees, 3rd largest market in Europe, illegal market ~10%. No Agrofarma source is registered and no industry record exists. The CROP PROTECTION MARK
  [REMOVE_SUBCOMPONENT] INTERNAL[] — SELL-IN, SELL-OUT, CRM/PIPELINE, ORDERS, DISTRIBUTOR INVENTORY, WAREHOUSE STOCK, PRICE REALIZATION, SALES BY REGION @ italy-market-pulse.js 455-464; passed as cpm.internal at portale.html 2995 — ver :: This is the private-data hunt result inside the Market Pulse layer. Each entry pairs a private field with a 'why' clause arguing what Sintonia could do with it ('would connect market timing to real commercial conversatio
  [REMOVE_SUBCOMPONENT] SOURCES[] — the 10-row MARKET SOURCE MAP @ italy-market-pulse.js 491-502; markup 1321-1331 :: Lists ISMEA Mercati, CUN Grano Duro, BMTI/Unioncamere, ISTAT, Eurostat, EC DG AGRI, JRC MARS, ICQRF, Agrofarma, Veneto Agricoltura. Replace with AM.collections.sources.records.filter(s => s.type === 'MARKET') — measured,
  [REMOVE_SUBCOMPONENT] tomato / sugarbeet / apple crop tabs @ italy-market-pulse.js 357-430; portale.html 2916, 2918, 2920-2924 :: Measured 0 market observations each. They are already marked thin:true and their prose is honest about the absence, but they still render a temperature chip, a FARMER CONFIDENCE row and a trajectory. Either drop the tabs
  [DERIVABLE_FROM_REAL] LAST_REVIEW — 'Last source review 1 September 2026' @ italy-market-pulse.js 594; portale.html 3402; markup 1279 :: A second clock, which rule 6 forbids. Replace with AM.referenceDate (2026-09-02) or the source's LATEST_OBSERVATION (2026-08-23).
  [DERIVABLE_FROM_REAL] fresh[] — the DATA FRESHNESS rail @ italy-market-pulse.js 520-580 (EXTRA[key].fresh); markup 1001-1010; built at 296 :: The only fixture element on this screen that survives, because freshness IS computable: seriesState ('CORRENTE' vs 'PARADA_EM_YYYY'), the parsed end of REFERENCE_PERIOD, and daysFrom(AM.referenceDate). But it must stop l
  [DERIVABLE_FROM_REAL] gaps[] — WHAT WE STILL DO NOT KNOW @ markup 1266-1268; built at 2963 :: Keep the component, regenerate the content from measured coverage: 3 crops with zero observations; 12 of 77 rows on stopped series; STAGE missing on 37 of 77; PUBLICATION_DATE missing on 37 of 77; no production, trade, s
  [DERIVABLE_FROM_REAL] adamaDays / adamaStance / nextWindowLabel @ markup 1211, 1213; built at 2949, 2986-2988 :: Derivable from AM.collections.windows, but the STATE must be the canonical CURRENT_STATUS, not a days-threshold ladder invented in the view. Measured statuses available: WINDOW_OPEN, WINDOW_CLOSED, NEXT_CYCLE, DATE_UNKNO
  [DERIVABLE_FROM_REAL] productCount / products — 'registered products relevant to this crop' @ markup 1212, 1231; built at 2947, 2989 :: Derivable from IG.LINKS, but the number changes materially and the engineer must expect that: Maize 11 -> 3, Grapevine 8 -> 1, Wheat 6 -> 5, Olive 3 -> 4, Tomato 1 -> 7, Apple 1 -> 2, Sugar Beet 5 -> 3, Durum Wheat 9 -> 
  [DERIVABLE_FROM_REAL] caseCount — 'VIEW n OPPORTUNITIES' @ markup 1241; built at 2946, 2967 :: Today it counts demo cases. The honest count is upstream-real opportunities only — measured 3 in total across the whole application (IT-OPP-001 Grapevine, IT-OPP-002 Maize, IT-OPP-003 portfolio-transversal), so most crop
  [REMOVE_SUBCOMPONENT] mSignals — PORTFOLIO & RELATED SIGNALS bullets @ markup 1234-1236; built at 2948 :: Already dead: the crop join can never match (real futureSignals crops are 'MAIS', 'TRIGO e TRIGO DURO', 'TRANSVERSAL' against mc.crop 'Maize' / 'Durum Wheat'), and status is null on all 3 records. The markup also reads x
  [SHOW_UNKNOWN] w.issueL in RELATED CROP WINDOWS @ markup 1224; builder at 2945 produces only 'issue' :: Live rendering bug independent of the migration: the window label is blank today. The rebuild must emit issueL (through the existing il() translator) or the markup must read w.issue.

-- PLAN --
  1. 1. Freeze the contract first. In italy-app-model.js, extend the marketObservations map (lines 151-160) with cropKey, periodStart, periodEnd, daysSinceObservation, isCurrentSeries and stoppedYear. Declare the GROUP+PRODUCT -> crop lookup as a named constant in the model with a comment saying it is an interpretation of EC product codes, not an upstream field.
  2. 2. Add the marketByCrop projection to collections (counts, piazze, units, productDefinitions, latestPeriodEnd, changeCoverage, hasData, hasCurrentObservation, resolved source). Verify at runtime that hasData is false for Tomato, Sugar Beet and Apple and that Barley appears with 8 records.
  3. 3. Add portfolioLinksByCrop over IG.LINKS, and marketSummaries over IG.MARKET_SUMMARIES (manifest only, loaded:false).
  4. 4. In portale.html, replace the five D.* reads at 2945-2949 with the AM equivalents given above. Fix the mWins builder to emit issueL so the markup at 1224 stops rendering blank. Delete mSignals and its markup at 1234-1236, or repair the crop join first — do not leave it half-dead.
  5. 5. Rewrite the mp object (2950-2991) so it spreads marketByCrop plus a rows array of real observations, and stops spreading mc. Keep from MK only key, label, it and color, and only through an explicit ui sub-object.
  6. 6. Delete, in this order, so the grid never renders a half-empty section: the trajectory strip (markup 1116-1125), the outlook section (1128-1145), the forces section (1148-1165), the sparkline (1025-1034), the production card (1042-1058), the trade card (1060-1077), the confidence card (1079-1095), the input-cost card (1097-1110), the what-changed card (1246-1263), the economic-context list (1270-1273).
  7. 7. Rebuild the two cards that survive with real content: PRICE MOMENTUM (one real latest observation: priceRaw, unit, market, product, referencePeriod, changeVsPrev, changeVsYearAgo) and REGIONAL MARKETS · PIAZZA (every ingested observation for the crop, with a stopped-series badge wherever seriesWarning is set).
  8. 8. Rebuild the PRICE OBSERVATIONS chart as a per-piazza bar comparison scoped to one product definition and one unit, or drop the card when fewer than 2 bars remain. Never convert EUR/100kg to EUR/kg and never mix STAGE classes in one comparison.
  9. 9. Regenerate the DATA FRESHNESS rail and the WHAT WE STILL DO NOT KNOW list from measured coverage.
  10. 10. Rewrite the hero: replace mp.temp with a coverage line, keep LAST CONFIRMED OBSERVATION now fed by latestPeriodEnd, remove the SINTONIA INTERPRETATION pill.
  11. 11. Handle the crop tabs: hide or explicitly empty Tomato, Sugar Beet and Apple; decide with the product owner whether to add Barley. Re-source the nav badge at 2608.
  12. 12. Clean the industry tab: delete the Agrofarma metrics grid, replace cpm.sources with the registered MARKET sources, rewrite the feasibility rows so they describe the one connected route, and remove MK.INTERNAL from both cpMarketObj (2995) and italy-market-pulse.js (455-464).
  13. 13. Remove the cross-screen temperature bleed: mkCropTemp (2574 -> markup 679), mkTemp (2653 -> markup 732, 2762), compMoments market (3169 -> markup 1693), mkTempFor (3191/3204/3226 -> markup 2132), csMarket (2992-2993 -> markup 329-336). Coordinate with the owners of those four domains before deleting their props.
  14. 14. Re-source mpReview (3402) to AM.referenceDate and confirm no new Date() was introduced anywhere in the rewritten block.
  15. 15. Last: reduce italy-market-pulse.js to presentation metadata only (ICON, SEM, TONE_SEM, FRESH_COLOR, and a crop ui list of key/label/it/color) and delete CROPS[].temp, reading, drivers, current, outlook, price, regional, production, trade, confidence, weather, commentary, changed, gaps, plus CONFIDENCE_IT, CONTEXT_IT, INPUT_ROUTES, EXTRA, CP_MARKET.metrics, INTERNAL, SOURCES and LAST_REVIEW. Leaving the file intact but unread invites a future re-wiring.
  16. 16. Report the residual private-data phrases found outside this domain to the Crop Windows owner: portale.html 806 renders the literals 'Customer purchase timing' and 'Internal stock and readiness' in the window drawer's UNKNOWN column, and portale.html 2859 builds ['Pressure in adjacent regions','Customer purchase timing','Distributor stock position'] / [...,'Customer purchase timing']. Also portale.html 3128 seeds the Field Sales composer with dealer/customer prose, which is inside the labelled demo and may stay if that mode stays off by default.

-- RISKS --
  ! The screen shrinks hard. Of roughly 14 rendered components on the crop tab, 9 disappear outright and 2 more lose their headline number. Rule 3 says a smaller truthful screen wins and rule 12 says do not redesign, so the engineer must resist the instinct to backfill the empty grid space.
  ! Blast radius outside the domain. The temperature verdict is read on five other screens (Radar 679, Crop Windows 732/2762, Competitor Watch 1693, Science 2132, Opportunity case 329-336). Removing it silently changes four other people's screens. This must be sequenced with them, not done unilaterally.
  ! The crop mapping is an interpretation, not a field. 'MAI|FEED', 'BLTPAN|PAN', 'DUR|UNKNOWN', 'ORGFOUR|FEED' are EC product codes; nothing upstream says MAI = Maize. It is a safe reading, but it must be declared in the model and visible on screen, and 'DUR|UNKNOWN' carries an explicit unknown quality class that must not be presented as 'durum fino'.
  ! Stale data can be read as current. 12 of 77 rows are on stopped series and their own SERIES_WARNING says the last quotation is NOT a current price — including the only Grapevine row (stopped 2025) and 10 of the 36 olive rows (some last quoted in 2010). If the piazza table renders them plainly, the screen becomes less trustworthy than the fixture it replaced.
  ! Unit and stage collisions. Olive is EUR/100kg, cereals EUR/t, wine Euro/HL; and 40 of 77 rows carry a STAGE (farm gate vs delivery to first customer vs departure from farm) while 37 carry none. Any averaging, ranking or single 'the price' headline across those is methodologically wrong.
  ! One source, one point in time. All 77 rows come from a single provider (EC Agri-food Data Portal) and only the latest observation of each series was ingested, though the source holds 190-364 points per series. The screen must not imply a monitored multi-source market picture.
  ! Numbers the business already saw will change downward. The ADAMA panel's product count drops for 5 of 8 crops (Maize 11->3, Durum 9->3, Grapevine 8->1) and the opportunities count drops from 29 to 3 application-wide. Someone will read that as a regression rather than a correction; it needs to be announced, not just shipped.
  ! Three crop tabs go empty and one uncovered crop (Barley, 8 real observations) has no home. Whatever is decided, the nav badge, the crop rail and the sc-for placeholder hints must be updated together or the rail will render ghosts.
  ! MK.INTERNAL is currently unreachable, so deleting it is invisible in QA. If only the cpMarketObj prop is removed and the constant is left in italy-market-pulse.js, the next person to build an 'internal data' card will find it sitting there ready to use.

-- OPEN QUESTIONS --
  ? IG.MARKET_SUMMARIES points at five written analyses (MAIS, FRUMENTO DURO, OLIVO/OLIO DI OLIVA, VITE/VINO, RISO — 22k to 33k characters each) whose .md bodies are not in the client bundle. Is that upstream prose the intended replacement for the fixture reading/commentary, and should it be ingested? If yes, most of what 
  ? Rice has an upstream market summary and a canonical window but zero price observations and no crop tab. Barley has 8 current price observations and no tab, no window, no summary. What decides which crops get a Market Pulse tab — portfolio relevance, window coverage, or data coverage?
  ? Should the FEASIBILITY audit table survive? It is methodology rather than fact, which makes it legal, but every row currently names a route (ISMEA, CUN, BMTI, Eurostat, JRC MARS) that was never connected, so as printed it overstates what the product can do today.
  ? The EC records give week-on-week and year-on-year percentages per piazza (72/77 and 67/77 present). Is showing those percentages acceptable, or does a per-piazza percentage read as a market trend and therefore fall under the rule against presentation inventing a state?
  ? Who owns the removal of the market temperature from the four other screens, and does the Crop Windows drawer keep its 'MARKET PULSE ... context, not proof of demand' box at all once there is no verdict to put in it?
  ? The i18n key t.cannotProveList and t.notObservableNote are correct and should stay, but t.subMarket still promises 'condizioni, dinamica, offerta, fiducia e prospettive' — dynamics, supply, confidence and outlook all die in this migration. Who rewrites that subtitle in both languages?

====================================================================================================
DOMAIN: COMPETITOR WATCH + COMPETITOR x WINDOW (feed, filters, tabs, company profile, gallery, crop density, changed-this-week, company x crop MATRIX, ISSUE_ROWS, competitor products, event cross-links, lead-time)
CONFIDENCE: high  |  realSource: APP.collections.competitorActivities (503 records, already normalized in italy-app-model.js lines 132-148 with a correct geoClass: 414 REACHED_IN_ITALY, 89 MULTI_COUNTRY_OR_UNRESOLVED, 0 REACH_NOT_RESOLVED) + IG.COMP_COMPANIES (14 rows that are really 11 companies — the paid rows are title-case "Bayer" and the organic rows are upper-case "BAYER"; they must be merged on upper-case name) + IG.COMP_PRODUCTS (36) + APP.collections.windows (29 canonical windows) for the WINDOW cross-link + window.ITALY_LABEL_VERDICTS (12 VERIFIED crop/issue/product triples, 7 NOT_FOUND) for any ADAMA-portfolio stat
SUMMARY: Today the whole Competitor Watch screen is fed by window.ITALY_DEMO: 72 fabricated activities across 6 companies and 6 activity types (PAID 23, ORGANIC 16, VIDEO 10, PEOPLE 8, EVENT 9, PRODUCT/PORTFOLIO 6), plus 4 pre-baked demo aggregates (D.MATRIX, D.CROP_COLS, D.ISSUE_ROWS, D.WHAT_CHANGED) and 14 fabricated competitor products (D.CPRODUCTS). Every visible fact on the screen — headline, crop, issue, region, timing state ("IN WINDOW"/"EARLY"), "ADAMA RESPONSE: VERIFIED PORTFOLIO RESPONSE", "NEWLY OBSERVED", lead-time bars, video durations, transcripts, people mentions, event participation, creative thumbnails — is invented by the fixture or by actDeco() decorating the fixture. The real backing exists and is bigger: IG.COMP_ACTIVITIES has 503 records (414 PAID from META_ADS_LIBRARY reaching IT, 89 ORGANIC_VIDEO from YOUTUBE), 11 distinct companies, 36 proven competitor products. But the real records are much thinner per row: 320/503 carry no crop at all, 405/503 carry no issue, 401/503 carry no product, 89/503 carry no date, no url, no text and no country. The screen must therefore keep its layout but show far fewer decorated facts, drop four of its six activity types entirely, and stop claiming a portfolio verdict, a timing verdict or a lead time on each card.

-- MODEL CHANGES --
  * competitorActivities (extend the existing projection, italy-app-model.js lines 132-148) :: The view currently reads scalar a.crop / a.issue / a.product / a.days that do not exist, and re-derives crop names by string equality against demo crop labels. Normalizing once in the model keeps the view dumb and stops 
      fields: cropsCanonical: string[] — resolved ONLY through an explicit botanical/common-name identity table (Vitis vinifera->Grapevine, Zea mays->Maize, Olea europaea->Olive, Solanum lycopersicum->Tomato, Malus domestica->Apple, Triticum aestivum->Wheat, Oryza sativa->Rice, Beta vulgaris->Sugar Beet, Helianthus annuus->Sunflower, Prunus persica->Peach, Citrus->Citrus). This is a taxonomic identity, not an i
      derivation: 
  * competitorCompanies (NEW, REAL_DERIVED) :: The view builds companies from a 6-row fixture with invented counts, themes and confidence. IG.COMP_COMPANIES cannot be used raw because it has 14 rows for 11 companies — the paid rows are title-case (Bayer, IT-COMP-004,
      fields: company: string — canonical title-case name taken from COMP_ACTIVITIES.company (11 distinct: BASF, FMC, Corteva, Bayer, Syngenta, UPL, Sipcam, Certis, Sumitomo, Gowan, Nufarm), paidAdsReachingIt: number (sum of PAID_ADS_REACHING_IT across the merged rows), organicVideosInCorpus: number (sum of ORGANIC_VIDEOS_IN_CORPUS), observedTotal: number — verified to equal paid+organic per company (BASF 138, 
      derivation: 
  * competitorProducts (NEW, REAL_SOURCE + REAL_DERIVED) :: D.CPRODUCTS is 14 invented products; IG.COMP_PRODUCTS has 36 with an upstream proof string. The per-product context the detail screen wants (crops, issues, first/last seen) must be derived from competitorActivities, in t
      fields: product: string verbatim, never translated, company: string, adsReachingIt: number (upstream ADS_REACHING_IT), proof: string verbatim ('MARCA_REGISTRADA_NO_TEXTO do anuncio') — shown as the evidence line, activityIds: string[] — activities whose products[] contains this name, cropsObserved / issuesObserved: string[] derived from those activities only, firstSeen / lastSeen: ISO|null from those acti
      derivation: 
  * competitorCropDensity (NEW, REAL_DERIVED) :: Replaces D.CROP_COLS + the hand-tuned HIGH/MEDIUM/LOW thresholds. The scale must be relative to what was actually observed, and the level verdict must go.
      fields: crop: canonical crop name, items: number (Maize 67, Grapevine 42, Olive 24, Citrus 15, Tomato 12, Sunflower 9, Apple 5, Sugar Beet 4, Rice 3, Peach 2, Wheat 1), companies: number of distinct companies with at least one item on that crop, sharePct: items / max(items) — a bar length, not a market share, unresolvedItems: 320 (no crop term) and genericItems: 51 (umbrella term only), exposed as a foote
      derivation: 
  * competitorIssueDensity (NEW, REAL_DERIVED) :: Replaces the hand-written D.ISSUE_ROWS. Real issue terms are the advertiser's own words and there are only 14 of them, covering 98 of 503 records.
      fields: issue: verbatim term, issueScope: 'SPECIES' | 'GENERIC_TERM', items: number, companies: string[], NOTE: no paid column (all 98 are PAID), no ADAMA product column, no caseId link
      derivation: 
  * competitorMatrix (NEW, REAL_DERIVED) :: Replaces D.MATRIX, a hand-written 6x6 grid. Also needs to publish its own column order so matrixCols stops being a fixture.
      fields: columns: canonical crop names ordered by observed volume, records: [{ company, cells: [{ crop, n }] }] for all 11 companies, maxCell: number (measured 20, FMC x Olive) so the dot ramp is computed from the data instead of hard-coded at 3/5, allZeroCompanies: ['Syngenta','Sipcam','Certis','Sumitomo','Nufarm','Gowan'] — 6 of 11 companies have no crop-resolved activity at all and the matrix must say s
      derivation: 
  * competitorWindowMoments (NEW, REAL_DERIVED over CANONICAL) :: Replaces compMoments. The window side must come from AM.collections.windows verbatim (status is upstream, §7) and the ADAMA side from ITALY_LABEL_VERDICTS, which the model does not read today.
      fields: windowId, crop, issue, region, status: verbatim CURRENT_STATUS (16 WINDOW_CLOSED, 6 WINDOW_OPEN, 2 NEXT_CYCLE, 5 DATE_UNKNOWN) and daysToStart from the model's daysFrom(), companiesObserved / itemsObserved on that crop (whole corpus; there is no honest 30-day slice — measured 11 records in 30 days across all crops), portfolioVerified: products from ITALY_LABEL_VERDICTS.VERIFIED matching [crop, iss
      derivation: 
  * labelVerdicts (NEW top-level accessor on ITALY_APP_MODEL) :: italy-app-model.js does not read window.ITALY_LABEL_VERDICTS at all (verified: the file never references it). Product link strengths today come from D.CASES[].productLinks — a demo fixture — at lines 102-105. Every ADAMA
      fields: verified: [crop, issue, product][] (12), notFound: [crop, issue, product][] (7), strengthFor(crop, issue, product) -> VERIFIED_LABEL_MATCH | NO_CONFIRMED_MATCH_CURRENT_READING, auditDate, scopeNote, absenceRule verbatim, so any negative statement can quote the upstream absence rule instead of asserting absence
      derivation: 

-- FIELDS THAT DIE (28) --
  [REMOVE_SUBCOMPONENT] a.headline / a.observed @ portale.html markup 1732 ({{ a.headline }}), 277 ({{ a.observed }}); produced at :: No real record has a Sintonia-written headline. The nearest real thing is `text` — the advertiser's own Italian ad copy, present on 392/503 records, average 239 characters. Show it verbatim as a quoted excerpt under a la
  [REMOVE_SUBCOMPONENT] a.crop / a.issue / a.product (scalars) @ 2573 winMatch(a.crop, a.issue), 3158 filters, 3168/3169 density, 3157 productObj :: Real records carry ARRAYS. Measured: crops non-empty on 183/503 (of which only 132 resolve to a real crop and 51 are umbrella words like colture/cereali), issues non-empty on 98/503, products non-empty on 102/503. 320 of
  [REMOVE_SUBCOMPONENT] a.region @ markup binding {{ a.region }}; fixture value 'DEMO' :: There is no region field on any of the 503 records and there cannot be: Meta Ad Library reach is country-level. Delete the region chip from the activity card.
  [DERIVABLE_FROM_REAL] a.days / a.when / a.dayLabel @ 3158 sort and fPeriod filter, 3162 feedGroups, 3163/3166/3167 the 7-day widgets, :: Derivable from `start` for the 414 paid records only. The 89 ORGANIC_VIDEO records have start = null, end = null, country = null, url = null, text = null — they are literally id/type/platform/company/companyRaw/prov and 
  [REMOVE_SUBCOMPONENT] a.newly / 'NEWLY OBSERVED' badge @ markup 1734 ({{ a.hasNewly }} / {{ a.newly }}) :: 'Newly observed' needs a previous snapshot to diff against. The corpus is a single load with no first-seen-by-Sintonia timestamp. A start date is when the advertiser started, not when we first saw it. Delete the badge.
  [REMOVE_SUBCOMPONENT] a.transcript / a.transcriptLabel / a.duration @ 2599 transcriptLabel, markup 1727 (video overlay: {{ a.duration }}, 'YouTube · { :: No transcript, duration, view count or title exists on any ORGANIC_VIDEO record. (IG.CHANNELS[30] does carry EXAMPLE_TITLE/EXAMPLE_URL/VIEWS, but those are channel exemplars, a different collection and a different domain
  [REMOVE_SUBCOMPONENT] a.topics / a.kind / a.color / a.companyColor / a.brand @ fixture fields consumed by actDeco and the radar list ({{ a.color }} at markup 2 :: topics, kind and brand ('Brand captured') do not exist and carry no real equivalent. color/companyColor are VISUAL_ONLY and may keep coming from the static CH map under record.ui per §4.
  [DERIVABLE_FROM_REAL] a.companyLabel @ 3167 topMoves, markup 1719 (card header) :: Replace with the observed page name `page` (414/503, e.g. 'Bayer Crop Science Italia', 'BASF Agricultural Solutions'). For the 89 organic records page is null — show the bare company name; do not invent a legal entity na
  [REMOVE_SUBCOMPONENT] a.person / a.personId / a.personRole / a.relationship / a.personInitials — the entire PEOPLE activity type @ 2599 actDeco, markup 1728 (a.isPeople block), 3159 PEOPLE tab, 3157 countRows 'P :: There is no PEOPLE activity type in reality. Measured type distribution across all 503 records: PAID 414, ORGANIC_VIDEO 89. The demo has 8 PEOPLE records; reality has 0. Delete the tab, the card block and the company-pro
  [REMOVE_SUBCOMPONENT] a.event / a.eventId / a.participation / a.partColor / a.eventDates / a.eventCity — the entire EVENT activity type @ 2599 actDeco, markup 1729 (a.isEvent block), 3159 EVENT tab, 3182 evActs, 3184 t :: No EVENT activity type and no eventId. Separately, competitor participation itself is almost entirely unknown: of 18 real events, 16 have CONFIRMED_PARTICIPATION = {}, and the two populated ones name only ADAMA and SYNGE
  [REMOVE_SUBCOMPONENT] 'PRODUCT / PORTFOLIO' and 'VIDEO' activity types @ 3159 compTabs, 3166 changed7, 3157 countRows :: Neither exists. VIDEO is not a type; it is media = 'VIDEO' on 180 of the 414 paid records. PRODUCT / PORTFOLIO has no real counterpart at all. Two tabs remain: PAID and ORGANIC_VIDEO. The company-profile count grid drops
  [REMOVE_SUBCOMPONENT] a.timing / a.timingColor / a.leadLabel / a.windowLabel ('IN WINDOW', 'EARLY', 'X days before the expected window') @ 2573-2591 actDeco, markup 1735 (COMMUNICATION TIMING tile) :: This is presentation inventing an agronomic state, which §7 forbids, on top of a demo window list and a substring match between an issue name and a window name. Replace with a RELATED CROP WINDOW tile that prints the can
  [REMOVE_SUBCOMPONENT] a.adamaResponse / a.adamaColor / a.adamaProducts ('VERIFIED PORTFOLIO RESPONSE') @ 2580-2594 actDeco, markup 1736 (ADAMA RESPONSE tile) :: The strongest false claim on the screen. It is built from D.CASES[].productLinks — demo. Measured: all 29 canonical windows have PRODUCT_MATCHES = [], and ITALY_LABEL_VERDICTS has only 12 VERIFIED crop/issue/product trip
  [REMOVE_SUBCOMPONENT] a.caseId / a.caseTitle / a.ctaL ('VIEW RELATED CASE →') / a.openCase @ 2580, 2599, markup 1737 :: No real activity links to a case. The CTA becomes 'VEDI ANNUNCIO →' for the 414 paid records that have a url, and nothing at all for the 89 organic records (url null on every one).
  [REMOVE_SUBCOMPONENT] a.marketTemp / a.marketColor / a.goMarket (MARKET PULSE line on every card) @ 2595-2597 actDeco, markup 1737 :: Keying a market temperature off a.crop when 320/503 records have no crop means most cards would print a market state for a crop they never named. Cross-domain: this belongs to the Market domain and should be reached from
  [REMOVE_SUBCOMPONENT] D.CROP_COLS as the monitored-crop set @ 3168, 3169, 3441 matrixCols, 3443 compCropOptions, markup 1747 (grid-template-co :: The fixture declares 6 crops (Grapevine, Maize, Wheat, Olive, Tomato, Apple). Reality gives 11, in a different order, with Wheat at 1 item and two crops the fixture never mentions: Citrus (15) and Sunflower (9). The mark
  [REMOVE_SUBCOMPONENT] cropDensity level (HIGH / MEDIUM / LOW) and pct (acts/20) @ 3168 :: The 14/8 thresholds and the /20 denominator were fitted to a 72-record fixture. On 503 records they would label almost everything HIGH. A three-level verdict on communication volume also drifts toward reading as commerci
  [REMOVE_SUBCOMPONENT] leadTimes (COMMUNICATION LEAD TIME card) @ 3177-3178, markup 1753-1759 :: Not measurable. The earliest observation per company is set by the Meta Ad Library retention horizon, not by the company: BASF 2023-03-08, Corteva 2024-11-26, Syngenta 2025-07-29, Bayer 2025-08-28, FMC 2025-10-21, UPL 20
  [REMOVE_SUBCOMPONENT] galleryItems creative wall (a.creativeBg, a.galleryH, a.productOrHeadline) @ 3179, markup 1766 :: No creative asset is captured for any record. The real data says only media = 'VIDEO' (180) or 'IMAGE' (234) with no file, thumbnail or dimension. The varying tile heights are computed from a.days purely to look like a r
  [REMOVE_SUBCOMPONENT] ISSUE_ROWS.paid column @ 3180, markup 1787 header, 1788 ({{ r.paid }}) :: Measured: every one of the 98 issue-bearing activities is PAID (organic records have empty issues arrays). The PAID column would equal the ITEMS column on every row, which invites the reader to believe it is telling them
  [REMOVE_SUBCOMPONENT] ISSUE_ROWS.adama / adamaLabel ('Portfolio check needed') @ 3180, markup 1788 :: The demo pairs canonical ADAMA issue names with product lists. Real observed issue terms are the advertiser's own words and do not line up: 'Plasmopara viticola' vs the canonical 'Downy Mildew', 'Zymoseptoria tritici' vs
  [REMOVE_SUBCOMPONENT] ISSUE_ROWS.caseId / 'CONTEXT →' link @ 3180, markup 1788 :: No real issue term carries a case id.
  [REMOVE_SUBCOMPONENT] co.cases (competitor company -> ADAMA cases) @ 3157, markup {{ co.cases }} :: There is no field linking a company to a case or an opportunity. A shared crop is not a relationship.
  [REMOVE_SUBCOMPONENT] co.themes / co.confidence / co.brands @ 3157, markup {{ co.themes }}, {{ co.confidence }} :: Communication 'themes' and a 'confidence' score are pure fixture inventions with no counterpart in COMP_COMPANIES (which has exactly ID, COMPANY, PAID_ADS_REACHING_IT, ORGANIC_VIDEOS_IN_CORPUS, PAGES, PRODUCTS_PROVED, PR
  [DERIVABLE_FROM_REAL] co.crops / co.count / co.recent30 / co.last @ 3157, markup {{ co.crops }}, {{ co.count }}, {{ co.recent30 }}, {{ co.last }} :: All four are derivable from the real activities, but with honest holes: co.last and co.recent30 are null for the 5 organic-only companies (Sipcam, Certis, Sumitomo, Nufarm, Gowan), which must read DATA NON OSSERVATA, not
  [REMOVE_SUBCOMPONENT] cp.people / cp.cases / cp.first / cp.last (competitor product detail) @ 3185-3186, markup {{ cp.people }}, {{ cp.cases }}, {{ cp.first }}, {{ cp.last }} :: IG.COMP_PRODUCTS has only PRODUCT, COMPANY, ADS_REACHING_IT, PROOF. cp.people (people mentions) and cp.cases (ADAMA case links) have no real counterpart and die outright. cp.first/cp.last are derivable from the mentionin
  [REMOVE_SUBCOMPONENT] D.WHAT_CHANGED @ 3441 (whatChanged prop) :: Six frozen rows ('new paid creatives observed: 10', etc.) that are not computed from anything. Replace with the live 30-day, 2-type projection or drop the prop.
  [DERIVABLE_FROM_REAL] Data-State panel 'Competitor real / demo' split @ 2529 :: Today it reports the real/demo split of a 72-row fixture on the one panel whose entire purpose is to tell the user what is real. After migration it reads real 503 / demo 0 from AM.collections.competitorActivities.

-- PLAN --
  1. 1. In italy-app-model.js, add the botanical/common-name identity table and extend the existing competitorActivities projection (lines 132-148) with cropsCanonical, cropScope, issuesObserved, issueScope, hasDate/dateState, displayName, textExcerpt, geoLabel, geoCaveat. Do not add relatedWindows yet. Verify counts in node: 132 crop-resolved, 51 generic-only, 320 crop-absent, 414 dated, 89 undated.
  2. 2. Add competitorCompanies to the model, merging IG.COMP_COMPANIES's 14 rows into 11 on upper-case name and cross-checking each merged total against the per-company activity count (BASF 138, FMC 102, Corteva 94, Bayer 86, Syngenta 55, UPL 13, Sipcam 6, Certis 4, Sumitomo 2, Gowan 2, Nufarm 1 = 503). If any company fails the cross-check, stop and report rather than papering over it.
  3. 3. Add competitorProducts (36 from IG.COMP_PRODUCTS) plus the derived activityIds / cropsObserved / issuesObserved / firstSeen / lastSeen.
  4. 4. Add the three REAL_DERIVED aggregates — competitorCropDensity, competitorIssueDensity, competitorMatrix (with columns, maxCell, allZeroCompanies) — and register all of them in the `collections` object so AM.counts and the provenance summary pick them up automatically.
  5. 5. Add the labelVerdicts accessor to ITALY_APP_MODEL (reading window.ITALY_LABEL_VERDICTS, which the file does not read today) and only then add relatedWindows / competitorWindowMoments. Nothing that makes an ADAMA-portfolio statement may land before this step.
  6. 6. portale.html line 2529: point the Data-State competitor row at AM.collections.competitorActivities. This is the smallest edit and the one that most changes what the user believes; do it early so the rest of the migration is visible while it is in progress.
  7. 7. portale.html line 2608: nav badge -> AM.counts.competitorActivities.
  8. 8. Rewrite actDeco (2575-2599) as competitorActivity presentation only: keep the CH colour/icon lookup and the initials helper; delete winMatch's use inside it, the D.CASES portfolio block, the D.EVENTS lookups, duration, transcriptLabel, personInitials, galleryH and the per-record creativeBg. Delete winMatch (2573) entirely once nothing calls it.
  9. 9. Rewrite the competitor block 3157-3186 top to bottom against the new collections, in this order: companies -> actsAll -> compTabs -> feedGroups -> compStrip -> changed -> topMoves -> cropDensity -> compMoments -> matrix -> issueRows -> cproduct. Delete leadCrop/leadWin/leadTimes (3176-3178) and evActs/evd.story (3182, 3184) outright.
  10. 10. Update the renderVals tail (3441, 3443, 3444): compTotal, whatChanged, matrixCols, compCropOptions, compIssueOptions, and drop leadTimes/leadCrop from the returned props.
  11. 11. Apply the markup edits listed in markupImpact, deleting dead blocks (a.isVideo overlay, a.isPeople, a.isEvent, ADAMA RESPONSE tile, COMMUNICATION TIMING tile, MARKET PULSE line, lead-time card, PAID and PORTFOLIO columns of the issue table) before rewiring the surviving ones, so no binding is briefly pointed at a missing field.
  12. 12. Fix the two hard-coded 'repeat(6,1fr)' matrix grids (1747, 1748) and the 6-column issue-table grids (1787, 1788) to match their new column counts.
  13. 13. Update the Italian and English strings in italy-i18n.js for the labels that change meaning: the what-changed window (7 -> 30 days), RELATED CROP WINDOW, DATA NON OSSERVATA, NON NOTO, ASSET CREATIVO NON CATTURATO, RAGGIUNTO IN ITALIA vs MULTIPAESE / NON RISOLTO. T.PROV already has REACHED_IN_ITALY = 'RAGGIUNTO IN ITALIA'.
  14. 14. Verify in node that AM.counts.competitorActivities is 503, that geoClass splits 414/89, and that no surviving competitor prop reads window.ITALY_DEMO except the CH/TIMING colour maps and parseField's demo company list. A grep for 'D.ACTIVITIES', 'D.COMPANIES', 'D.MATRIX', 'D.ISSUE_ROWS', 'D.CPRODUCTS' and 'D.CROP_COLS' should return only line 2397.

-- RISKS --
  ! The screen gets visibly emptier per card while the totals get much bigger (72 -> 503). A reader who only looks at the header will think the product improved; a reader who opens a card will think it regressed. The what-changed strip in particular goes from six confident tiles to essentially one number, because only 1 real record falls inside 7 days.
  ! 320 of 503 activities carry no crop and 405 carry no issue. Any widget that groups by crop or issue silently describes a minority of the corpus. Every such panel must publish its own denominator or it will be read as covering everything.
  ! The botanical-to-common-name mapping (Vitis vinifera -> Grapevine) is safe, but the temptation to extend it to the umbrella words (colture, cereali, frutta, ortaggi — 96 activities) is not. 'cereali' is the advertiser's word for an unspecified set; turning it into Wheat would manufacture a fact.
  ! Latin binomials are both crop names and issue names in this corpus, and §11 forbids translating them or truncating at '('. A generic il()/cl() translation pass over the issue filter would corrupt 'Plasmopara viticola' into a Sintonia label.
  ! The 89 ORGANIC_VIDEO records are near-empty (no date, url, text, country, crop, issue). They are real and must be counted, but any list that sorts or filters by time will drop them without saying so. If they are quietly excluded, the company totals on two different panels will disagree (e.g. Syngenta 55 total but 30 in any dated view).
  ! REACHED_IN_ITALY is already correct in the model, but the countrySem string literally says AD_REACHED_COUNTRY != AD_TARGETED_COUNTRY. If the card shows only the flag and drops the caveat, 414 reach observations silently become 414 Italian targeting claims.
  ! IG.COMP_COMPANIES' case-duplicate rows (Bayer/BAYER, Corteva/CORTEVA, Syngenta/SYNGENTA) will double-count company totals if merged naively by exact string. Bayer would show 69 paid on one panel and 86 total on another.
  ! The ADAMA-portfolio side of this domain currently rests entirely on D.CASES[].productLinks, which italy-app-model.js reads at lines 102-105 while never reading ITALY_LABEL_VERDICTS. Until step 5 lands, any 'portfolio' wording anywhere in Competitor Watch is demo-backed. This is a cross-domain dependency and the serial engineer should not let a competitor-side edit ship a portfolio claim ahead of i
  ! The matrix and issue-table markup hard-code their column counts in grid-template-columns strings that appear twice each (header row and data row). Changing one and not the other misaligns the table without any error.

-- OPEN QUESTIONS --
  ? Can upstream supply a synonym table mapping observed advertiser issue terms to canonical ISSUE_NAMEs (Plasmopara viticola -> Downy Mildew, Erysiphe necator -> Powdery Mildew, Zymoseptoria tritici -> Septoria Leaf Blotch, Ceratitis capitata -> Mediterranean Fruit Fly)? Without it the COMPETITOR x ISSUE table can show no
  ? Should Citrus, Sunflower and Peach appear in the crop density and matrix even though they have no canonical crop window? They are real observed communication (15, 9 and 2 items) but they cannot cross-link to anything. My recommendation is yes, with no window column.
  ? What is the actual collection horizon of the Meta Ad Library pull? The corpus spans 2019-12-05 to 2026-08-26 but per-company first dates differ wildly (BASF 2023-03, FMC 2025-10). If the horizon is per-page rather than global, even the per-company 'first observed' date is not comparable across companies and should be s
  ? The reference date is 2026-09-02 and the latest observation is 2026-08-26. Is the corpus a one-off load or will it refresh? If it will not refresh, every 'last 30 days' widget on this screen will decay into an empty box and should be reframed as a fixed observation period with explicit start and end dates.
  ? Do the 89 ORGANIC_VIDEO records have any recoverable metadata upstream (title, publish date, url, view count)? IG.CHANNELS carries EXAMPLE_TITLE / EXAMPLE_URL / VIEWS / EXAMPLE_PUBLISHED_AT for 30 channels — if those channels are the same YouTube sources, upstream may be able to attach dates and titles, which would res
  ? Is the CREATIVE GALLERY worth keeping at all with no captured assets, or should the tab be removed so the remaining views (feed, by issue) are not diluted? This is a product decision, not an engineering one.

====================================================================================================
DOMAIN: Opportunity Radar + Opportunity Detail (D.CASES, D.REGION_STATS, decorate(), Data-State panel)
CONFIDENCE: high  |  realSource: APP.collections.upstreamOpportunities — 3 records from ITALY_INGEST.OPPORTUNITIES (PROVENANCE REAL_DERIVED), enriched by APP.collections.windows (29 ITALY_CANONICAL windows, provenance CANONICAL/EXPECTED_NORM) for dates and status, by window.ITALY_LABEL_VERDICTS.verdict() for portfolio strength (currently NOT read by italy-app-model.js at all), and by AM.productByKey for product identity. Per-region numbers can only come from APP.collections.windows (9 regions, 29 windows). The 29 D.CASES become an explicit default-OFF demo collection (APP.collections.demoOpportunityScenarios, provenance DEMO_
SUMMARY: Today the Opportunity Radar is 100% legacy fixture. `const CASES = D.CASES.map(c => this.decorate(c))` (portale.html:2515) turns 29 hand-authored demo cases into the default feed: 8 KPI tiles, the card grid, the 20-tile region map with per-region "cases / +N signals", and the full Opportunity Detail (hero, window bar, portfolio block, KNOW/WATCH, evidence bars, mini-map, competitor rows, action map, executive timeline, sources). Facts on that screen come from three different places and only one is real. The audited part is the CANONICAL layer: each demo case looks up its canonical window by LEGACY_CASE_ID and takes START_DATE, END_DATE, DATE_STATE, DATE_CONFIDENCE, CURRENT_STATUS, STATUS_REASON, CROP_STAGE_CLASS, ISSUE_STAGE_CLASS from it (italy-demo-data.js:243-273) — 29 windows, all PROVENANCE=EXPECTED_NORM, all SOURCE_IDS=[], all PRODUCT_MATCHES=[]. The portfolio strength classes are also real: italy-demo-data.js:280-311 calls ITALY_LABEL_VERDICTS.verdict(crop, issue, product) (12 VERIFIED, 7 NOT_FOUND, default LABEL_CHECK_NEEDED). Everything else on the card and the detail page — issue title, latin name, crop, region, `sub`, `cat`/category colour, products list, `primary`, happening, why, know[], watch[], timeline[], evidence{field,science,official,people,market}, evidenceTotal, evidenceLabel, origin, updated, adjacent[], source, competitors[], actions[], departments[] — is

-- MODEL CHANGES --
  * opportunities (REDEFINED — becomes the real feed) :: The default radar feed must be the 3 upstream records, fully normalized so the view is dumb. Every field below is measured present in all 3 records unless a count is given.
      fields: id  <- o.ID  (3/3: IT-OPP-001..003), legacyCaseId <- o.LEGACY_CASE_ID (3/3: IT-HERO-001..003; matches nothing in the app today — do not join on it), title <- o.TITLE (3/3), crop <- o.CROP (3/3, free text, Portuguese), cropKeys <- explicit synonym map from CROP to the SCIENCE/VOICES vocabulary (VINE|MAIZE|DURUM_WHEAT) and the canonical English (Grapevine|Maize); opp 003 CROP is 'Portfolio ADAMA Ita
      derivation: 
  * demoOpportunityScenarios (NEW) :: The 29 D.CASES survive only here: an explicit, default-OFF mode. state.showScenarios already exists at portale.html:2384 and defaults to false. This collection must never be summed into a real count, and italy-app-model.
      fields: everything currently on a D.CASES record, untouched, provenance forced to DEMO_SCENARIO, isDemoScenario: true, canonicalWindow: the ITALY_CANONICAL window matched by LEGACY_CASE_ID — keep it, because the window itself IS audited; just never let it upgrade the case's provenance
      derivation: 
  * products (CHANGED — links now come from the label audit) :: italy-app-model.js currently does not read window.ITALY_LABEL_VERDICTS at all; the strength classes reach it laundered through D.CASES[i].productLinks. Reading LV directly removes the last data-bearing D.CASES dependency
      fields: links[].strength <- LV.VERIFIED (12) / LV.NOT_FOUND (7) / default LABEL_CHECK_NEEDED, links[].source <- 'LABEL_AUDIT' | 'UPSTREAM_OPPORTUNITY', links[].caseId <- null for audit-derived links (the audit is keyed crop|issue|product, it has no case), labelAuditDate <- LV.AUDIT_DATE ('2026-09-02'), labelAuditScopeNote <- LV.SCOPE_NOTE, absenceRule <- LV.ABSENCE_RULE — the §10 sentence must be availabl
      derivation: 
  * ui.regionGrid (NEW, VISUAL_ONLY) :: Separates the allowed layout (§4) from the forbidden numbers. Today both live in one D.REGION_STATS row and the engineer cannot keep one without the other.
      fields: name (20 canonical Italian region names), short (VD, TAA, FVG, ...), gc <- col + 1, gr <- row + 1, NOTHING ELSE. cases, signals, color, border, textColor and active are all deleted from the fixture and recomputed from real counts at render time.
      derivation: 
  * windowsByRegion (NEW, REAL_DERIVED) :: The only defensible per-region number in the product. Measured from AM.collections.windows and it exactly reproduces the D.REGION_STATS `cases` column, which proves the demo number was a copy of the window count all alon
      fields: region -> count. Measured: Veneto 6, Puglia 6, Emilia-Romagna 4, Lombardia 3, Piemonte 3, Sicilia 3, Toscana 2, Friuli-Venezia Giulia 1, Trentino-Alto Adige 1, total 29 over 9 of 20 regions., Label it 'finestre canoniche' / 'canonical windows', NOT 'cases' and NOT 'opportunities'.
      derivation: 

-- FIELDS THAT DIE (18) --
  [REMOVE_SUBCOMPONENT] D.REGION_STATS[].signals (the '+N signals' number on the region ranking) @ portale.html:2565 regionRank sort; markup line 265 '+{{ r.signals }} signals' :: Measured: the 20 values sum to 56, which is exactly D.SIGNALS, the DEMO_SCENARIO future-scenario feed. The real futureSignals collection has 3 records and their REGION values measure as 'NAO SEI' (x2) and 'UE' (x1) — the
  [REMOVE_SUBCOMPONENT] case.evidence {field, science, official, people, market} and the derived evidenceTotal / evidenceLabel ('Strong'/'Good'/'Building') @ portale.html:2618 evChips, 2619 evBars, 2546 the 'relevant' sorter; markup lines :: Hand-typed on each of the 29 cases in italy-demo-data.js (e.g. {field:4,science:6,official:4,people:3,market:2}) and then incremented by the REAL_OBS literal at line 237. It is invented source convergence — §2's 'source 
  [REMOVE_SUBCOMPONENT] case.competitors[] {company, items, topic, days, type} @ portale.html:2632; markup lines 443-451 :: italy-demo-data.js:317 builds it as `[0,1,2].map(k => { const co = COMPANIES[(i + k*3) % COMPANIES.length]; const items = 1 + Math.floor(seed(i*7+k)*4); days: 1 + Math.floor(seed(i*11+k)*12); type: pick([...]) })`. A see
  [REMOVE_SUBCOMPONENT] case.actions[] and case.departments[] (the Action map and every GENERATE BRIEF button) @ portale.html:2617 deptChips, 2634 actions, 2544 the fDept filter, 3085-3092 the  :: Generated in italy-demo-data.js:313-314 from a static ACTIONS[c.cat] table with {crop}/{product} substitution. It tells six internal ADAMA departments what to do and when — private workflow, not external intelligence (§1
  [REMOVE_SUBCOMPONENT] case.timeline[] / case.tl (Executive timeline) @ portale.html decorate consumes c.tl; markup lines 470-476 :: italy-demo-data.js:229-231 builds it as fmt(addDays(TODAY, -origin)), fmt(addDays(b, 8)), fmt(addDays(b, origin-6)), fmt(addDays(TODAY, -updated)) with the labels 'Entered Future Radar', 'Regional bulletin', ... Dates AN
  [REMOVE_SUBCOMPONENT] case.origin and the 'Originated from Future Radar · N days ago' tile @ portale.html:2634 originLabel; markup line 315 :: A fixture integer (0 to 120) claiming provenance history the system does not have. Nothing in ITALY_INGEST records when a candidate entered any radar.
  [DERIVABLE_FROM_REAL] case.updated / updatedLabel ('1d ago') @ portale.html decorate; markup lines 244 and 305; the 'newest' sorter at 2546 :: The demo integer dies, but WHAT_IS_HAPPENING.FRESHNESS_DAYS is present 3/3 (17, 18, 6) and is a real, source-published freshness. Relabel: it is the age of the underlying document, not the age of an internal edit.
  [SHOW_UNKNOWN] case.latin (the scientific name under the hero title) @ portale.html:2396 parseField scoring, 3377 search matching; markup line 307 :: No real equivalent field. The Latin names do exist inside the real ISSUE strings ('via o vetor Scaphoideus titanus', 'Piralide (Ostrinia nubilalis) e Diabrotica virgifera virgifera') but parsing them out would mean the p
  [REMOVE_SUBCOMPONENT] case.adjacent[] and adjacentLabel ('Validate next') @ portale.html:2625 miniMap adjacency tint, 2626 adjacentLabel; markup line 434 :: A hand-authored 2-4 region list per case that is simultaneously a region claim and a recommendation. §2 forbids both from a fixture. The real REGION string already names its own secondary areas in its own words ('Veneto 
  [REMOVE_SUBCOMPONENT] case.source (the attribution line under the evidence bars) @ portale.html markup line 423; also read at 2860 as a Crop-Windows source :: A free-text demo string ('Regional phytosanitary bulletin · compulsory control decree'). SOURCE_IDS is the real, resolvable replacement, but note it is identical across all 3 records so it cannot differentiate them.
  [SHOW_UNKNOWN] case.stage / case.signal ('BBCH 67-73', 'Adult captures above threshold') @ portale.html markup lines 355-356 :: The demo authored these, but italy-demo-data.js:270-271 already overrides them from the canonical window and measured all 29 canonical windows have CROP_STAGE = null and ISSUE_STAGE = null, so both render 'NOT_OBSERVED' 
  [SHOW_UNKNOWN] case.label ('Apply at flowering (BBCH 61-65)', 'Vector control per regional decree calendar') @ portale.html markup lines 357 and 378 :: A label-timing instruction authored in the fixture. Measured: all 29 canonical windows have LABEL_TRIGGER = null and LABEL_SOURCE = null. Rendering a spray timing that no label record supports is the highest-consequence 
  [KEEP_AS_LABELLED_DEMO] case.cat / case.category (pest | disease | weed, and every colour, icon and CAT LABEL derived from it) @ portale.html:2426 (decorate's first line, throws without it), 2687, 2818, and ~1 :: The colour/icon half is legitimate presentation metadata under §4 and must move to record.ui. The classification itself is a fact claim, and the real field ISSUE_TYPE does not map onto it: measured values are 'FITOPLASMA
  [DERIVABLE_FROM_REAL] case.hero, case.primary as a fixture choice, case.products[] @ portale.html:2544 fProduct filter, 2621 primaryGo, decorate primaryLabel/moreLab :: ADAMA_PRODUCTS exists but only on 1 of 3 records (6 names, all resolving in AM.productByKey). Opp 002's CURRENT_EVIDENCE says in prose 'seis produtos ADAMA nomeiam Piralide e/ou Diabrotica no rotulo' while its ADAMA_PROD
  [DERIVABLE_FROM_REAL] case.realObs (the green 'Real observation' quote) @ portale.html:2637; markup line 346 :: Currently a 9-entry literal inside italy-demo-data.js:235 stamped provenance:'REAL_FACT' — a demo file asserting a fact class about itself, and it also mutates c.evidence.official. The real WHAT_IS_HAPPENING object is st
  [DERIVABLE_FROM_REAL] D.KPI.total = 29 (the headline radar number and the nav badge) @ portale.html:2559 kpiDef[0], 2607 navDef radar count :: Becomes 3. This is the single most visible consequence of the migration and the client will ask about it. The honest framing: 29 was never a count of anything real — it was the length of a fixture array.
  [DERIVABLE_FROM_REAL] D.KPI.matches = 13 and D.KPI.links = 80 @ portale.html:2564 kpiDef[5] and [6] :: Both come from the demo productLinks. The audited numbers are LV.verifiedCount = 12 and LV.notFoundCount = 7. 80 has no real counterpart until every candidate relationship is enumerated through the audit.
  [DERIVABLE_FROM_REAL] the Data-State panel's six hard-coded rows @ portale.html:2526-2534; markup 122-134 :: The honesty instrument is itself inaccurate and incomplete. It reads D.ACTIVITIES (72 demo) for the Competitor row while 503 real records exist, reads D.ARCHIVE while the real archive index has 740, and has NO ROW for op

-- PLAN --
  1. STEP 0 — freeze a baseline. Run the node recipe and record AM.counts and AM.provenanceSummary to a scratch file. Every later step is checked against it. Current baseline: opportunities 29 (3 flagged upstream-real, one of them wrongly), windows 29, products 166, searchIndex 309.
  2. STEP 1 — italy-app-model.js: kill the id-collision. Delete the upstreamOppIds map (lines 252-253) and the `upstream` lookup at 256. Nothing may join IG.OPPORTUNITIES.ID to D.CASES.id; they are different id spaces that collide on three values, and the collision currently mislabels the Fusarium demo case as real.
  3. STEP 2 — italy-app-model.js: add mapUpstreamOpportunity(o) producing the normalized opportunity record listed in modelChanges. Include the region parser (REGION string -> regionKeys against the 20 canonical names; measured results ['Veneto','Lombardia'] / ['Friuli-Venezia Giulia'] / []) and the crop/issue synonym table (Videira->Grapevine/VINE, Milho grao->Maize/MAIZE, and NO mapping for the transversal portfolio record). Keep the table as one explicit literal with a comment naming every source vocabulary, so the next data load can extend it in one place.
  4. STEP 3 — italy-app-model.js: rename the D.CASES collection to demoOpportunityScenarios (provenance DEMO_SCENARIO) and define `opportunities` as the mapped IG.OPPORTUNITIES. Both go into `collections`, so provenanceSummary and counts expose 3 real and 29 demo separately. Verify AM.counts.opportunities === 3.
  5. STEP 4 — italy-app-model.js: read window.ITALY_LABEL_VERDICTS directly. Replace the D.CASES productLinks loop at line 102 with the LV.VERIFIED / LV.NOT_FOUND loops, plus a LABEL_CHECK_NEEDED link for every name in ADAMA_PRODUCTS not covered by the audit. Expose LV.AUDIT_DATE, LV.SCOPE_NOTE and LV.ABSENCE_RULE on the model. Verify: AM.strengthFor('EVURE PRO','Grapevine','Flavescenza Dorata') === 'VERIFIED_LABEL_MATCH'.
  6. STEP 5 — italy-app-model.js: fix the search index at line 300 (index the real feed, drop c.latin) and add APP.ui.regionGrid (name/short/gc/gr only) plus windowsByRegion derived from collections.windows. Verify windowsByRegion reproduces {Veneto:6, Puglia:6, Emilia-Romagna:4, Lombardia:3, Piemonte:3, Sicilia:3, Toscana:2, 'Friuli-Venezia Giulia':1, 'Trentino-Alto Adige':1}.
  7. STEP 6 — portale.html: add decorateOpportunity(o) next to decorate() at line 2425. It must return a NEW object (decorate's Object.assign(c, ...) mutates the shared model record — do not repeat that on real data) and must not touch category, st, progress, fieldCount, productLinks, primaryAi or cat. In the same edit, make decorate()'s first line defensive: `const cat = c.category || D.CAT.disease;`.
  8. STEP 7 — portale.html:2515: swap the root. CASES from APP.collections.opportunities; DEMO_CASES from demoOpportunityScenarios, gated on the existing state.showScenarios (already defaults to false at line 2384). Add a labelled SCENARI DIMOSTRATIVI toggle in the radar toolbar near line 191.
  9. STEP 8 — portale.html:2544-2547: rewrite filtering. Drop the fDept clause and the department filter control; fProduct reads adamaProducts; fRegion reads regionKeys. Remove the 12-card slice. At 2546 delete the 'relevant' and 'newest' sorters and the matching <option> tags at markup line 189.
  10. STEP 9 — portale.html:2557-2563: re-source the 8 KPI tiles per the occurrences entry. Preserve all eight tiles and their layout (§12); change only values and sub-labels. Every sub-label must name its denominator (opportunities vs canonical windows vs label-audit verdicts).
  11. STEP 10 — portale.html:2564-2565: split layout from facts. regionTiles = APP.ui.regionGrid + windowsByRegion; regionRank sorts on the window count. Delete r.signals here and delete the '+{{ r.signals }} signals' span at markup line 265.
  12. STEP 11 — portale.html:2615-2639: rebuild the cs object against the real record. Remove deptChips, evChips, evBars, competitors, actions, adjacentLabel, relatedThemes, fieldMessages/fieldCount/noField. Re-point sourceRows at sourceIds. Replace realObs with WHAT_IS_HAPPENING. Add caseLabel, forbiddenLabel, currentEvidence, whatWeDoNotKnow, interpretations, scienceContext, windowApplication/Monitoring/NextCycle. Guard the CASES[1] fallback at 2615.
  13. STEP 12 — portale.html:2938-2939: make winFor/goWindow null-safe (it throws today for any non-demo case id) and gate the ORIGIN tile on hasWindow.
  14. STEP 13 — portale.html markup 299-488: apply the deletions and re-bindings in markupImpact, one panel at a time, checking the rendered page after each. Preserve every surviving panel's geometry, radius, spacing and ADAMA colours (§12). Where a panel loses its content entirely, remove the whole card rather than leaving an empty shell.
  15. STEP 14 — portale.html:2526-2534: rebuild dataState from AM.provenanceSummary and make sure opportunities (3 real) and demoOpportunityScenarios (29 demo) both appear. This is the acceptance instrument for the whole migration.
  16. STEP 15 — sweep the remaining D.CASES reads. Fix 2580 and 3169 (portfolio-claim strings) via the label audit; fix 2665 (drop kase, bind the calendar row to the canonical window, and delete dw0.kase.source at 2860 plus the two brief callbacks at 2861/2863); fix 2946 (mProducts from IG.PRODUCTS.crops); re-point 3130/3183/3369/3377 per their entries. Leave 2687 and 2818 alone — measured VISUAL_ONLY.
  17. STEP 16 — i18n. Add it/en strings for every new label: caseLabel, forbiddenLabel, whatWeDoNotKnow, freshnessDays, windowApplication states (CLOSED_FOR_2026, OPEN_BUT_NARROW, NOT_APPLICABLE, ANCHORED_BY_SOURCE, TO_BE_CONFIRMED), NON OSSERVABILE DA FONTI ESTERNE, and LV.ABSENCE_RULE. Also replace the five hard-coded English strings still sitting in the markup at lines 391, 397, 425, 433, 443 and 454.
  18. STEP 17 — acceptance. Confirm: (a) grep -n 'D\\.CASES' portale.html returns only lines 2396, 2687, 2818 (demo-mode and visual-only); (b) grep -n 'D\\.CASES' italy-app-model.js returns nothing; (c) grep -n 'REGION_STATS' returns only the ui.regionGrid construction; (d) AM.counts.opportunities === 3 and AM.counts.demoOpportunityScenarios === 29; (e) with showScenarios false, no string from italy-demo-data.js reaches the radar or the detail page; (f) opening every one of the 3 real opportunities and one bad deep link throws no console error.

-- RISKS --
  ! The screen loses roughly 70% of its content. Measured honestly: the radar goes from 29 cards to 3, and the detail page loses the evidence bars, the competitor panel, the action map, the executive timeline, the field inbox, the adjacency map shading, the latin name, the origin tile and the label-timing line. What remains — title, what is happening with a named document and date, why it matters, wha
  ! The three real records are written in PORTUGUESE, and §11 makes Italian the default language. TITLE, CROP, ISSUE, CURRENT_EVIDENCE, WHAT_WE_KNOW, WHAT_WE_DO_NOT_KNOW and INTERPRETATIONS are all pt-BR ('Videira', 'Milho grao', 'a obrigacao de tratar o vetor e norma, nao recomendacao'). The regulatory quotes inside them are Italian. This is not a translation the engineer may perform — translating a 
  ! ISSUE_TYPE on opp 003 is the literal string 'NAO SEI' ('I do not know'). The same literal appears across the real data: VOICES.REGION is 'NAO SEI' 17/17, NEWS.REGION 8/8, FUTURE_SIGNALS.REGION 2/3. Any code path that renders a raw field will print 'NAO SEI' to an Italian user. Every new binding needs an explicit guard mapping it to t.notKnown.
  ! Four incompatible crop vocabularies coexist in the real data and the synonym table is load-bearing. Measured: OPPORTUNITIES.CROP is Portuguese; CANONICAL.CROP_NAME and D.CASES.crop are English; COMP_ACTIVITIES.crops is Latin/Italian ('Vitis vinifera' 42, 'Zea mays' 67, 'colture' 59, 'cereali' 24); VOICES.CROP and SCIENCE.CROP are SCREAMING_SNAKE ('VINE' 45, 'MAIZE' 8, 'DURUM_WHEAT' 40). If the tab
  ! There is a FIFTH id space for cases. VOICES.CASE_ID measures as IT-VINE-FLAVESCENCE (8 voices), IT-MAIZE-WEED (8) and IT-DURUM_WHEAT-FUSARIUM (1). Only the first corresponds to a real opportunity. So if the detail page adds a field-voices panel, opp 001 gets 8 voices and opp 002 and 003 get zero — and the 8 MAIZE voices are about WEEDS, not the piralide that opp 002 is about. Do not join maize to 
  ! All 29 canonical windows measure PROVENANCE = 'EXPECTED_NORM', SOURCE_IDS = [] and PRODUCT_MATCHES = []. They are audited agronomic norms, not observations, and they carry no traceable source. Treating them as the radar's real feed instead of the 3 opportunities would produce a denser and more comfortable screen that still cannot answer 'who published this'. That temptation will present itself aro
  ! MARKET_CONTEXT, COMPETITOR_CONTEXT and FIELD_VOICES are present on 3/3 records but their values are folder paths ('ver 01-DESIGN-READY/MARKET-PULSE/'). They look like fields and are not data. Any engineer scanning the schema will try to render them. They must be dropped in the mapper, not passed through.
  ! WHAT_IS_HAPPENING.SOURCE_ID is present 3/3 and resolvable 0/3 ('IT-T3-002', 'IT-T3-006 (ERSA FVG)', 'IT-T4-001' against an IG.SOURCES id set that only contains IT-SRC-* ids). Rendering it as a clickable source would produce dead links on every real card. Text only.
  ! CURRENT_EVIDENCE and WHAT_WE_KNOW are byte-identical on all 3 records. Rendering both fills two panels with the same list and manufactures an appearance of corroboration. Render one.
  ! decorate() at portale.html:2425 does `Object.assign(c, ...)` — it mutates its input. Because it is called per render (see the comment at 2516) it currently writes presentation labels back into the demo records. Reusing that pattern on AM.collections.opportunities.records would corrupt the shared model across views. decorateOpportunity must return a new object; this is easy to miss when copying the
  ! Cross-domain edits. Lines 2580, 2665, 2860-2863, 2946 and 3169 sit in the Competitor, Crop Windows and Market domains and are owned by other specs. Their D.CASES reads must be removed for this domain to be clean, so the serial engineer has to sequence these with the other domain owners or the same lines will be edited twice.
  ! state.showScenarios already exists and is already false by default, but grep it before wiring: if another screen already uses it for a different purpose, the new radar toggle would silently change that screen too.

-- OPEN QUESTIONS --
  ? Will upstream reissue the 3 OPPORTUNITIES records in Italian, or does the portal ship with Portuguese body text on its three real cards? This blocks nothing technically but it is the single most visible quality issue after the count drop, and it is not the engineer's call to translate.
  ? All three records carry the identical SOURCE_IDS triple [IT-SRC-MINISTERO, IT-SRC-PIEMONTE, IT-SRC-OPENALEX], so the 'sources behind this case' panel shows the same 3 rows on every case. Is that the real provenance, or is it a placeholder that upstream intends to make per-record? If placeholder, the panel should show t
  ? Opp 003 ('Portfolio ADAMA Italia — authorisation expiry calendar') is not a crop x issue case at all: no crop, no region beyond 'Italia (nacional)', no agronomic window, ISSUE_TYPE 'NAO SEI'. Should it appear on the crop-oriented Opportunity Radar, or does it belong on the Portfolio screen? Rendering it as a radar card
  ? Opp 002's CURRENT_EVIDENCE states in prose that six ADAMA products name Piralide/Diabrotica on the maize label, while its ADAMA_PRODUCTS array is empty. Is the array simply unfilled upstream? Confirm before shipping a card that says LABEL_CHECK_NEEDED next to a sentence claiming six labelled products.
  ? Should the radar keep the word 'opportunity' at all? FORBIDDEN_LABEL is present 3/3 and reads 'nao chamar de "oportunidade Italia" nem de "oportunidade comercial"', and CASE_LABEL says 'CONVERGENCIA QUE MERECE INVESTIGACAO'. That affects the nav item, the i18n keys, the KPI tile and the page title — a naming decision, 
  ? Is a canonical window allowed to be bound to a real opportunity by content match (crop + region + issue), given that LEGACY_CASE_ID does not connect the two id spaces? A content match is a judgement, and §7 says presentation may not invent an agronomic state. Recommendation: bind only opp 001 -> IT-WIN-0001 if a human 
  ? Does the '29 canonical windows' number get to keep the word 'cases' anywhere in the UI, or must every occurrence be relabelled? The region map, the KPI status tiles and the windows nav badge all currently count windows while saying or implying cases.

====================================================================================================
DOMAIN: CROP WINDOWS + CROP CALENDAR + PREPARATION CLOCK
CONFIDENCE: high  |  realSource: Primary: APP.collections.windows (29 records, provenance CANONICAL, from window.ITALY_CANONICAL.windows). Secondary, currently NOT exposed by the model and must be added: window.ITALY_INGEST.CROP_WINDOWS (7 records — 5 grapevine Flavescenza rows with REGULATORY_ACT / REGULATORY_WINDOW / MONITORING_WINDOW / APPLICATION_WINDOW_2026 / PREPARATION_WINDOW / COVERAGE_STATE, plus 2 Modena field-observation rows that are the only OBSERVED_STAGE values in the whole pack) and window.ITALY_LABEL_VERDICTS (12 VERIFIED tuples, the only real product-to-window relationship). IG.CROPS[17] has NO region and NO
SUMMARY: Two screens live here. (A) view 'windows' (markup 535-751) is NOT a window list at all — it is the CROP CALENDAR: a rolling month timeline whose rows, dates, phenology bands, hectares, per-region date offsets and observed-stage dots ALL come from the demo fixtures D.CROP_CAL (8 crops, 40 crop x region rows) and D.OBSERVED (10 rows). The canonical contract is never consulted on this screen. (B) view 'window' (markup 752-810) is the window detail, fed by D.WINDOWS[29], a fixture that wraps each canonical window in ~20 decorated fields (prep, bucket, early, why, readiness, ladder, scale, kind) that have no upstream equivalent. Measured reality: CANONICAL supplies 29 windows with crop/issue/region/windowType/status/statusReason/dateState/dateConfidence 29/29, START_DATE and END_DATE 24/29, and NOTHING else — CROP_STAGE 0/29, ISSUE_STAGE 0/29, GENERATION_OR_STAGE 0/29, LABEL_TRIGGER 0/29, REGULATORY_TIMING 0/29, PRODUCT_MATCHES empty 29/29, SOURCE_IDS empty 29/29. IG.CROP_WINDOWS[7] carries the only real regulatory acts (5 grapevine Flavescenza rows) and the only two real observed stages (both Modena province). ITALY_LABEL_VERDICTS supplies 12 VERIFIED crop x issue x product tuples covering 11 of 29 windows. Everything else on both screens is fixture.

-- MODEL CHANGES --
  * collections.windows (extend existing 29 records) :: The view must stop deriving status, dates and product links itself. Every field here is a direct copy or a rule-7-legal date arithmetic, so the calendar and the detail screen become dumb renderers.
      fields: canonicalStatus — alias of status; exists purely to kill the portale.html:3004 defect where w.canonicalStatus is undefined and every product-page window reads DATE TO CONFIRM. Derivation: w.CURRENT_STATUS, 29/29 present., open — status === 'WINDOW_OPEN'. Upstream decision only, never a date comparison. Measured 6/29 true., hasDates — !!(START_DATE && END_DATE). Measured 24/29 true; the 5 false row
      derivation: All from window.ITALY_CANONICAL.windows plus two joins: window.ITALY_LABEL_VERDICTS.VERIFIED (crop + issue) and window.ITALY_INGEST.CROP_WINDOWS (crop + region). No demo read except record.ui.
  * collections.windowCalendarRows (NEW projection, 29 records) :: The calendar screen currently builds its own 40-row universe from D.CROP_CAL. This projection makes the canonical set the row universe so the screen stops being a second source of timing truth. It is deliberately a thin 
      fields: windowId, crop, region, issue, issueType — copied., startDate, endDate, hasDates, status, statusReason, dateState, dateConfidence — copied. dateState is the honest replacement for the fixture's 'CONFIRMED WINDOW / SEASONAL WINDOW / EXPECTED WINDOW' kind label: measured EXPECTED_NORM 24, DATE_TO_CONFIRM 5., daysToStart, daysToEnd — already computed by daysFrom() against the single reference date 20
      derivation: One row per record in collections.windows. No fixture read except ui.icon / ui.color.
  * collections.cropWindowsUpstream (NEW, 7 records from IG.CROP_WINDOWS) :: Five real regional regulatory acts and the only two real field observations in the whole pack are currently invisible to the app because the model never reads IG.CROP_WINDOWS. Three of the five regulatory rows (Lombardia
      fields: id, crop (normalized: VITE -> Grapevine, OLIVO -> Olive), region, issue, expectedCycle, observedStage, fieldReportedStage — each passed through an unknownGuard() that returns null when the value starts with 'NAO SEI' or 'NÃO SEI'. Measured: expectedCycle real 5/7, observedStage real 2/7, fieldReportedStage real 2/7., regulatoryAct, regulatoryActState, regulatoryWindow, monitoringWindow, applicatio
      derivation: Direct normalization of window.ITALY_INGEST.CROP_WINDOWS. provenance REAL_SOURCE / REAL_FACT as carried by the record.
  * preparation (NEW top-level block, provenance SINTONIA_INTERPRETATION) :: D.PREP_LEAD (90) and the six DEPTS offsets are the BUSINESS PREPARATION CLOCK. They are the feature the screen is built around and they must survive — but a channel purchase lead time is not externally observable and tod
      fields: provenance: 'SINTONIA_INTERPRETATION', observable: false, basis: 'Regola di pianificazione Sintonia. Non è un fatto osservato e non deriva da alcuna fonte esterna.', leadDays: 90, ladder: [{days:90,dept:'MARKET DEVELOPMENT'},{days:60,dept:'MARKETING'},{days:45,dept:'SALES / RTV'},{days:30,dept:'SUPPLY'},{days:14,dept:'SALES / RTV'}] — the five rungs moved out of D.LADDER unchanged., departments: t
      derivation: Constants lifted verbatim from the fixture and from portale.html:2652, re-labelled. Nothing is computed from data; the only data input is the canonical startDate the offsets anchor to.

-- FIELDS THAT DIE (19) --
  [REMOVE_SUBCOMPONENT] row hectares (D.CROP_CAL[crop].regions[n][1], rendered as '~350k ha') and the HIGH/MEDIUM/LOW crop-scale label derived from it @ portale.html 2665 (rg[1] -> c.ha), 2745 (scale), 2744 (why 'regional scale (ISTA :: There is no area field in any real source. Measured: zero hectare-like keys in ITALY_INGEST; IG.CROPS[17] carries only PRODUCTS_MENTIONING_CROP, PRODUCTS_WITH_USE_ROW_READ and DISTANCE. The 40 fixture numbers are attribu
  [REMOVE_SUBCOMPONENT] per-region phenology offset (D.CROP_CAL[crop].regions[n][2], values -14 to +7 days) @ portale.html 2664-2670 (off), 2688-2699 (every inst()/spanFor call), 2708 (prepB :: Shifts every drawn bar per region. Measured effect on the 20 rows that do have a canonical window: Grapevine/Sicilia -14d, Durum Wheat/Sicilia -12d, Durum Wheat/Puglia and Grapevine/Puglia -10d. A canonical window has ON
  [REMOVE_SUBCOMPONENT] crop phenology stages (D.CROP_CAL[crop].stages — Sowing, Tillering, Stem elongation, Flowering, Grain fill, Harvest, etc.) @ portale.html 2689 (cycle lane), 2827-2830 (calSeason phases), 2856 (dw.expectedN :: The grey crop-cycle band is the visual spine of the timeline and it has no upstream source at all. CANONICAL CROP_STAGE is null 29/29 and CROP_STAGE_SOURCE is null 29/29. IG.CROP_WINDOWS.EXPECTED_CYCLE exists for only 5 
  [REMOVE_SUBCOMPONENT] weed-control windows (D.CROP_CAL[crop].weed) @ portale.html 2690 (weed lane), 2831 (calSeason), markup 656 lane 'weed', 617 leg :: Same as stages: an unsourced agronomic norm. Note the canonical set does contain four herbicide windows as first-class records (IT-WIN-0024 Maize grass weed complex, IT-WIN-0025 Durum Wheat resistant grass weeds, IT-WIN-
  [DERIVABLE_FROM_REAL] monitoring window (D.CROP_CAL[crop].monitor) @ portale.html 2691 (monB), 2726 (monBars2), markup 656 lane 'mon', 620 legend :: Survives, but for 2 of 29 rows only. IG.CROP_WINDOWS.MONITORING_WINDOW is real prose for the 5 grapevine Flavescenza regions, of which Veneto and Piemonte join a canonical window. The other 27 rows must show no monitorin
  [DERIVABLE_FROM_REAL] mandatory treatment window (D.CROP_CAL['Grapevine'].mandatory) and its 'DATE TO CONFIRM' future-year label @ portale.html 2692, 2727, markup 662-664, 621 legend :: This one gets STRONGER, not weaker. IG.CROP_WINDOWS carries named regional acts: DDR n. 13645 (Veneto, 2026-05-14), Determinazione Dirigenziale n. 280 (Piemonte, 2026-03-16), Comunicato Giunta n. 39 (Lombardia), Bolletti
  [SHOW_UNKNOWN] observed crop stage — the white dot, r.current, dw.current, dw.obsMeta, the calStrip 'observed' counter and the CURRENT/PARTIAL coverage classes (D.OBSERVED, 10 rows) @ portale.html 2666, 2717 (obsMark), 2721 (coverage), 2748-2749, 2855 (obsMeta); m :: This is the most valuable-looking thing on the screen and it is the least supported. The 10 fixture rows cite real-sounding sources (Consorzio Brunello, ERSA FVG, Regione Veneto) but they are not in the ingested pack, an
  [REMOVE_SUBCOMPONENT] window bucket and the bucket filter (D.WINDOWS[].bucket: OPEN NOW / NEXT 30 DAYS / 31-60 / 61-90 / 3-6 MONTHS / NEXT CYCLE) and the parallel calendar bState() ladder (WINDOW OPEN / ACT NOW / ACTIVATE / PREPARE / PLAN / NEXT CYCLE) @ portale.html 2650 (bState), 2673-2675 (calKpis), 2762 (r.state), 2820 (b.sub), 2 :: Both are presentation deciding an agronomic state, which rule 7 forbids outright. They are also arithmetically broken: D.WINDOWS sets days = Math.max(0, daysToOpen), so all 16 WINDOW_CLOSED and all 5 DATE_UNKNOWN windows
  [SHOW_UNKNOWN] window kind label (D.WINDOWS[].kind: CONFIRMED WINDOW / SEASONAL WINDOW · UNCONFIRMED / EXPECTED WINDOW) and D.WINDOWS[].confirmed @ portale.html 2649 (currentEvidence), 2669 (kind); markup 756, 805, 806 :: confirmed requires CROP_STAGE_CLASS or ISSUE_STAGE_CLASS to be OFFICIAL_OBSERVED_CURRENT or FIELD_REPORTED_CURRENT. Measured: both classes are NOT_OBSERVED for 29/29. So 'CONFIRMED WINDOW' can never appear and the confir
  [REMOVE_SUBCOMPONENT] calendar window occurrence dates (nextOcc / spanFor over D.CROP_CAL[crop].issueWin), including the roll-forward into 2027 @ portale.html 2655 (nextOcc), 2664 (occ), 2691, 2708, 2764-2765 (windowFrom/windo :: A second window source that contradicts the contract. Measured on the 20 matched rows: 5 draw a different ISSUE than the canonical window for that crop x region, and offsets move dates by up to 14 days. Worse, nextOcc() 
  [REMOVE_SUBCOMPONENT] early market signal (D.WINDOWS[].early {state,color,text}, compCount, fieldMessages) @ portale.html 2657 (early), 2741 (fms), 2769; markup 733 (dw.early), 768-771 (wd. :: Measured 9 of 29 windows read 'FIELD REPORTED', every one of them sourced from the 18 synthetic FIELD_MESSAGES. That is a field observation supplied by a fixture — the flattest violation of rule 2 in the domain. The comp
  [REMOVE_SUBCOMPONENT] WHY PREPARE NOW / WHY NOW reason lists (D.WINDOWS[].why, 6 rows, and the calendar's own why at portale.html 2742-2747) @ portale.html 2658-2665, 2742-2747; markup 735 (dw.why), 764 (wd.why) :: Every one of the twelve assertions is fixture-derived: 'High maize relevance in Lombardia' (hectares that do not exist), 'Current-year pressure signal present' (an observation class that is NOT_OBSERVED 29/29), 'N early 
  [REMOVE_SUBCOMPONENT] portfolio readiness matrix (D.WINDOWS[].readiness, 8 rows) @ portale.html 2666; markup 776 :: Rows include 'Italy authorization: CONFIRMED · demo pack', 'Label window: CONFIRMED', 'Current crop timing: CURRENT/EXPECTED' and 'Field signal: REPORTED/WAITING' — four fabricated regulatory and agronomic verdicts. Only
  [REMOVE_SUBCOMPONENT] case prose carried onto the window (D.WINDOWS[].calLabel from c.label, and c.know / c.source used by the drawer) @ portale.html 2669 (calLabel), 2859 (dw.know), 2861 (dw.sources); markup 757, 744 :: Sentences such as 'Vector treatments · mid-June -> late July' and the drawer's know/sources bullets come from D.CASES. CANONICAL SOURCE_IDS is empty 29/29, so a window genuinely has no source to cite; the drawer must say
  [SHOW_UNKNOWN] Future Radar signals attached to a window (D.WINDOWS[].signals from D.SIGNALS) @ portale.html 2656; markup 798-799 :: D.SIGNALS is the 56-record presentation-scenario set that the model already fences off as DEMO_SCENARIO (futureScenarios). Real upstream supply is 3 futureSignals for the entire country. After the switch this panel is em
  [KEEP_AS_LABELLED_DEMO] business preparation lead time (D.PREP_LEAD = 90) and the six department lead offsets (DEPTS at portale.html 2652, -210 to -10 days) and the five-rung advance-warning ladder (D.LADDER 90/60/45/30/14) @ portale.html 2646, 2652, 2701, 2708, 2762-2766 (prepFrom, leadDays, prepStarted, :: This is the BUSINESS PREPARATION CLOCK and it must NOT be deleted — it is the product's reason for existing next to the agronomic clock. But nothing external can observe when a channel actually buys, and today the screen
  [REMOVE_SUBCOMPONENT] the entire unrendered window-list layer: visibleWindows, windowCount, windowKpi, windowBuckets, windowCropChips, earlyWindows, top-level ladder, calKpiNote @ portale.html 2871-2875, 2887, 3403 (props object); markup: none :: Verified: none of these eight props has a single binding in the markup template range 51-2374. They compute nine D.WINDOWS reads, two D.WINDOW_KPI reads and one D.LADDER read on every render and display nothing. Deleting
  [REMOVE_SUBCOMPONENT] window record identity (id 'WIN-001' taken from the fixture instead of WINDOW_ID 'IT-WIN-0001') @ italy-app-model.js 73; every openWindow() call site: portale.html 2869, 2945, 30 :: Measured 29/29 records take the demo id. This is not cosmetic: the canonical contract's own identifier is discarded, so a window cannot be traced back to the audit that produced it, and any deep link the user saves point
  [REMOVE_SUBCOMPONENT] calendar rows that have no canonical window at all @ portale.html 2671 (ALL_CORES), 2686, 2812 :: Measured: 20 of the 40 crop x region rows on the screen do not correspond to any canonical window — Durum Wheat in Basilicata / Marche / Emilia-Romagna, all five Wheat rows, Grapevine in Toscana / Trentino / Lombardia, O

-- PLAN --
  1. STEP 0 — delete the dead layer first, before touching anything visible. In portale.html remove lines 2871-2875 (wins, wCrops, windowCropChips, WK, windowBuckets), 2887 (earlyWindows) and 2676 (calKpiNote), and remove the keys visibleWindows, windowCount, windowKpi, windowBuckets, windowCropChips, earlyWindows, ladder and calKpiNote from the props object at 3403. Confirm with a grep over lines 51-2374 that none of those eight names appears in the markup — it does not today. This alone removes 9 D.WINDOWS reads, both D.WINDOW_KPI reads and the D.LADDER read, with zero pixels changed.
  2. STEP 1 — italy-app-model.js: extend the windows mapper (lines 70-82). Add canonicalStatus, open, hasDates, windowLabel, sourceIds, and switch id to w.WINDOW_ID while keeping legacyId. Trim uiOf to icon+color only (order resolves for 0/29). Do not add any new demo read.
  3. STEP 2 — italy-app-model.js: read window.ITALY_LABEL_VERDICTS for the first time. Add a normalizeIssue() that folds ' / ' and ' · ' to one separator, then attach verifiedProducts and labelVerdictState to each window. Assert in a one-off node check that 11 of 29 windows resolve at least one verified product and that IT-WIN-0016 now matches MAVRIK EW; without the fold it matches nothing.
  4. STEP 3 — italy-app-model.js: add collections.cropWindowsUpstream from IG.CROP_WINDOWS[7], with an unknownGuard() that nulls any value starting with 'NAO SEI' or 'NÃO SEI'. Normalize CROP VITE->Grapevine and OLIVO->Olive. Tag languageState 'PT_ANALYST_SOURCE' on the free-text fields. Expect 5 records with a regulatory act, 2 with a real observed stage.
  5. STEP 4 — italy-app-model.js: join cropWindowsUpstream onto windows by normalized crop + exact region, producing window.regulatory and window.observedStage. Expect exactly 2 joins (IT-WIN-0001 Veneto, IT-WIN-0008 Piemonte) and 0 observedStage. Record those two numbers in the commit message so the next engineer can tell drift from a data change.
  6. STEP 5 — italy-app-model.js: add the preparation block (provenance SINTONIA_INTERPRETATION, observable false, leadDays 90, the five ladder rungs lifted from D.LADDER, the six departments with their offsets lifted from portale.html:2652 and their colours from D.DEPT). Nothing here is computed from data; this is a relocation plus a label.
  7. STEP 6 — italy-app-model.js: add collections.windowCalendarRows as a thin projection over collections.windows (29 rows), including coverageState derived only from what is present. Do NOT add area — there is no source for it.
  8. STEP 7 — portale.html, the calendar engine (2660-2810). Replace ALL_CORES/coreFor with a map over AM.collections.windowCalendarRows.records. Delete nextOcc, spanFor's use for issueWin, the off offset, bState, PREP_RULES-style bucketing and the cal.stages / cal.weed / cal.monitor lanes. Anchor the issue bar and the preparation bar on row.startDate/row.endDate. Keep clipB, posOf, the month header, the drag/scroll nav and the today line untouched — those are pure geometry and rule 7 explicitly allows them.
  9. STEP 8 — portale.html, the calendar chrome. Rebuild calKpis on the four canonical statuses, calCropBtns on the 10 canonical crops, calStrip on the new counts, calMoments on daysToStart/daysToEnd and wst(status). Delete calSeason's phase and weed cards; keep the PRE-SEASON card only if it sits inside the interpretation fence.
  10. STEP 9 — portale.html, the drawer (2853-2866) and its markup (716-750). Delete ha, scale, obsMeta, expectedNow and early. Rebuild why from statusReason + dateState + dateConfidence + labelVerdictState + coverageState. Point prods at verifiedProducts and use the rule-10 wording for the 18 windows with none. Re-anchor deptPlan on startDate and render '—' for the 5 rows without one. Replace dw.sources with regulatory.sourceId or an explicit 'nessuna fonte collegata'.
  11. STEP 10 — portale.html, window detail (2876-2886 and markup 752-810). Point wd0 at AM.collections.windows by windowId. Replace kind with dateState, delete CROP SCALE, drive the PORTFOLIO CLOCK from verifiedProducts, drive the BUSINESS PREPARATION CLOCK from AM.preparation inside the interpretation fence, rebuild why, cut readiness to the three NOT_EXTERNALLY_OBSERVABLE rows, point products at verifiedProducts via AM.findProduct, point signals at AM.collections.futureSignals, and delete the dead confirmed branches at 805-806.
  12. STEP 11 — the seven cross-screen reads: 2573 (radar winMatch), 2740 (calendar row win), 2868 (winFor), 2945 and 2949 (market), 3003 (product detail, and fix the always-DATE_UNKNOWN defect by reading w.status), 3107 (field demo, keep as demo but retarget), 3149 (future radar, stop printing 'open now' from a clamped number), 3169 and 3177 (competitors), 3197 (science winFor2). All ten become AM.collections.windows reads with openWindow(w.windowId).
  13. STEP 12 — italy-i18n.js: rewrite cwBucketNote, cwFootNote, subWindows and the legend keys cwLegCycle / cwLegWeed / cwLegObs for the lanes that no longer exist, in both it and en. Add the interpretation caption string. Do not translate product names, company names or Latin names (rule 11).
  14. STEP 13 — verification pass, all numbers measured, not assumed: nav badge 29; calendar rows 29 over 10 crops and 9 regions; status counts 6 open / 16 closed / 5 date-unknown / 2 next-cycle; rows with a drawn bar 24; rows with a verified ADAMA product 11; rows with a regulatory act 2; rows with an observed stage 0; occurrences of the string 'ACT NOW' produced by presentation code 0; occurrences of new Date() outside the AM.REF anchor 0; remaining D.WINDOWS / D.CROP_CAL / D.WINDOW_KPI / D.OBSERVED / D.PREP_LEAD / D.LADDER reads 0, remaining D.DEPT reads colour-and-name only.

-- RISKS --
  ! The screen gets visibly emptier and there is no way around it. The timeline loses the crop-cycle band, the weed band and the observed dot on every row; the region column loses its hectare line and its scale chip; the row count drops 40 -> 29; the season view loses its phase cards. What remains per row is: the canonical window bar, the preparation bar (labelled interpretation), a status chip, a dat
  ! Whole crops disappear from the crop rail. Wheat loses all five of its regions (no canonical Septoria window exists for Veneto, Piemonte, Lombardia, Toscana or Umbria) and Durum Wheat keeps 3 of 6. Anyone who has demoed the Wheat calendar will read this as a regression.
  ! IG.CROP_WINDOWS free text is in Portuguese, not Italian — 'aberta — inicio de agosto a fim de setembro', 'FECHADA — as janelas obrigatorias terminaram em junho'. Piping it straight into an Italian-default UI (rule 11) would be worse than showing nothing. Either fence it as a verbatim analyst note or hold it back until the next ingest translates it.
  ! The label-verdict join is separator-sensitive. IT-WIN-0016 'Cereal Aphids · BYDV Risk' versus the audit's 'Cereal Aphids / BYDV Risk' is the only case today, but the same class of mismatch will silently drop future matches. Normalize once in the model and never in a view.
  ! strengthFor() and productByKey links in italy-app-model.js still route through D.CASES.productLinks (line 102). This domain does not own that path, but the window screens consume its output through wd.c.productObjs. If the products domain migrates on a different schedule, the window detail will briefly read verified products from one source and the case-derived list from another. Coordinate step 1
  ! The upstream OPPORTUNITIES use LEGACY_CASE_ID 'IT-HERO-001..003' while canonical windows use LEGACY_CASE_ID 'IT-OPP-001..029'. There is no overlap, so the three real opportunities cannot currently be joined to a window by legacy id. Do not invent a join.
  ! Deleting the fixture id (WIN-001 -> IT-WIN-0001) breaks any saved deep link and any hardcoded id in briefs, notifications or tests. The legacyId field plus a redirect in openWindow() is the cheap mitigation; skipping it will look like a routing bug.
  ! D.WINDOW_KPI feeds the nav badge at line 2608 and today returns 29 by coincidence. If someone fixes the badge without fixing the badge's source, the number stays right for the wrong reason and the coupling survives the migration unnoticed.

-- OPEN QUESTIONS --
  ? Are the 10 D.OBSERVED rows (Consorzio Brunello harvest start, ERSA FVG BBCH 65-75, the Veneto and Umbria bulletin series) real observations from an earlier collection that simply never made it into ITALY_INGEST? If they are, the right fix is to promote them upstream into CROP_WINDOWS with dates and source ids, not to d
  ? Is the empty PRODUCT_MATCHES array on all 29 canonical windows intentional (the audit deliberately keeps product linkage in ITALY_LABEL_VERDICTS) or is it an unfilled field? If it is meant to be filled, the model should prefer it over the verdicts join and the join becomes a fallback.
  ? SOURCE_IDS is empty on all 29 windows. Should a window inherit the source ids of the regional regulatory act it joins (IT-SRC-REGIONAL, IT-SRC-PIEMONTE), or must a window's provenance stay strictly EXPECTED_NORM until the audit attaches its own sources?
  ? WINDOW_TYPE is 'APPLICATION' for 29/29. Are MONITORING and REGULATORY window types planned as separate canonical records? If so, the calendar's monitoring and regulatory lanes should be built as row types now rather than as joined attributes of an application window.
  ? D.PREP_LEAD is a single global 90 days for every crop, region and department. Is 90 a Sintonia planning convention that should be stated as such once, or a placeholder that the business owner intends to vary by crop? The interpretation label should name whichever it is.
  ? Should the three regulatory acts whose region has no canonical window (Lombardia, Trentino, Emilia-Romagna Flavescenza) appear on the calendar as regulatory-only rows with no application window, or only in a separate list? They are real, dated, primary-source-backed facts and hiding them because the window audit has no

====================================================================================================
DOMAIN: PORTFOLIO / PRODUCT INTELLIGENCE / PRODUCT RELATIONSHIPS
CONFIDENCE: high  |  realSource: APP.productRelationships (new canonical collection) fed, in precedence order, by: (1) ITALY_CANONICAL.windows[].PRODUCT_MATCHES — MEASURED EMPTY: all 29 windows have PRODUCT_MATCHES = [], so this tier contributes 0 rows today and exists only so the next HANDOFF load lights it up; (2) window.ITALY_LABEL_VERDICTS — 12 VERIFIED triples + 7 NOT_FOUND triples, audit date 2026-09-02, source "Claude Code reading of 163 official Italian product labels"; (3) IG.LINKS — 219 rows, all prov REAL_FACT / evidence REGULATORY_FACT, carrying crop enum + Latin target + product + reg + ai + moa + labelUrl, cover
SUMMARY: Two screens plus a cross-cutting relationship graph. PORTAFOGLIO (portale.html markup 1397-1424, logic 3026-3047) is already clean: it lists AM.products (166 = 163 real registrations + BUDGE/EXELGROW/PARLEAF which are catalog-only) in two deliberately separate tabs, CATALOGO COMMERCIALE (44 catalog items) and UNIVERSO REGOLATORIO (163 registrations), filtered by categoryLabel (ERBICIDI 91 / FUNGICIDI 46 / INSETTICIDI 23 / SPECIALI 6). PRODUCT INTELLIGENCE (markup 1339-1395, logic 2999-3023) shows one product's identity plus three relationship cards (verified / check-needed / not-confirmed) and a separate "VEDI OPPORTUNITÀ COLLEGATE" CTA. The identity facts are real (IG.PRODUCTS: name, reg, holder, ai, form, regCat, line, status, expiry, hrac/frac/irac, labelUrl — 163/163 populated). The RELATIONSHIPS are not: italy-app-model.js line 102 builds every product link by walking D.CASES[].productLinks, so which crop x issue x product triples exist at all is decided by a demo fixture (29 demo cases, 80 links). window.ITALY_LABEL_VERDICTS — the real 163-label audit — is loaded but italy-app-model.js never reads it; only italy-demo-data.js line 281 calls LV.verdict() to GRADE the fixture's own candidate list. The measured consequence: 57 distinct triples, of which 42 are LABEL_CHECK_NEEDED invented by the fixture; the visible 10 / 10 / 9 is the portfolioState of the 29 demo opportunity 

-- MODEL CHANGES --
  * productRelationships (NEW, in AM.collections and as AM.productRelationships) :: One canonical relationship truth so no view can invent, promote or hide a portfolio match. Replaces the D.CASES walk at italy-app-model.js:102. Every record answers three questions the screen must be able to show: which 
      fields: id, product, productKey, windowId, legacyCaseId, crop (canonical CROP_NAME), issue (canonical ISSUE_NAME), issueType (PEST|DISEASE|WEED), region (from the window, never from the label — a label is national), strength (VERIFIED_LABEL_MATCH | RELATED_PORTFOLIO | LABEL_CHECK_NEEDED | NO_CONFIRMED_MATCH_CURRENT_READING), strengthRank (0..3, from ITALY_LABEL_VERDICTS.STRENGTH[x].rank), evidenceKind (CA
      derivation: Iterate the 29 ITALY_CANONICAL.windows. TIER 1 CANONICAL: window.PRODUCT_MATCHES — MEASURED [] on all 29, so 0 rows today; keep the reader. TIER 2 LABEL_AUDIT: for each (crop, issue) match against ITALY_LABEL_VERDICTS.VERIFIED -> VERIFIED_LABEL_MATCH, and against .NOT_FOUND -> NO_CONFIRMED_MATCH_CURRENT_READING; needs an issue-alias table because the audit writes 'Cereal Aphids' and 'Cereal Aphids / BYDV Risk' where the window writes 'Cereal Aphids · BYDV Risk', and 'Diabrotica' where the window writes 'Diabrotica Adults'. Yields 14 verified rows and 15 not-confirmed rows across the 29 windows. TIER 3 REGULATORY_USE_ROW: IG.LINKS rows where LINK.crop is in CROP_KEY[window.CROP_NAME] and LINK
  * CROP_KEY (new lookup exported on AM, provenance REAL_DERIVED) :: The three real sources speak three crop vocabularies and nothing upstream joins them. Making the join a visible, named table stops each view from inventing its own string match.
      fields: windowCropName (e.g. 'Sugar Beet'), ingestCropEnums (e.g. ['SUGARBEET']), note
      derivation: Hand-authored from the measured value sets: CANON.CROP_NAME has Grapevine, Maize, Olive, Sugar Beet, Wheat, Durum Wheat, Apple, Tomato, Rice, Soybean; IG.LINKS.crop has SUGARBEET, WHEAT_GENERIC, SORGHUM, SOYBEAN, TOMATO, POTATO, TRITICALE, BARLEY, MAIZE, GRAPEVINE, COMMON_WHEAT, APPLE, OLIVE, RICE; IG.CROPS adds DURUM_WHEAT, SUNFLOWER, ALFALFA. One judgement call must be recorded in `note`: Wheat -> [COMMON_WHEAT, WHEAT_GENERIC] and Durum Wheat -> [DURUM_WHEAT, WHEAT_GENERIC]. MEASURED: DURUM_WHEAT has PRODUCTS_WITH_USE_ROW_READ = 0, so every Durum Wheat check-needed row would come from the WHEAT_GENERIC ('Frumento') label rows.
  * ISSUE_TARGET (new lookup exported on AM, provenance REAL_DERIVED, explicitly labelled as a Sintonia mapping) :: IG.LINKS targets are Latin species ('Cercospora beticola', 'Zymoseptoria tritici'); canonical windows name issues in English ('Cercospora Leaf Spot', 'Septoria Leaf Blotch'). Nothing upstream connects them. This is the O
      fields: windowIssueName, targetPatterns (array of Latin genus/species prefixes), author, note
      derivation: 22 distinct window issues. MEASURED: only 6 of them have any matching target in the 219 use rows — Diabrotica Adults/Larvae (Diabrotica virgifera, MAIZE), Cercospora Leaf Spot (Cercospora beticola, SUGARBEET), Septoria Leaf Blotch (Zymoseptoria tritici / Septoria tritici, COMMON_WHEAT + WHEAT_GENERIC), Fusarium Head Blight (Fusarium spp, COMMON_WHEAT), Grass weed complex (Digitaria/Echinochloa/Sorghum halepense, MAIZE), Herbicide resistance · rice (Leersia/Glyceria/Echinochloa, RICE). The other 16 issues — including Flavescenza Dorata (Scaphoideus titanus), European Corn Borer (Ostrinia), Olive Fruit Fly (Bactrocera oleae), Codling Moth (Cydia), Grapevine Moth (Lobesia), Downy Mildew (Plasmo
  * competitorProducts (NEW) :: To retire D.CPRODUCTS (14 invented competitor products) without deleting the 'cproduct' view, which is a real navigation destination from every competitor activity chip.
      fields: id, product, company, adsReachingIt, proof, activities (IG.COMP_ACTIVITIES rows whose products[] contains this product), activityCount
      derivation: IG.COMP_PRODUCTS[36] verbatim (ID, PRODUCT, COMPANY, ADS_REACHING_IT, PROOF). Activities joined from the 102 of 503 COMP_ACTIVITIES that carry a non-empty products array. adsReachingIt is a PAID-reach count and must keep the REACHED_IN_ITALY wording, never 'targeted Italy'.
  * products (EXISTING — corrections) :: The Portafoglio and Product Intelligence identity block is already real; three measured defects make it read less true than the data allows.
      fields: ai must be exposed as both aiList (array) and aiLabel (joined with ' + '), moaLabel derived from hrac/frac/irac (70/163 products gain a real value; today all 166 read NON OSSERVABILE), relationships (the product's slice of productRelationships) replacing the demo-built `links`, verifiedLinks / relatedLinks / checkNeededLinks / rejectedLinks recomputed from that slice, catalogOnly boolean (BUDGE, E
      derivation: All from IG.PRODUCTS and ITALY_CATALOG.ITEMS, which are already joined. Only the derivations change, not the source.

-- FIELDS THAT DIE (11) --
  [REMOVE_SUBCOMPONENT] Application timing / crop stage for a product use ('Post-emergence · crop 2–6 leaves') @ portale.html:2622 primaryUse · portale.html:2880 p.use · markup line 782 'Stage  :: MEASURED, and this is the hardest one: IG.LINKS.timing is non-null on 219/219 rows and every single one holds the identical sentence 'NAO SEI — a coluna de epoca do rotulo nao foi extraida' (I don't know — the label's ti
  [REMOVE_SUBCOMPONENT] targetFit ('this product fits this issue because its target string overlaps the issue name') @ portale.html:2880 targetFit · markup line 782 'Target fit · {{ p.targetFit }}' :: It is computed in the view by lowercasing a demo target and testing substring containment against the issue name, then falling back to p.targets[0] when nothing matches — so it prints a target that does NOT fit whenever 
  [SHOW_UNKNOWN] Product dose @ not currently rendered; IG.LINKS.doses was proposed as a new source :: Do not build a dose component. MEASURED: only 35 of 219 use rows have a non-empty doses array, and the values are water volumes ('500 l/ha', '80-400 l/ha') not product doses. maxApp is null on 219/219. interval is presen
  [DERIVABLE_FROM_REAL] region on a product relationship @ italy-app-model.js:102 (l.region from the case) · portale.html markup 1360, 1366 :: A label authorisation is national — the product has no regional relationship. Today the region shown is the demo case's region. Keep the line ONLY if it is relabelled as the region of the WINDOW the relationship is attac
  [REMOVE_SUBCOMPONENT] Competitor product crops, issues, first/last date, paid count, people count, linked cases @ portale.html:3185-3186 cp0.crops, cp0.issues, cp0.first, cp0.last, cp0.paid, cp0 :: IG.COMP_PRODUCTS carries FOUR fields only: PRODUCT, COMPANY, ADS_REACHING_IT, PROOF. There is no crop, no issue, no date, no case link. Crops/issues/dates can be re-derived ONLY from the 102 of 503 activities that name a
  [REMOVE_SUBCOMPONENT] 'VERIFIED PORTFOLIO MATCH' / 'VERIFIED PORTFOLIO RESPONSE' as a green state @ portale.html:3218 (science theme) · portale.html:2592 (competitor activity) · ma :: These two strings are hand-written in the view and triggered by `prods.length > 0` on a demo array. They are not one of the four audited strength classes and they claim verification that no audit performed. Delete both l
  [SHOW_UNKNOWN] 'No confirmed ADAMA position' / 'No registered portfolio match connected.' / 'Portfolio check needed — no confirmed ADAMA label position for this crop × issue.' @ portale.html:2745 · markup line 1233 · markup line 784 · portale.html:3218 'NO C :: These are the rule-10 violations. Today they fire whenever a demo array is empty. After the rebuild they must fire ONLY when the record's strength is NO_CONFIRMED_MATCH_CURRENT_READING, which comes exclusively from the 7
  [REMOVE_SUBCOMPONENT] The 33-product ADAMA universe @ portale.html:3418, 3426 (filter options) · portale.html:3379 (search) · italy-ap :: 133 of the 166 products the Portafoglio screen already displays are invisible to search and to both product filters. This is not a fake fact, it is a missing one — but it makes the portal contradict itself on the same se
  [REMOVE_SUBCOMPONENT] 31 of the 32 products that currently show a portfolio relationship @ consequence of italy-app-model.js:102 across the whole app :: The honest cost of the rebuild, stated plainly. MEASURED today: 32 products carry at least one relationship, 31 of them via LABEL_CHECK_NEEDED rows that exist only because a demo case named the product. After the rebuild
  [DERIVABLE_FROM_REAL] Mode of action on the Product Intelligence header @ portale.html:3014 pd.moa :: Opposite direction: the screen is currently MORE pessimistic than the data. e.regulatory.moa and .MODE_OF_ACTION_DECLARED do not exist as keys, so all 166 products print 'NON OSSERVABILE DA FONTI ESTERNE'. Building the l
  [KEEP_AS_LABELLED_DEMO] Commercial performance, sell-in, share, volume @ italy-app-model.js:121 commercialPerformance: P.NOT_OBSERVABLE · markup line 139 :: Already correct and must stay exactly as is. This is the model behaving the way the whole relationship layer should.

-- PLAN --
  1. 1. In italy-app-model.js, add the two lookup tables CROP_KEY and ISSUE_TARGET as named constants with an explicit comment saying they are Sintonia-authored joins, not upstream data, and that every row they produce carries mappingRule. Export them on the model so a future audit can diff them.
  2. 2. In italy-app-model.js, add buildProductRelationships() implementing the four tiers in order (CANON PRODUCT_MATCHES -> ITALY_LABEL_VERDICTS -> IG.LINKS use rows -> IG.PRODUCTS crop+line registration). Include the issue-alias table for 'Cereal Aphids' / 'Cereal Aphids / BYDV Risk' / 'Cereal Aphids · BYDV Risk' and 'Diabrotica' / 'Diabrotica Adults', or two verified rows are lost. Gate tier 4 by ISSUE_TYPE->line or it produces 1261 rows instead of 321.
  3. 3. Assert the tier-1 reader works even though it returns nothing today, and log the count so the next data load makes it visible: canonicalRelationshipRows = 0 at 2026-09-02.
  4. 4. Delete italy-app-model.js line 98 (the D.PRODUCTS fallback). Confirm AM.counts.products is still 166 — it adds 0 records today, so any change in that number means the deletion was wrong.
  5. 5. Replace italy-app-model.js lines 102-105 with the new collection. Rebuild each product's verifiedLinks / relatedLinks / checkNeededLinks / rejectedLinks from its slice of productRelationships. Add relationships to the product record.
  6. 6. Rewrite strengthFor() to read the new collection and to return NOT_ASSESSED (a fifth, non-audited value) when no tier matched, so 'the audit rejected it' and 'nobody has looked yet' can never render the same.
  7. 7. Add aiLabel (array joined with ' + ') and moaLabel (HRAC/FRAC/IRAC groups) to each product record. Expose competitorProducts from IG.COMP_PRODUCTS.
  8. 8. portale.html: fix the Product Intelligence builder at 2999-3023 — pd.ai from aiLabel, pd.moa from moaLabel, pd.wins from AM.collections.windows joined on windowId (not D.WINDOWS), and add pd.related / pd.hasRelated.
  9. 9. portale.html markup 1339-1395: add the RELATED_PORTFOLIO card, add the evidence line (target + labelUrl) to the check-needed rows, keep the 'VEDI OPPORTUNITÀ COLLEGATE' CTA and the three-card layout untouched.
  10. 10. portale.html: replace the vocabulary reads — line 3379 (D.PRODUCT_LIST -> AM.products), lines 3418 and 3426 (Object.keys(D.PRODUCTS) -> distinct products carrying a relationship, extracted once into a shared const).
  11. 11. portale.html: replace the relationship reads — 2880 (window detail products), 2863 (goProduct), 2621-2623 (case primary/alternatives, removing primaryUse), 2947 (market crop products), 2745 (why-now count, split by class), 2592 and 3216-3219 (delete the hand-written 'VERIFIED PORTFOLIO' literals).
  12. 12. portale.html: retire D.CPRODUCTS at 3185-3186 in favour of AM.collections.competitorProducts, dropping crops/issues/first/last/paid/people/cases unless each is separately measured as derivable from the 102 product-bearing activities. Make the route fall back cleanly when an old demo cproduct id no longer exists.
  13. 13. portale.html markup: remove 'Stage · {{ p.use }}' and 'Target fit · {{ p.targetFit }}' at line 782; split the three absence strings (784, 1233, 2745) into NOT_CONFIRMED (with t.absenceRule) and NOT_ASSESSED.
  14. 14. Add the i18n key prodRelatedRel (it: 'PORTAFOGLIO CORRELATO', en: 'RELATED PORTFOLIO') and a NOT_ASSESSED entry in PSTATE for both languages.
  15. 15. Re-measure and publish, on the Portafoglio note line, the post-rebuild counts alongside LV.AUDIT_DATE: 12 windows verified, 6 to verify, 10 related-portfolio, 1 with no confirmed match — and 20 products carrying evidence-backed relationships out of 166.

-- RISKS --
  ! The rebuild's headline number gets WORSE, and that is the point. Window-level portfolio state goes from 10 VERIFIED / 10 LABEL_CHECK_NEEDED / 9 NO_CONFIRMED (measured today, on the 29 opportunity cards) to 12 VERIFIED / 6 LABEL_CHECK_NEEDED / 10 RELATED_PORTFOLIO / 1 NO_CONFIRMED. Verified goes UP (12 vs 10, because 4 audited verified triples were never reachable through the fixture and 2 fixture 
  ! The ISSUE_TARGET table is the one hand-authored input in the entire chain and it decides 15 of the 44 evidence-backed rows. If it is written loosely it manufactures matches; if it is written tightly it starves the check-needed class. It must be versioned, attributed, and surfaced per row via mappingRule — otherwise the rebuild replaces a demo fixture with an undocumented human judgement, which is 
  ! The RELATED_PORTFOLIO tier is enormous and low-value: 321 rows over 29 windows, with individual windows carrying up to 38 related products (Durum Wheat · Resistant grass weeds). It must be collapsed in the UI (a count plus a 'see all' link), never rendered as a list, or the screens become unreadable and the four-class hierarchy loses meaning.
  ! Tier 1 is empty. The precedence 'HANDOFF V2.1 canonical > verified label verdict' is honoured in code but contributes nothing today: all 29 canonical windows have PRODUCT_MATCHES = []. Do not describe the result as canonical-backed. The strongest real evidence in the system is a 12-verified / 7-not-found audit of 163 labels, dated 2026-09-02, whose own SCOPE_NOTE says it verified the main visible 
  ! The label audit is small relative to the screens it governs: 19 audited triples against 29 windows and 166 products. Its NOT_FOUND set is 7 triples covering 6 products, and three of those seven are the same claim (Olive Fruit Fly) repeated across three windows — so a single audit row drives three cards to the palest state.
  ! IG.LINKS covers 19 of 163 products. IG.CROPS states the gap explicitly and it is severe: GRAPEVINE has 61 products mentioning the crop and 1 with a use row read; DURUM_WHEAT has 14 and 0. Any check-needed count computed from use rows is a floor, not a measurement of the portfolio.
  ! Deep links break. The competitor-product route ('cproduct') currently addresses 14 demo names; the real set is 36 different names. Old ids, bookmarks and any in-page chip pointing at Belanty/Revysol/Movento will 404 unless a fallback is added.
  ! Four different ADAMA legal entities hold the 163 registrations (ADAMA ITALIA 85, ADAMA AGAN 35, ADAMA MAKHTESHIM 26, ADAMA DEUTSCHLAND 17) while the catalog is the Italian commercial site. Do not merge holder and seller into one 'ADAMA has this product' claim, and keep the two Portafoglio tabs separate — 41 of 44 catalog items match a registration, 3 (BUDGE, EXELGROW, PARLEAF, all SPECIALI) do not
  ! 15 of the 163 registrations have an expiry earlier than the 2026-09-02 reference date and 41 expire before 2027-01-01. Nothing in the current portfolio screens reads expiry. Surfacing a verified label match for an expired authorisation would be a new, avoidable false claim — worth guarding even though it is outside the strict scope of this migration.
  ! D.CASES remains the source of the 29 opportunity objects even after relationships are rebuilt. This spec removes the demo from PRODUCT RELATIONSHIPS only; the opportunity/radar domain owner must handle the rest, and until then the relationship layer will be correct while the card it sits inside is still a scenario.

-- OPEN QUESTIONS --
  ? Is the ISSUE_TARGET mapping (English issue name -> Latin label target) acceptable as a Sintonia-authored artefact, or must it come from upstream? If it must come from upstream, tier 3 disappears and the result is 12 verified / 0 check-needed / 16 related / 1 no-confirmed across the 29 windows.
  ? Should Durum Wheat inherit the WHEAT_GENERIC ('Frumento') use rows? DURUM_WHEAT itself has 0 use rows read. Inheriting adds check-needed candidates on two Durum Wheat windows; not inheriting leaves them related-only. This is an agronomic/regulatory call, not an engineering one.
  ? Should RELATED_PORTFOLIO be gated more tightly than crop + product line? A tighter gate (e.g. same MoA family as an already-verified product) would cut the 321 rows sharply but adds another authored rule.
  ? The 'region' line under each relationship card: relabel it as the window's region, or delete it? A label authorisation has no region.
  ? Does the competitor-product view survive at all? IG.COMP_PRODUCTS has four fields. If crops/issues/dates cannot be derived from the 102 product-bearing activities, the view becomes a name, a company, an ad count and a proof string — which may be better folded into the company page than kept as its own route.
  ? Is a fifth state (NOT_ASSESSED) acceptable alongside the four mandated strength classes, or must 'nothing has been looked at' be expressed as LABEL_CHECK_NEEDED? Collapsing it into LABEL_CHECK_NEEDED would put ~53 products and every unmatched triple into the amber class and re-inflate the number the rebuild is meant to

====================================================================================================
DOMAIN: GLOBAL SEARCH (top-bar query → view 'search' results screen)
CONFIDENCE: high  |  realSource: AM.searchIndex, rebuilt in italy-app-model.js over AM.collections: products(166) · windows(29, CANONICAL) · opportunities filtered to isUpstreamReal(3 of 29) · futureSignals(3) · voices(17) · researchers(60) · resistance(34) · scienceRecords(88) · themes(5, new) · sources(31) · channels(30) · events(18) · news(8) · marketObservations(77) · competitorActivities(503) · competitorCompanies(11 normalized, new) · competitorProducts(36, new). Measured target size 1114 entries vs 309 today, with ZERO cross-kind id collisions (verified). AM.archive is NOT indexed separately — it is already a derived i
SUMMARY: Today the visible global search is 100% legacy demo. `AM.searchIndex` (309 entries) exists in italy-app-model.js but the string "searchIndex" appears ZERO times in portale.html — it is dead code. portale.html lines 3374-3385 build eight hardcoded groups by manually scanning D.CASES(29 decorated), A_SIGREAL(3, the only real source), D.PRODUCT_LIST(33), D.SCI_THEMES(10)+D.RECORDS(36), D.PEOPLE(39), D.COMPANIES(6), D.ARCHIVE(448) and D.SOURCES(53); each group caps display at 6 items while printing the full count. The markup (2355-2369) renders those groups and calls `i.go`, so every route target is decided inside the group builder. Facts come from the demo fixture even where a real equivalent exists: real portfolio is 166 products (163 regulatory + 3 catalog-only), not 33; real sources are 31, not 53; the real archive index is 740 rows across 7 kinds, not 448 manufactured rows. The searchTotal line also prints a hardcoded English sentence enumerating the eight groups, in an Italian-default product.

-- MODEL CHANGES --
  * searchIndex (rebuilt) :: The view must group and route with one reduce and one dispatcher, never a per-kind scan. Entry shape today lacks group, meta, routeArgs and provenance, which is precisely why portale.html hand-writes eight scans.
      fields: kind — record type, e.g. 'product','window','voice','science','market', group — UI group key the view buckets on: OPPORTUNITY | WINDOW | PRODUCT | SIGNAL | FIELD_VOICE | SCIENCE | PEOPLE | COMPETITOR | MARKET | SOURCE | NEWS | EVENT, id — the record's own id, in the namespace its destination view resolves, label — visible row text; never a concatenation containing a not-known note, meta — right-ha
      derivation: One idx() call per collection over AM.collections. Sizes measured: product 166, window 29, opportunity 3 (isUpstreamReal only), signal 3, voice 17, researcher 60, resistance 34, science 88, theme 5, source 31, channel 30, event 18, news 8, market 77, competitorActivity 503, company 11, competitorProduct 36 = 1114 entries. Verified: no id collides across kinds.
  * terms helper (new, exported as AM.terms) :: Search terms are the one place where a research note becomes a visible fact. Measured note contamination: voices.role 17/17, voices.organization 17/17, voices.region 17/17, researchers.role 60/60, futureSignals.region 2/
      fields: NOT_KNOWN(v) — /n[ãa]o sei|non so|^unknown$|^not stated$/i, TERMS(list) — filter null/''/NOT_KNOWN/length>80, fold diacritics, lowercase, dedupe, cropTerms(raw) — raw + canonical i18n key + Italian label, issueTerms(raw) — raw + canonical i18n key + Italian label
      derivation: cropTerms uses a measured alias table over the FIVE crop vocabularies actually present: canonical i18n keys in windows (Grapevine, Maize, Olive, Sugar Beet, Apple, Wheat, Durum Wheat, Tomato, Rice, Soybean); SCREAMING_SNAKE in voices/science (DURUM_WHEAT, VINE, MAIZE, OLIVE, CEREAL); Italian/Portuguese in news (MAIS, VITE, FRUMENTO, SOIA, ORTICOLE, CEREAIS); Latin binomials in competitor activities (Zea mays, Triticum aestivum, Vitis vinifera, Olea europaea, Solanum lycopersicum, Beta vulgaris, Malus domestica, Oryza sativa, Helianthus annuus, Citrus, Prunus persica) plus Italian generics (cereali, colture, frutta, ortaggi); and product crop codes (WHEAT_GENERIC, BARLEY, TRITICALE). Map ONLY
  * researchers (fix) :: org null 60/60 and url null 60/60 because the model reads the wrong keys; that alone makes 60 real researchers searchable by surname only.
      fields: org ← INSTITUTIONS.join(' · ') (60/60 non-empty), institutions ← INSTITUTIONS (array), theme ← THEME (60/60, one of the 5 IT-THEME values), orcid ← ORCID (60/60), lastActivity ← LAST_ACTIVITY (60/60 real dates), identityStatus ← IDENTITY_STATUS, role — must render UNKNOWN: 60/60 are 'NÃO SEI — o índice não declara papel', crops/issues — REMOVE, no such field exists upstream
      derivation: Direct rename against the measured IG.RESEARCHERS schema.
  * scienceRecords (fix) :: date and year are null 88/88 because the model reads DATE/YEAR while IG.SCIENCE supplies PUBLISHED_AT (88/88). A search result with no date is a search result the user cannot judge.
      fields: date ← PUBLISHED_AT (88/88), author ← AUTHOR (88/88), venue ← VENUE (88/88), doi ← DOI, materialType ← MATERIAL_TYPE, materialRole ← MATERIAL_ROLE, countryOfFact ← COUNTRY_OF_FACT, studyArea — REMOVE, no such field upstream
      derivation: Direct rename against the measured IG.SCIENCE schema.
  * themes (new) :: The search SCIENCE group currently offers 10 demo themes with slug ids. Five real themes exist and 60/60 researchers link to them by THEME, giving a real researcher↔theme join the demo one only pretended to have.
      fields: id ← ID (IT-THEME-001..005), theme ← THEME, query ← QUERY, works ← WORKS (135/70/71/27/17), authorsIt ← AUTHORS_IT, authorsWithOrcid ← AUTHORS_WITH_ORCID, authorsActiveSince2024 ← AUTHORS_ACTIVE_SINCE_2024, institutionsTop ← INSTITUTIONS_TOP
      derivation: coll(IG.THEMES.map(...), REAL_SOURCE). Note the ids do not overlap D.SCI_THEMES slugs, so openTheme must not be the route until the Science view migrates.
  * events (fix) :: name is null 18/18 — the model reads NAME||name while IG.EVENTS supplies EVENT. Consequence beyond search: all 18 EVENT rows in AM.archive have title: null (measured).
      fields: name ← EVENT (18/18), url ← OFFICIAL_URL, organizer ← ORGANIZER, sector ← SECTOR, cropRelevance ← CROP_RELEVANCE, timeState ← TIME_STATE
      derivation: Direct rename against the measured IG.EVENTS schema.
  * news (fix) :: outlet null 8/8 (reads OUTLET/SOURCE, upstream is PUBLISHER) and url null 8/8 (reads URL, upstream is SOURCE_URL 8/8).
      fields: outlet ← PUBLISHER (8/8), url ← SOURCE_URL (8/8), author ← AUTHOR, contentKind ← CONTENT_KIND, summary ← SINTONIA_SUMMARY, caveat ← CAVEAT
      derivation: Direct rename against the measured IG.NEWS schema.
  * channels (fix) :: platform null 30/30 and url null 30/30. IG.CHANNELS has no PLATFORM field at all and the link is CHANNEL_URL.
      fields: url ← CHANNEL_URL (30/30), exampleTitle ← EXAMPLE_TITLE, exampleUrl ← EXAMPLE_URL, contentTypeExample ← CONTENT_TYPE_EXAMPLE, identityState ← IDENTITY_STATE, caseId ← CASE_ID, views ← VIEWS, platform — REMOVE, not observable from this source
      derivation: Direct rename against the measured IG.CHANNELS schema.
  * sources (fix) :: group is null 31/31; the model reads GROUP but IG.SOURCES has no such field. The demo grouping taxonomy (GOVERNMENT & OFFICIAL, RESEARCH & SCIENCE, …) is a fixture invention.
      fields: role ← ROLE, country ← COUNTRY, frequency ← FREQUENCY, accessStatus ← ACCESS_STATUS, limitations ← LIMITATIONS, latestObservation ← LATEST_OBSERVATION, group — REMOVE or derive strictly from TYPE
      derivation: Direct rename against the measured IG.SOURCES schema.
  * competitorCompanies (new) + competitorProducts (new) :: Search must reach the 11 real observed companies, not the 6 in the fixture. IG.COMP_COMPANIES has 14 rows for 11 companies because of case duplication (SYNGENTA vs Syngenta, BAYER vs Bayer, CORTEVA vs Corteva).
      fields: company — deduped on UPPER(COMPANY); measured activity counts BASF 138, FMC 102, CORTEVA 94, BAYER 86, SYNGENTA 55, UPL 13, SIPCAM 6, CERTIS 4, SUMITOMO 2, GOWAN 2, NUFARM 1, paidAdsReachingIt ← PAID_ADS_REACHING_IT, organicVideosInCorpus ← ORGANIC_VIDEOS_IN_CORPUS, pages ← PAGES, productsProved ← PRODUCTS_PROVED, competitorProducts: product ← PRODUCT, company ← COMPANY, adsReachingIt ← ADS_REACHI
      derivation: Merge the case-duplicated COMP_COMPANIES rows on UPPER(COMPANY) and sum. §8/§9: adsReachingIt is communication density reaching Italy, never market share and never targeting.
  * marketObservations (routing metadata only) :: The Market Pulse view is addressed by MK.CROPS key (maize|durum|soft|olive|wine|tomato|sugarbeet|apple), while market records carry GROUP (CEREAL|OLIVE_OIL|WINE) and a raw product code.
      fields: ui.marketCropKey — only where the mapping is unambiguous
      derivation: Measured group|product pairs: 'MAI|FEED'(10) and 'Feed maize'(1) → maize; 'DUR|UNKNOWN'(7) and 'Durum wheat'(1) → durum; 'BLTPAN|PAN'(12) and 'Breadmaking common wheat'(1) → soft; OLIVE_OIL (36 across 6 grades) → olive; WINE(1) → wine. 'Feed barley'(1) and 'ORGFOUR|FEED'(7) map to NOTHING — barley is not a MK crop and ORGFOUR is unresolved; those route to view 'market' without changing mCrop rather than being forced into a bucket.

-- FIELDS THAT DIE (16) --
  [REMOVE_SUBCOMPONENT] ARCHIVE search group (448 demo rows) @ portale.html:3383 sgp('ARCHIVE', …, D.ARCHIVE.filter(…)) :: AM.archive(740) is a derived index over science/market/competitor/voice/event/news/window — the same records the per-kind groups now reach directly and route to their own screen. Keeping an ARCHIVE group would double-cou
  [REMOVE_SUBCOMPONENT] archive row .region and .product @ portale.html:3383 match(a.title + ' ' + a.crop + ' ' + a.region + ' ' + (a.produ :: AM.archive rows carry only kind, id, title, date, source, url, crop, issue. Searching the archive by region or by product simply stops existing. Measured archive field coverage: title 722/740, date 521/740, crop 401/740,
  [REMOVE_SUBCOMPONENT] opportunity search results 29 → 3 @ portale.html:3377; italy-app-model.js:300 :: Measured: 26 of 29 opportunities are provenance DEMO_SCENARIO. Only IT-OPP-001, IT-OPP-002 and IT-OPP-003 have an upstream record. A search for 'Septoria' or 'Cercospora' will return zero opportunity cases after this cha
  [REMOVE_SUBCOMPONENT] opportunity .latin (scientific name of the pest/pathogen) @ portale.html:3377 c.latin :: IG.OPPORTUNITIES has no Latin-name field. Today 'Scaphoideus titanus' finds IT-OPP-001 through the fixture. After migration, only the ISSUE string is searchable for opportunities — Latin names remain reachable through th
  [DERIVABLE_FROM_REAL] science theme .works count and theme ids @ portale.html:3380 D.SCI_THEMES … meta: t.works + ' works' :: 10 demo themes with slug ids (grapevine-phytoplasma, scaphoideus, maize-borer, olive-bactrocera, durum-fusarium, cercospora-dss, maize-mycotoxins, xylella, ipm, resistance) become 5 real ones (VINE_FLAVESCENCE 135 works,
  [REMOVE_SUBCOMPONENT] science record .descriptor and .location (study area) @ portale.html:3380 D.RECORDS.filter(r => match(r.descriptor + ' ' + r.institution :: Real SCIENCE records have TITLE, AUTHOR, VENUE, INSTITUTION, COUNTRY_OF_FACT — no descriptor and no study area. This is the field the screen itself already warns about at line 2063 ('affiliation ≠ field location'). Searc
  [SHOW_UNKNOWN] researcher .crops, .issues, .region and .role @ portale.html:3381 people.filter(p => match(p.label + ' ' + p.org + ' ' + p.crops :: Measured on IG.RESEARCHERS(60): no crops field, no issues field, FACT_REGION is 'NÃO SEI — a afiliação é do AUTOR, não do estudo' for all 60, and ROLE is 'NÃO SEI — o índice não declara papel' for all 60. You cannot sear
  [REMOVE_SUBCOMPONENT] researcher search results routing to a person page @ italy-app-model.js:303 route 'person'; portale.html:3370 pr0 = people.find(p =>  :: Hard block, not a soft one. Researcher ids are IT-PER-001..060 and D.PEOPLE ids are RR-01..RR-14, P-13..P-30, TSR-1..TSR-7. Zero overlap. Every one of the 60 would open the same demo person. Until the Sources/People view
  [REMOVE_SUBCOMPONENT] company .crops and .issues @ portale.html:3382 companies.filter(c => match(c.name + ' ' + c.crops.join(' ') + :: IG.COMP_COMPANIES has COMPANY, PAID_ADS_REACHING_IT, ORGANIC_VIDEOS_IN_CORPUS, PAGES, PRODUCTS_PROVED — no crop coverage and no issue coverage at company level. Searching 'which competitor talks about maize' at the compa
  [REMOVE_SUBCOMPONENT] source .cov (coverage) and .topics @ portale.html:3384 D.SOURCES.filter(x => match(x.name + ' ' + x.type + ' ' + x.co :: IG.SOURCES has GEOGRAPHY (31/31, values like 'IT') and ROLE/LIMITATIONS, but no topic list. Searching a source by the crop it covers dies. Source count also drops 53 → 31, and 25 of the 31 real ids (IT-SRC-*) currently c
  [SHOW_UNKNOWN] voice .role, .organization, .region @ italy-app-model.js:301 (proposed terms); portale.html:3058-3061 roleLine / regio :: Measured 17/17 for all three: the upstream value is the Portuguese note 'NAO SEI …'. These must never become search terms, and the Voci view's `v.region || T.vociRegionNotStated` fallback does not fire because the note s
  [DERIVABLE_FROM_REAL] product .use, .moa, .cat @ portale.html:3379 D.PRODUCT_LIST (fields use/moa/cat) :: Real registry supplies hrac/frac/irac and line/regCat instead. But the honest coverage cost is elsewhere: of the 166 real products, 46 have an empty crops list and 67 have an empty targets list. So 'which product for Ama
  [SHOW_UNKNOWN] future signal label region @ italy-app-model.js:302 label f.issue + ' · ' + f.region :: 2 of 3 signals have REGION = 'NAO SEI — o recorte cientifico e por afiliacao nacional, nao por regiao de estudo'. Today that whole sentence is the search result label. After the fix the label is the issue alone, and the 
  [SHOW_UNKNOWN] resistance .crop as a searchable crop @ italy-app-model.js:304 terms include r.crop :: CROP_DECLARED is the GIRE researcher's own Portuguese prose: 7 of 34 exceed 60 characters, e.g. 'soia, pomodoro e melone (soja, tomate e melao) — literal: "Sistemi colturali: soia, pomodoro e melone." ATENCAO: a ficha li
  [SHOW_UNKNOWN] searchTotal as a count of real connected items @ portale.html:3456 and markup 2358 :: Today this number sums 26 demo opportunity cases, 448 demo archive rows and 53 demo sources into a figure the screen calls 'connected items'. After migration it counts route-ready real entries only. Expect the visible nu
  [SHOW_UNKNOWN] cross-language search (Italian query against English-only terms) @ portale.html:2541 match(); italy-app-model.js:298 terms :: Measured: SINTONIA_I18N is a one-way EN-key → IT-label map and en.CROPS is an EMPTY object. Terms today are raw source values only. So an Italian user who sees 'Frumento duro' on screen must type 'Durum Wheat' to find it

-- PLAN --
  1. 1. italy-app-model.js — add the term hygiene helpers above the search section: NOT_KNOWN(v) = /n[ãa]o sei|non so|^unknown$|^not stated$/i, FOLD(v) = String(v).normalize('NFD').replace(/[̀-ͯ]/g,'').toLowerCase(), and TERMS(list) = list.flat().filter(v => v && !NOT_KNOWN(v) && String(v).length <= 80).map(FOLD) deduped. Nothing else in the file changes yet.
  2. 2. italy-app-model.js — add CROP_ALIAS and ISSUE_ALIAS tables mapping ONLY the unambiguous measured vocabularies to a SINTONIA_I18N.it.CROPS / .ISSUES key, plus cropTerms(raw) and issueTerms(raw) that emit [raw, key, italianLabel]. Leave CEREAL, CEREAIS, cereali, colture, frutta, ortaggi, ORTICOLE, Citrus, Prunus persica, WHEAT_GENERIC, BARLEY, TRITICALE unmapped and verbatim.
  3. 3. italy-app-model.js — fix the five collections whose search terms are currently null because the model reads the wrong upstream keys: researchers (org ← INSTITUTIONS.join(' · '), theme ← THEME, orcid ← ORCID, lastActivity ← LAST_ACTIVITY, drop crops/issues, role renders UNKNOWN), scienceRecords (date ← PUBLISHED_AT, author ← AUTHOR, venue ← VENUE, doi ← DOI, drop studyArea), events (name ← EVENT, url ← OFFICIAL_URL, organizer ← ORGANIZER), news (outlet ← PUBLISHER, url ← SOURCE_URL, author ← AUTHOR), channels (url ← CHANNEL_URL, exampleTitle ← EXAMPLE_TITLE, caseId ← CASE_ID, drop platform).
  4. 4. italy-app-model.js — add the three new collections: themes(5 from IG.THEMES), competitorCompanies(11, deduped on UPPER(COMPANY) from the 14 case-duplicated COMP_COMPANIES rows, counts summed), competitorProducts(36 from IG.COMP_PRODUCTS). Register them in the `collections` object so provenanceSummary and counts pick them up.
  5. 5. italy-app-model.js — lift the GIRE species/mechanism/crop normalizer that lives in portale.html lines 3247-3287 (NOTE regex, split on '—' never on '(', genus extraction, MOA table, year-only-if-bare-year) into the model as resistance record fields (species, speciesFull, speciesIt, genus, mech, cropClean, year). The view then reads them instead of re-deriving, and the search index and the science screen finally agree. §11: never truncate at '(' — 'Schoenoplectus (Scirpus) mucronatus' and 'Oryza sativa (L.) var. sylvatica' must survive.
  6. 6. italy-app-model.js — replace lines 297-304 with the new idx(kind, group, id, label, meta, terms, route, routeArgs, provenance) and one call per collection: PRODUCT(166), WINDOW(29), OPPORTUNITY(3, isUpstreamReal only), SIGNAL(3), FIELD_VOICE(17), SCIENCE(88 records + 5 themes + 34 resistance), PEOPLE(60), SOURCE(31 + 30 channels), EVENT(18), NEWS(8), MARKET(77), COMPETITOR(503 + 11 companies + 36 products). Guard every push with !isDemo(record, collectionProvenance). Verify the total lands near 1114 and that no entry has an empty terms array.
  7. 7. portale.html — add the routing dispatcher as a class method next to openSource (around line 2420): openSearchHit(e) switching on e.route → openProduct(e.id) | openCase(e.id) | openWindow(e.id) | openSignal(e.id) | go({view:'voices', voiceId:e.id}) | go({view:'science', sciFocusId:e.id}) | go({view:'science', gireAll:true, gireFocusId:e.id}) | go(Object.assign({view:'market', mTab:'crop'}, e.routeArgs)) | openSource(e.id) | openCompany(e.id) | compWith({fCompany:e.routeArgs.company}) | openEvent(e.id) | openPerson(e.id). One method, no per-kind logic in the group builder.
  8. 8. portale.html — add the readiness gate immediately above it as a single constant: const SEARCH_ROUTE_READY = { product:1, case:1, window:1, signal:1, voices:1, science:1, gire:1, market:1 }. Routes source, company, competitor, event, news and person stay OFF until their own domain reads AM, because each currently resolves with a `|| collection[0]` fallback into a demo entity (verified at lines 3171 companies[0], 3181 D.EVENTS[0], 3185 D.CPRODUCTS[0], 3367 D.SOURCES[0], 3370 people[0]). Flipping one flag per landed domain is the whole integration cost.
  9. 9. portale.html — add state keys voiceId:null, sciFocusId:null, gireFocusId:null to the state initialiser at line 2375-2386. They are presentation focus only: the Voci row and the GIRE row gain a `focused` boolean used for a border colour. No ordering change, no new screen.
  10. 10. portale.html — replace lines 3374-3385 with: SGROUP_DEF (12 group keys with their i18n label and colour, order fixed), hits = AM.searchIndex.filter(e => SEARCH_ROUTE_READY[e.route] && e.terms.some(t => t.indexOf(q) >= 0)), byGroup = hits.reduce(...), searchGroups = SGROUP_DEF.filter(g => byGroup[g[0]]).map(sgp). Delete the `match` helper at line 2541 once confirmed it has no other caller (verified: it does not).
  11. 11. portale.html:2540 — fold diacritics in q the same way TERMS folds them, so 'Marzachi' finds 'Cristina Marzachì'. Both sides of the comparison must use the identical fold, or the index and the query drift.
  12. 12. portale.html:3456 — searchTotal: hits.length (not the sum of capped group counts).
  13. 13. portale.html markup 2358 and 2364 — swap the two hardcoded English strings for new i18n keys (searchResultsFor, searchTotalNote, searchMore) and repurpose the g.empty node into the g.hasMore overflow row. Bump hint-placeholder-count at 2360 from 6 to 8. Nothing else in the card changes.
  14. 14. italy-i18n.js — add searchResultsFor, searchTotalNote and searchMore to both it and en (the file is currently symmetric: 371 keys each, zero missing). Keep the total-note wording generic so it does not enumerate groups.
  15. 15. Verify by query, in the browser, in Italian and in English: 'Frumento duro' and 'Durum Wheat' return the same window and market rows; 'flavescenza' returns opportunity IT-OPP-001, 8 voices, 43 science records and the VINE_FLAVESCENCE theme; 'Sipcam' returns nothing until the competitor gate opens (and never opens BASF); 'Marzachì' returns the researcher only when the people gate is open. Confirm no result opens a screen whose header shows a different entity than the row clicked.

-- RISKS --
  ! Six of the twelve search groups (SOURCE, PEOPLE, COMPETITOR, EVENT, NEWS, and any science route through openTheme) land on screens that still resolve ids against the demo fixture with a `|| collection[0]` fallback. If the engineer wires them before those domains migrate, search becomes the fastest way in the whole product to reach a demo entity while believing it is real. The readiness gate is not
  ! Search visibly gets emptier for some queries. Opportunity hits drop from 29 to 3; source hits from 53 to 31; researcher, source, company, event and news groups disappear entirely in phase 1. A reviewer who does not know the measurements will read this as a regression. Publish the before/after counts alongside the change.
  ! The crop alias table is the one place in this spec where judgement enters the data path. Every entry must be a 1:1 lexical identity ('Zea mays' = 'Maize' = 'Mais'), never a semantic inference. 'CEREAL' → wheat, or 'cereali' → 'Frumento tenero', would be inventing a fact inside a lookup table, and it would be invisible in review because it lives in a synonym list rather than in a record.
  ! Indexing all 503 competitor activities makes COMPETITOR the loudest group for almost any crop word (BASF alone has 138). With a 6-row cap and an honest '+497 more', the screen stays truthful, but the group ordering in SGROUP_DEF now decides what the user sees first. Put OPPORTUNITY, WINDOW and PRODUCT before COMPETITOR.
  ! italy-app-model.js line 98 still admits D.PRODUCTS keys into `products` as SYNTHETIC_DEMO when they are absent from the registry and the catalog. Measured today: 0 such products, so nothing leaks. That is luck about the current data load, not a guarantee — the index push must still be guarded by !isDemo, or the next intelligence package silently puts demo product names into global search.
  ! Fixing events.name, news.outlet, news.url, science.date, researchers.org and channels.url changes fields that other domains are reading right now as empty. Anything currently rendering a fallback dash for those will start rendering content, and anything that branched on the null (a `hasX` flag) will flip. Coordinate with the Archive, Sources and Science domains before landing step 3.
  ! The Voci and GIRE focus highlight adds two new state keys and a border colour. Kept to that, it preserves the visual product. If it grows into a per-voice detail page it becomes a redesign, which §12 forbids in this pass.

-- OPEN QUESTIONS --
  ? Is there any appetite for a real per-voice detail screen? Today the Voci view is a flat list with no id anchor, so a voice search result can only scroll-and-highlight. That is honest but thin for 17 records that each carry a quote, a WHAT_IT_PROVES and a WHAT_IT_DOES_NOT_PROVE.
  ? Which domain owns fixing the `|| collection[0]` fallbacks at portale.html 3171, 3181, 3185, 3367 and 3370? The signal view already has the correct pattern (sgMissing at 2892). Search cannot be made safe alone if those five keep silently substituting the first demo record.
  ? Should channels (30) be their own search kind routed to Sources, or folded into the voice entries as extra terms? 17 voices carry a channel name; the other 13 channels have no voice. Folding loses 13 records, a separate kind needs a screen that lists real channels.
  ? Market records CEREAL|'Feed barley' and CEREAL|'ORGFOUR|FEED' (8 observations) have no MK.CROPS bucket. Route them to Market Pulse with no crop preselected, or omit them from search until the market crop list covers barley?
  ? italy-app-model.js never reads window.ITALY_LABEL_VERDICTS, so product link strengths still come from D.CASES[].productLinks (portale.html:102-105 in the model). That is outside this domain, but it means the PRODUCT search group's relationship classes are fixture-derived. Worth confirming whoever owns Product Intellige
  ? The search input placeholder says 'Cerca coltura o prodotto…'. Once the index covers windows, science, market and sources, should the copy widen — and does that need brand sign-off?

====================================================================================================
DOMAIN: Helper relationships, visual tokens, Field Sales, reference date, localization
CONFIDENCE: high  |  realSource: Visual tokens have no real source and do not need one — they move from window.ITALY_DEMO into a new AM.UI namespace in italy-app-model.js. The one fact they currently carry, the pest/disease/weed classification, comes from window.ITALY_CANONICAL.windows[].ISSUE_TYPE (measured 29/29 non-null: PEST 15, DISEASE 9, WEED 5) surfaced as AM.collections.windows.records[].issueType. The reference date comes from AM.referenceDate / AM.REF (2026-09-02, from ITALY_CANONICAL.meta.referenceDate). Localization comes from window.SINTONIA_I18N (371 keys, it + en both complete). Field Sales stays on AM.collecti
SUMMARY: This domain is the connective tissue: the colour/icon token tables (D.CAT, D.DEPT, D.F_COLOR, D.GROUP_COLOR, D.ATYPE_COLOR, D.STATUS), the formatting helpers (D.fmt, D.ago, D.inkOn, D.setMonths), the single clock (D.TODAY), the competitor crop axis (D.CROP_COLS), and the Field Sales inbound module (D.FIELD_MESSAGES, D.TSR, parseField/sendComposer/simulateInbound). Most of it really is presentation and can stay, but four pieces smuggle facts: D.CAT is not only colour and icon — its `label` ("Pest Control"/"Disease Control"/"Weed Control") is rendered as the big category chip via `catLabel` (portale.html:2450, markup 219 and 305), so every `|| D.CAT.disease` fallback (2819, 3139, 3207) prints a pest/disease/weed CLASSIFICATION that reality did not supply; D.F_COLOR is the enumeration of a 7-value future-signal status vocabulary that the real FUTURE_SIGNALS records do not have (measured: 3/3 real signals have status null, so all 7 filter chips count 0); D.GROUP_COLOR keys a source-group taxonomy that 31/31 real IG.SOURCES records lack (AM sources `group` is null 31/31); and D.FIELD_MESSAGES — 18 explicitly synthetic WhatsApp messages — is merged into `allMessages` (2603) and then rendered on two CORE screens: the Opportunity case panel "SEGNALI DI CAMPO · N" (2637, markup 403-408) and the Crop Windows "WHY PREPARE NOW?" reason row (2741, markup 764). Field Sales itself is correctl

-- MODEL CHANGES --
  * AM.UI (new namespace on window.ITALY_APP_MODEL) :: Visual tokens are legitimate (§4) but must not live in the demo fixture, because the fixture is going away and because reading them from ITALY_DEMO is what let a colour lookup smuggle a classification label. Moving them 
      fields: CATEGORY: { pest, disease, weed, unknown } — each { key, label, color, dark, soft, ink, body, muted, icon, aShape }, INK / inkOn(fill) — the contrast table, STATUS: status code -> { color, text, rank } plus a DEFAULT entry, DEPARTMENT: department name -> { color, soft } plus a DEFAULT entry, SIGNAL_STATUS_COLOR: signal status -> color, SOURCE_TYPE_COLOR: IG.SOURCES.TYPE -> color, REGION_GRID: [{ n
      derivation: CATEGORY/INK/STATUS/DEPARTMENT/REGION_GRID are copied VERBATIM from italy-demo-data.js lines 22-66 — they contain no crop, issue, date, source or count, only ADAMA brand constants and grid coordinates, so copying them breaks no rule. Two additions are new: CATEGORY.unknown = { key:'unknown', label:null, color:'#8F8886', dark:'#3A3533', soft:'#B1A9A7', ink:'#fff', body:'#EDEAE9', muted:'#B1A9A7', icon:'', aShape:'' } — label is deliberately null so the catLabel chip can be hidden rather than guessed; and STATUS.DEFAULT / DEPARTMENT.DEFAULT so an unknown key cannot throw. SOURCE_TYPE_COLOR is new, keyed on the 12 TYPE values actually present in IG.SOURCES. fmtDate is D.fmt with the month array
  * AM.collections.windows.records[].issueType and AM.categoryOf(issueType) :: This is the one FACT the token table was carrying. It must come from the canonical contract, not from a fallback.
      fields: issueType (string: 'PEST' | 'DISEASE' | 'WEED'), AM.categoryOf(issueType) -> AM.UI.CATEGORY entry
      derivation: issueType = w.ISSUE_TYPE, read straight off ITALY_CANONICAL.windows in the existing map at italy-app-model.js line 70-82. Measured: 29/29 windows carry it (PEST 15, DISEASE 9, WEED 5), 0 null. Cross-check performed: the legacy demo `cat` on all 29 D.CASES agrees with canonical ISSUE_TYPE 29/29, 0 disagreements — so this migration changes no colour on screen today, it only removes the ability to invent one tomorrow. AM.categoryOf = (t) => AM.UI.CATEGORY[String(t||'').toLowerCase()] || AM.UI.CATEGORY.unknown. It must NEVER fall back to disease.
  * AM.collections.windows.records[].ui (replace the current uiOf join) :: italy-app-model.js:67-71 currently derives ui.icon and ui.color by joining canonical windows to D.WINDOWS on LEGACY_CASE_ID and reading the demo record's category object. That works today (measured: 0/29 records have an 
      fields: ui.categoryKey, ui.icon, ui.color, ui.order
      derivation: ui = Object.assign({}, AM.categoryOf(w.ISSUE_TYPE), { order: index }). No fixture join. Delete the dwByCase lookup at lines 68-69 and the `uiOf(dwByCase[...])` call at line 71.
  * AM.DEMO (new, explicitly-labelled demo rules) :: §5 allows labelled demo modes. The planning lead offsets and the escalation ladder are invented ADAMA business rules — they are not observations and not facts about Italy, but they are also not colours. They need a home 
      fields: PLANNING_LEAD_RULE: [{ dept, fromDays, toDays }] x6 (currently DEPTS at portale.html:2652), PLANNING_LADDER: [{ days, dept, text }] x5 (currently D.LADDER), PREP_LEAD_DAYS: 90 (currently D.PREP_LEAD), provenance: 'SYNTHETIC_DEMO' on the group
      derivation: Copied verbatim from portale.html:2652, italy-demo-data.js:634 and italy-demo-data.js:760. No derivation from real data is possible or claimed — these are configuration, and they must be stamped SYNTHETIC_DEMO so the provenance panel counts them.
  * AM.collections.fieldMessages (scope change, not a data change) :: The collection is already correct: 18 records, provenance SYNTHETIC_DEMO, note 'optional integration demonstration; never affects core intelligence'. The note is currently false because portale.html merges it into core s
      fields: (unchanged fields), records[].provenance already 'SYNTHETIC_DEMO' — the view must filter on this rather than trusting call-site discipline
      derivation: No new derivation. The change is in portale.html: rename `allMessages` to `demoMessages`, build it as AM.collections.fieldMessages.records.concat(s.extraMessages), and reference it only inside the isField branch and the labelled nav-integration count. Every core-screen consumer (2637, 2741, 3148) is deleted.
  * AM.crops.communicationAxis (new) :: D.CROP_COLS is a fabricated six-crop English axis. If the competitor matrix and density strip are to keep working, the axis has to come from the records being counted.
      fields: key (the raw crop term as published), label (display string; Latin binomials are never translated per §11), count (activities carrying that term)
      derivation: Group IG.COMP_ACTIVITIES[].crops. Measured distribution: Zea mays 67, colture 59, Vitis vinifera 42, cereali 24, Olea europaea 24, Citrus 15, Solanum lycopersicum 12, Helianthus annuus 9, frutta 8, Malus domestica 5, ortaggi 5, Beta vulgaris 4, Oryza sativa 3, Prunus persica 2, Triticum aestivum 1. CRITICAL CAVEAT to carry into the UI: 320 of 503 activities (63.6%) carry NO crop at all, so the axis covers 183 records, not 503, and the screen must say so. 'colture', 'cereali', 'frutta' and 'ortaggi' are generic Italian buckets, not species — do not silently merge them into a species row.

-- FIELDS THAT DIE (10) --
  [REMOVE_SUBCOMPONENT] Future signal STATUS (the 7-value vocabulary NEW SIGNAL / GAINING ATTENTION / MULTIPLE SIGNALS / WATCH CLOSELY / NEEDS VALIDATION / TIMING APPROACHING / PREPARE) @ portale.html:3150 fStatuses, 3152 futureStatusChips, 3149 visibleSignals color/a :: IG.FUTURE_SIGNALS has no STATUS field of any kind. Its fields are ID, LEGACY_ID, WHO_IS_TALKING, WHAT_CHANGED, CROP, ISSUE, REGION, WHY_WATCH, HOW_SINTONIA_GOT_HERE, OBSERVED_FACTS, SINTONIA_INTERPRETATION, UNKNOWN, NEXT
  [SHOW_UNKNOWN] Future signal category / pest-disease-weed class @ portale.html:3139 (`cat: 'disease', category: D.CAT.disease, color: '#00A0DF'`), :: All three real signals are painted Disease Control blue. IT-FUT-003's ISSUE is literally 'REGULATORIO'. There is no ISSUE_TYPE on FUTURE_SIGNALS, so the card must render the neutral unknown token with no category chip an
  [SHOW_UNKNOWN] Source group (GOVERNMENT & OFFICIAL / RESEARCH & SCIENCE / FIELD & PRODUCER ORGANIZATIONS / NEWS & TRADE MEDIA / COMPANIES & MARKET / EVENTS & TRADE FAIRS) @ portale.html:2904 D.GROUP_COLOR[src.group]; also the sourceKpis buckets at 3363- :: AM.collections.sources has group === null on 31/31 records, because IG.SOURCES publishes TYPE and ROLE and never GROUP. The six-bucket taxonomy is a demo invention. IG.SOURCES.TYPE has 12 real values (OFFICIAL, MARKET, R
  [REMOVE_SUBCOMPONENT] Person activity history — 5 dated observations per person @ portale.html:3372 `history:` prop; markup 2344-2346 :: This is the worst item in the domain. It manufactures five dates spaced 9 days apart from a synthetic `lastDays`, and four invented sentences ('Content observed on X season', 'Mention of Y pressure', 'Reference to region
  [REMOVE_SUBCOMPONENT] Field Sales signal count and quoted field text on the Opportunity case screen @ portale.html:2637 (cs.fieldMessages / cs.fieldCount / cs.noField), markup 403-40 :: 18 synthetic WhatsApp messages, mapped onto 8 real opportunity ids, printing a region, a date-relative stamp and a verbatim quote on a core screen. Field Sales is a legitimate labelled demo module — but only on its own s
  [REMOVE_SUBCOMPONENT] 'N Field Sales signals' as a reason to prepare, on Crop Windows @ portale.html:2741 and the why.push at 2746; markup 764 :: Same 18 demo records, here promoted to a green tick inside 'WHY PREPARE NOW?'. A demo fixture is supplying a reason to act. The panel keeps its other five rows.
  [DERIVABLE_FROM_REAL] Competitor crop axis — the six columns Grapevine, Maize, Wheat, Olive, Tomato, Apple @ portale.html:3168 cropDensity, 3169 compMoments, 3441 matrixCols, 3443 compCropO :: Derivable, but honestly it shrinks. Real crop terms in IG.COMP_ACTIVITIES are Latin binomials and Italian generics, and 320 of 503 activities (63.6%) carry no crop at all. Exact string matching on 'Grapevine' returns zer
  [KEEP_AS_LABELLED_DEMO] Department planning lead offsets (-210..-10 days) and the 90/60/45/30/14-day escalation ladder @ portale.html:2652 DEPTS, 2701 deptB bars, 2861 deptPlan date ranges, 3403 ladder :: These are invented ADAMA business rules, not observations. They are already captioned ('Business rule (demo configuration), not an agricultural fact' at markup 759; 'Sintonia business recommendations, not agronomic facts
  [REMOVE_SUBCOMPONENT] D.REAL_STATS, D.REALITY, D.BULLETINS, D.CO_META, D.ATYPE_COLOR, D.CAL @ nowhere in portale.html — measured 0 reads each :: Dead exports. They are consumed only inside italy-demo-data.js while it builds ACTIVITIES / ARCHIVE / COMPANIES. No screen loses anything when the fixture is retired. Listing them so the engineer does not spend time hunt
  [DERIVABLE_FROM_REAL] Region tile case/signal counts on the Radar map and Case mini-map @ portale.html:2564 regionTiles, 2625 cs.miniMap (both via D.REGION_STATS) :: The {col,row} grid for Italy's 20 regions is pure layout and survives. The `cases` and `signals` counts that drive each tile's fill colour are demo-derived and must be recomputed from AM.collections.windows.records group

-- PLAN --
  1. 1. Add the AM.UI namespace to italy-app-model.js: CATEGORY (pest/disease/weed copied verbatim from italy-demo-data.js:22-26, plus a new `unknown` token with label:null, color:'#8F8886', icon:''), INK + inkOn (from line 29-31), STATUS (from 33-47, plus DEFAULT), DEPARTMENT (from 48-55, plus DEFAULT), REGION_GRID (from 57-66), SIGNAL_STATUS_COLOR, SOURCE_TYPE_COLOR keyed on the 12 real IG.SOURCES.TYPE values, NEUTRAL, and fmtDate(dateOrIso, monthNames). None of these carry a crop, issue, date, source or count, so copying them breaks no rule.
  2. 2. In italy-app-model.js, add `issueType: w.ISSUE_TYPE` to the windows mapper (line 70-82) and export `AM.categoryOf = (t) => AM.UI.CATEGORY[String(t||'').toLowerCase()] || AM.UI.CATEGORY.unknown`. It must never fall back to disease. Then replace the ui join: delete dwByCase (68-69) and uiOf(dwByCase[...]) (71), and set ui from AM.categoryOf(w.ISSUE_TYPE). Verified safe: demo cat agrees with canonical ISSUE_TYPE on 29/29 cases, so no colour changes today.
  3. 3. Add AM.DEMO = { PLANNING_LEAD_RULE (from portale.html:2652), PLANNING_LADDER (from italy-demo-data.js:634), PREP_LEAD_DAYS: 90, provenance: 'SYNTHETIC_DEMO' }.
  4. 4. Add AM.crops.communicationAxis, grouped from IG.COMP_ACTIVITIES[].crops, each entry { key, label, count }, plus AM.crops.uncoveredActivities = 320 and AM.crops.totalActivities = 503 so the view can publish the coverage gap.
  5. 5. REFERENCE DATE. portale.html:3391 -> todayLabel: AM.UI.fmtDate(AM.REF, T.months).toUpperCase(). Add a todayYear prop from AM.REF.getFullYear() and replace the hardcoded ' 2026' in markup 568 and 684. Then fix the one real second clock: italy-briefs.js:119 `new Date().toLocaleString(...)` -> format AM.referenceDate instead, and change the locale from the hardcoded 'en-GB' to the active language. portale.html:2638 (`AM ? AM.REF : new Date('2026-09-02T00:00:00')`) already derives from AM.REF and only needs its literal fallback removed once AM is guaranteed loaded.
  6. 6. VISUAL TOKENS, mechanical pass. Replace D.inkOn -> AM.UI.inkOn at portale.html 2680, 2708, 2710, 2756, 3210 (5 sites). Replace D.fmt -> AM.UI.fmtDate(x, T.months) at 2681 x2, 2743, 2757 x2, 2758, 2844 x2, 2845 x2, 2846 x2, 2861 x2, 3372, 3391 (16 sites). Replace D.DEPT -> AM.UI.DEPARTMENT at 2701, 2861, 2883 x2, 3086 (add the DEFAULT guard), 3403, 3419, 3426. Delete the D.setMonths call at 2477.
  7. 7. CLASSIFICATION FIXES (the fact-bearing part of D.CAT). Line 2819 -> derive from the canonical window's issueType via AM.categoryOf, never `|| D.CAT.disease`. Line 3207 -> AM.categoryOf(t.issueType || t.cat), fallback unknown. Line 3139 -> delete the `cat: 'disease', category: D.CAT.disease, color: '#00A0DF'` triple; use AM.UI.CATEGORY.unknown. Line 3406 -> keep (labelled demo composer) but stamp the record SYNTHETIC_DEMO. Then in decorate() at 2450, make catLabel resolve to '' when category.label is null and add a hasCategory flag; wrap markup 219, 221, 303, 305 accordingly.
  8. 8. FUTURE SIGNAL STATUS. Line 3150 -> derive fStatuses from the distinct non-null statuses actually present in AM.collections.futureSignals.records. Today that array is empty, so render the whole chip row only when it is non-empty. Line 3151 -> AM.UI.SIGNAL_STATUS_COLOR. Line 2948 -> suppress the status pill when status is null rather than printing an empty chip.
  9. 9. SOURCE GROUP. Line 2904 -> key on src.type against AM.UI.SOURCE_TYPE_COLOR. Separately flag to the sources spec that the six sourceKpis buckets at 3363-3365 count a taxonomy that is null 31/31 in real data.
  10. 10. FIELD SALES CONTAINMENT — the biggest behavioural change. Rename `allMessages` (2603) to `demoMessages` and build it from AM.collections.fieldMessages.records.concat(s.extraMessages). Then remove every core-screen consumer: delete cs.fieldMessages / cs.fieldCount / cs.noField at 2637 and markup 403-408; delete the c.hasField badge (markup 238); delete the fms const and the why.push at 2741/2746 plus the fieldCount/fms row props; delete fieldSignalIds at 3148 and the `fromField` badge it drives (markup 1530). Keep demoMessages alive only inside the isField branch (3096-3132), the navIntegra
  11. 11. Harden the demo module: guard simulateInbound (3401) against an empty pool, and keep sendComposer (3406) setState-only. Both were read and verified to mutate no core collection today; the guard is so that stays true when the fixture shrinks.
  12. 12. PERSON HISTORY. Delete the `history` prop at 3372 and markup 2344-2346. Coordinate with the researchers spec so this is removed exactly once.
  13. 13. COMPETITOR CROP AXIS. Replace D.CROP_COLS at 3168, 3169, 3441, 3443 with AM.crops.communicationAxis; change markup 1747 grid from repeat(6,1fr) to repeat({{ matrixColCount }},1fr) on the header and every body row; recalibrate the HIGH/MEDIUM/LOW thresholds against the real denominator and print the 320/503 uncovered share next to the strip.
  14. 14. LOCALIZATION. Fix the two cheap wins first: 'All departments' at 3426 -> T.allDepartments (its twin at 3419 already uses it), and the single leftover Italian value on the EN key `chVideo` in italy-i18n.js. Remove the dead duplicated Italian block at italy-i18n.js:475-482 inside the EN object (harmless today because 503-510 override it, but it is a trap). Then work the backlog by screen, worst first: Market (64 hardcoded runs in markup), Science (51), Signal Detail (40), Competitors (37), Window Detail (34), Crop Windows (32).
  15. 15. Re-measure after the edits: rerun the D.* grep and confirm the only surviving reads in this domain are the ones classified VISUAL_ONLY or EXPLICIT_DEMO, and rerun the string-literal counter to confirm the localization backlog actually shrank.

-- RISKS --
  ! The Field Sales containment removes visible content from two core screens (the Opportunity case panel and one Crop Windows why-row). Screens get emptier. §3 says a smaller truthful screen beats a dense fake one, and §12 says do not redesign to compensate — but someone will see the gap and ask for it to be filled. Have the answer ready before the edit lands.
  ! Deleting the Future Radar status chip row leaves that screen with three real signals, no status, no category and a REGION that literally reads 'NAO SEI — o recorte cientifico e por afiliacao nacional, nao por regiao de estudo'. It will look broken. It is not broken; it is honest. That screen may need a deliberate empty-state, which is a design decision, not an engineering one.
  ! AM.UI.CATEGORY.unknown has label:null and icon:''. Any markup binding that assumes a non-empty icon URL will render a broken image box. Lines 221, 287, 303, 756, 917, 1530, 1809, 2160, 2177, 2307, 2332 all bind category.icon — every one needs an sc-if guard, and it is easy to miss one.
  ! D.DEPT currently has all six colours identical (#978B87). Moving to AM.UI.DEPARTMENT is a pure copy, so nothing changes visually — which means a mistake here is silent. Diff the rendered colours, do not trust the copy.
  ! The competitor crop axis change is the highest-variance item. Swapping in real Latin binomials will change every number in the matrix, the density strip and the compMoments cards at once, and the demo-tuned HIGH/MEDIUM/LOW thresholds will misfire. Consider shipping the axis change behind the competitor spec's own review rather than as part of this one.
  ! italy-briefs.js:119 currently timestamps briefs with the real wall clock AND formats with 'en-GB'. Changing it to AM.referenceDate means every generated brief will say 02 Sep 2026 forever. That is correct under §6 but will read oddly to a presenter — it needs a caption saying the brief is generated against the intelligence reference date.
  ! The localization counts are measurements with known noise. 431 markup text runs is a count of visible text fragments between tags, which over-counts split labels and under-counts text hidden inside attributes. 541 dc-script literals comes from a real JS string tokenizer with Italian-detection and CSS filtering, but a spot-read shows some are status codes and internal keys rather than user-visible 
  ! Line 3372's fabricated history is claimed by both this spec and the researchers spec. If both engineers delete it the second edit conflicts; if neither does, it ships.

-- OPEN QUESTIONS --
  ? Future Radar has no real status vocabulary. Should the screen show three unstatused signals with a plain 'DA VALIDARE' treatment, or should the whole status dimension disappear until upstream supplies one? This is a product call, not an engineering one.
  ? IG.SOURCES.TYPE has 12 values and the demo taxonomy had 6 buckets. Is there an intended mapping from the 12 to a smaller presentation grouping, or should the six KPI tiles be replaced by counts over the raw 12?
  ? Is the competitor crop axis meant to be species-level? Four of the 15 real terms ('colture', 'cereali', 'frutta', 'ortaggi', 96 activities between them) are Italian generic buckets. Merging them into species rows would be a fabrication; keeping them makes a mixed-granularity axis.
  ? The department list and the planning lead offsets are ADAMA internal routing configuration. §1 says core must not require CRM/sell-in/pipeline — configuration is not that, but should the whole 'WHO SHOULD PREPARE · WHEN' component be moved under the INTEGRAZIONI · DEMO group alongside Field Sales, rather than sitting o
  ? D.KPI.people is 39 of which only 14 are real, and 7 of the demo ones are the synthetic TSR reps that italy-demo-data.js:777 adds with `KPI.people += TSR.length`. That inflated number feeds the Fonti nav count (K.orgs + K.people = 92). It is the sources spec's field, but it is the Field Sales fixture doing the inflating
  ? italy-i18n.js has 371 keys per language. Roughly a thousand user-visible strings are outside it. Is the goal full coverage, or Italian-correct on the screens a pilot actually demos (Radar, Crop Windows, Competitors, Field Sales) with English tolerated elsewhere?

====================================================================================================
DOMAIN: MODEL-LEVEL DEMO DEPENDENCIES IN italy-app-model.js + THE V2.1 INGESTION BOUNDARY
CONFIDENCE: high  |  realSource: After migration: cropWindows ← window.ITALY_CANONICAL.windows[29] (the only source of START/END/CURRENT_STATUS/DATE_STATE, and the only real crop×issue vocabulary that matches the screen). productRelationships ← window.ITALY_LABEL_VERDICTS (12 VERIFIED + 7 NOT_FOUND triples, 10 distinct products, all 10 present in AM.products) anchored to region/windowId by joining CROP_NAME+ISSUE_NAME against ITALY_CANONICAL (28 window-anchored links, 4 orphan LV pairs), plus IG.LINKS[219] as the product-detail "registered uses" table (product × crop-code × Latin target × labelUrl, 219/219 non-null, prov REAL
SUMMARY: italy-app-model.js is already the declared contract (5-level precedence, one REF date, one provenance classifier), but eight places still let window.ITALY_DEMO reach into it, and three of those carry facts or identities rather than pixels. Measured today: all 29 canonical windows publish a DEMO id (WIN-001…, not IT-WIN-0001); 100% of product relationships (80 links, 32 products) come from D.CASES.productLinks and 53 of those 80 have no verdict in the real label audit at all; the radar/opportunity collection is 29 D.CASES records of which only 3 have any upstream counterpart, and that upstream join is FALSE — IG.OPPORTUNITIES "IT-OPP-003" is the ADAMA Italy authorisation-expiry calendar (national, transversal) while D.CASES "IT-OPP-003" is Durum Wheat / Fusarium Head Blight / Toscana, yet the model stamps provenance REAL_SOURCE and isUpstreamReal=true on it. The two "harmless ui" joins on sources and events are measurably dead (0/31 and 0/18 produce any icon/color/order, because uiOf reads r.category/r.n which D.SOURCES and D.EVENTS do not have) while still hijacking 6 of 31 source ids into the demo namespace. The events adapter reads e.NAME/e.URL but the real field names are EVENT/OFFICIAL_URL, so 18/18 event names are null and 18 archive rows ship with a null title. The honest real backing is: CANONICAL windows (29) for timing/status, ITALY_LABEL_VERDICTS (12 VERIFIED + 7 NOT_

-- MODEL CHANGES --
  * windows (cropWindows) :: Stop publishing a fixture id as the canonical window identity, and stop asserting traceability the record does not have.
      fields: id, windowId, legacyCaseId, ui, uiSource, sourceIds, sourceState
      derivation: id = w.WINDOW_ID (ITALY_CANONICAL.windows[].WINDOW_ID, 29/29 present and unique). legacyCaseId = w.LEGACY_CASE_ID (kept only as a bridge). ui = uiOf(uiByLegacyCase[w.LEGACY_CASE_ID]) — icon/color/order only, uiSource:'FIXTURE_PALETTE'. sourceIds = w.SOURCE_IDS || []; sourceState = sourceIds.length ? 'TRACEABLE' : 'NOT_EXTERNALLY_OBSERVABLE' (measured: 0 of 29 canonical windows carry a non-empty SOURCE_IDS, so sourceState is NOT_EXTERNALLY_OBSERVABLE on all 29 today).
  * productRelationships (NEW, promoted out of products[].links) :: Relationships are the single biggest fact currently supplied by the fixture (80 links, 53 of them with no audit verdict at all). They must become their own audited collection with per-link provenance, so a screen can nev
      fields: id, product, crop, issue, region, windowId, strength, anchor, provenance, auditDate, auditSource, absenceRule
      derivation: One record per (ITALY_LABEL_VERDICTS triple × matching ITALY_CANONICAL window). strength = 'VERIFIED_LABEL_MATCH' for LV.VERIFIED, 'NO_CONFIRMED_MATCH_CURRENT_READING' for LV.NOT_FOUND. region and windowId from the canonical window whose CROP_NAME+ISSUE_NAME equal the triple's crop+issue. anchor = 'CANONICAL_WINDOW' or 'NO_CANONICAL_WINDOW'. auditDate = LV.AUDIT_DATE, auditSource = LV.AUDIT_SOURCE, absenceRule = LV.ABSENCE_RULE (so §10 wording travels with the record). MEASURED OUTPUT: 28 window-anchored links + 4 orphan (Maize|Diabrotica->FORZA, Wheat|Cereal Aphids->MAVRIK EW, Wheat|Cereal Aphids / BYDV Risk->MAVRIK EW, Wheat|Fusarium Head Blight->MAXENTIS), 10 distinct products (was 32), 0
  * productRegisteredUses (NEW) :: IG.LINKS[219] is real, fully populated regulatory evidence that the model does not read at all today. It gives the product screen something truthful to show for the 156 products that will have no audited crop×issue relat
      fields: id, product, reg, cropCode, cropTerm, target, ai, moa, doses, interval, timing, labelUrl, provenance
      derivation: Direct 1:1 from IG.LINKS (219 records, prov REAL_FACT). MEASURED non-null: crop 219/219, cropTerm 219/219, target 219/219, product 219/219, reg 219/219, ai 219/219, moa 219/219, labelUrl 219/219, timing 219/219 (but the timing string is literally 'NAO SEI — a coluna de epoca do rotulo nao foi extraida', so it must render as unknown, not as a window), interval 15/219, maxApp 0/219 (drop the field). WARNING: cropCode is a code vocabulary (SUGARBEET, WHEAT_GENERIC, COMMON_WHEAT, MAIZE, GRAPEVINE, OLIVE, RICE, …14 values) and target is a Latin binomial. Neither maps to the screen's crop×issue vocabulary, and no real dictionary exists to bridge them. This collection may back a 'registered uses' t
  * opportunities :: Collapse the 29-record demo radar feed and the 3-record upstream feed into ONE upstream-only collection, which also removes the false id join.
      fields: id, legacyCaseId, title, crop, region, issue, issueType, caseLabel, forbiddenLabel, whatIsHappening, whyItMatters, currentEvidence, adamaProducts, adamaActiveSubstance, window, marketContext, competitorContext, scienceContext, fieldVoices, whatWeKnow, whatWeDoNotKnow, interpretations, sourceIds, ui, provenance
      derivation: 1:1 from IG.OPPORTUNITIES[3], every field taken from its own uppercase key, never merged with a D.CASES record. sourceIds measured 3/3 non-empty (each cites IT-SRC-MINISTERO, IT-SRC-PIEMONTE, IT-SRC-OPENALEX — all 3 resolve inside IG.SOURCES). ui = uiOf(uiByLegacyCase[o.LEGACY_CASE_ID]) which resolves to {} today (IT-HERO-00x is not a fixture key) — that is correct and must stay empty rather than be back-filled. Delete the separate upstreamOpportunities collection; keep the key as an alias to the same object for one release so nothing 404s.
  * sources :: Restore the real source identity (6 of 31 currently publish a demo id) and stop shipping an all-undefined ui object on every record.
      fields: id, sourceId, name, type, role, country, geography, url, frequency, latestObservation, accessStatus, limitations, linkedRecordCount, ui, provenance
      derivation: id = S(s.ID || s.SOURCE_ID) with NO fixture fallback (31/31 have ID). role = S(s.ROLE) (27/31), frequency = S(s.FREQUENCY) (27/31), latestObservation = S(s.LATEST_OBSERVATION) (28/31), accessStatus = S(s.ACCESS_STATUS) (31/31, values GREEN|PARTIAL|NOT_REACHED|BLOCKED), limitations = S(s.LIMITATIONS) (31/31), geography = S(s.GEOGRAPHY) (31/31, values IT|EU|EU/IT|GLOBAL|IT/ES/FR|Emilia-Romagna|Piemonte). linkedRecordCount = number of records in SCIENCE+MARKET+VOICES+RESISTANCE+RESEARCHERS+CHANNELS whose SOURCE_ID equals this id — MEASURED: only 4 of 31 sources are referenced at all (IT-SRC-OPENALEX 148, IT-SRC-AGRIFOOD 77, IT-SRC-GIRE 34, IT-SRC-YOUTUBE 17); the other 27 are 0. ui = { color: S
  * events (futureEvents) :: The adapter reads keys the real records do not have, so 18/18 names and 18/18 urls are null and 18 archive rows ship titleless.
      fields: id, name, date, dateEnd, location, url, sector, organizer, cropRelevance, timeState, exhibitorListState, confirmedParticipation, participationLaw, note, daysFromRef, provenance
      derivation: name = S(e.EVENT) (fixes 18/18 nulls), url = S(e.OFFICIAL_URL) (fixes 18/18 nulls), date/dateEnd split on ' a ' from e.DATE ('2026-11-10 a 2026-11-14'; 5 of 18 are single dates), sector = S(e.SECTOR) 18/18, organizer = S(e.ORGANIZER) 18/18, timeState = S(e.TIME_STATE) (FUTURO|PASSADO), exhibitorListState = S(e.EXHIBITOR_LIST_STATE) (values 'NAO CONSULTADA' | '161 marcas, segundo a UIV' | 'NAO APLICAVEL'), confirmedParticipation = e.CONFIRMED_PARTICIPATION || {} (non-empty on 14/18), participationLaw = S(e.PARTICIPATION_LAW) 18/18 — this string must travel with the record because it is the §9-style guard ('participacao FUTURA nunca se infere de participacao passada'). daysFromRef already corr
  * regulatoryFuture (NEW, real and unused today) :: IG.PRODUCTS.expiry is 163/163 non-null real data that nothing in the model or the views reads, and it is exactly what upstream opportunity IT-OPP-003 is about (the ADAMA Italy authorisation-expiry calendar).
      fields: id, product, reg, holder, status, expiry, daysToExpiry, regCat, line, labelUrl, provenance
      derivation: 1:1 from IG.PRODUCTS. expiry 163/163 (ISO dates, e.g. 2026-11-30, 2027-01-31). daysToExpiry = daysFrom(expiry) against the single REF 2026-09-02. status 163/163 (8 declared regimes, e.g. 'Ri-registrato', 'Autorizzato con procedura zonale'). No invention: this is a date the registry publishes.
  * channels / news / researchers / scienceRecords (field-name repairs found while auditing the same adapter pattern) :: The same wrong-key class of bug as events; all measured, all silently null today.
      fields: channels.name/platform/url, news.outlet/url, researchers.org, scienceRecords.date
      derivation: channels.platform 0/30 and channels.url 0/30 -> real keys are (none for platform; there is no PLATFORM field, so platform must become NOT_EXTERNALLY_OBSERVABLE) and CHANNEL_URL for url; channels.name should read c.CHANNEL. news.outlet 0/8 -> n.PUBLISHER; news.url 0/8 -> n.SOURCE_URL. researchers.org 0/60 -> r.INSTITUTIONS; researchers.crops 0/60 and researchers.issues 0/60 have NO real equivalent (see fieldsThatDie). scienceRecords.date 0/88 and year 0/88 -> r.PUBLISHED_AT; studyArea 0/88 and affiliation 0/88 have no real equivalent.
  * validationReport (NEW, exposed on ITALY_APP_MODEL) :: None of the bugs above would have survived a build that had to publish what it accepted and what it rejected. This is the deliverable that makes V2.1 safe to load.
      fields: referenceDate, families[], families[].name, families[].sourceKey, families[].received, families[].accepted, families[].rejected, families[].rejections[], families[].fieldCoverage{}, families[].emptyValid, idCollisions[], namespaceWarnings[], precedenceOverrides[], demoLeaks[]
      derivation: Produced by the adapter run itself, not hand-written. fieldCoverage is the measured non-null count per contract field (the numbers in this spec are exactly what it would print). rejections[] carries {recordIndex, reason} with reason drawn from a fixed vocabulary: MISSING_ID, DUPLICATE_ID, MISSING_REQUIRED_FIELD, UNKNOWN_ENUM_VALUE, UNPARSEABLE_DATE, CROSS_FAMILY_ID_COLLISION, FOREIGN_KEY_UNRESOLVED, DEMO_PROVENANCE_IN_REAL_SLOT. demoLeaks[] lists every field in a REAL_* collection whose value came from a rank-4/5 input — it must be empty for windows.id, sources.id, products[].links and opportunities after this migration.

-- FIELDS THAT DIE (21) --
  [REMOVE_SUBCOMPONENT] window record id (WIN-001 … WIN-029) @ italy-app-model.js:73 (id), :275 (archive key); portale.html:2876 wd0 lookup, :3 :: Not a fact, an identity — and it is the fixture's, not the world's. All 29 canonical windows publish it today. Replaced by WINDOW_ID (IT-WIN-0001…). Nothing on screen changes; 29 archive primary keys and every window rou
  [REMOVE_SUBCOMPONENT] product relationship strength LABEL_CHECK_NEEDED @ italy-app-model.js:104 (l.strength), :118 checkNeededLinks; portale.html:3016, m :: 53 of the 80 demo links carry this class and NOT ONE of them appears in ITALY_LABEL_VERDICTS. The real audit publishes only VERIFIED_LABEL_MATCH (12) and NO_CONFIRMED_MATCH_CURRENT_READING (7). The amber 'check needed' l
  [REMOVE_SUBCOMPONENT] product relationship strength RELATED_PORTFOLIO @ italy-app-model.js:119 relatedLinks :: Defined in ITALY_LABEL_VERDICTS.STRENGTH (rank 1, colour #5CC3EE) but zero records carry it, in the fixture or in the audit. relatedLinks is already empty for 166/166 products. Keep the class in the strength dictionary (
  [REMOVE_SUBCOMPONENT] product relationship .caseId @ italy-app-model.js:104; portale.html:3001 relRow -> this.openCase(l.caseId) :: A fixture case id used as a navigation target. The audit triples carry no case. Replaced by windowId, which routes to the canonical window instead — a different destination screen, so the click behaviour visibly changes.
  [DERIVABLE_FROM_REAL] product relationship .region @ italy-app-model.js:104; portale.html markup 1366 and 1372 ({{ r.region }}) :: Derivable for 28 of 32 audit-anchored links by joining the LV triple's crop+issue to ITALY_CANONICAL.CROP_NAME+ISSUE_NAME. Four LV pairs have no canonical window (Maize|Diabrotica, Wheat|Cereal Aphids, Wheat|Cereal Aphid
  [SHOW_UNKNOWN] canonical window traceable sources (windows[].sourceIds) @ italy-app-model.js:81 (sourceIds: w.SOURCE_IDS || []), :275 (archive prints the  :: MEASURED 0 of 29 canonical windows carry a non-empty SOURCE_IDS. The collection is labelled 'canonical audited crop windows' and the archive prints 'CANONICAL' in the source column, which reads to a user as 'this has a s
  [SHOW_UNKNOWN] source .group (GOVERNMENT & OFFICIAL, NEWS & TRADE MEDIA, …) and its colour swatch @ italy-app-model.js:207; portale.html:2278 {{ s.groupColor }}, :2295 {{ sr.group  :: MEASURED 0 of 31 real sources produce a group — IG.SOURCES has no GROUP field. It has TYPE (12 values: OFFICIAL, MARKET, RESEARCH, COMPANY, PEOPLE, FIELD, TECHNICAL_MEDIA, RESEARCH_INSTITUTION, COOPERATIVE, PRODUCER_ORG,
  [SHOW_UNKNOWN] source .related ('N cases') @ italy-app-model.js (not modelled today); portale.html:2285 {{ s.related }} cases :: The fixture gives every source a case count (e.g. 5). There is no real 'case' to count, and the nearest honest metric is brutal: only 4 of 31 sources are referenced by ANY record in the corpus — IT-SRC-OPENALEX 148, IT-S
  [REMOVE_SUBCOMPONENT] source record id for 6 sources (SRC-12, SRC-13, SRC-15, SRC-22, SRC-25, SRC-36) @ italy-app-model.js:206 :: Identity theft by name collision: OpenAlex, Regione Piemonte, Eurostat, L'Informatore Agrario, CREA and Coldiretti currently publish a demo id because their real NAME happened to match a fixture row. Every real SOURCE_ID
  [REMOVE_SUBCOMPONENT] event .name and .url @ italy-app-model.js:217; archive rows via :273 :: Not 'dies' — was never alive. MEASURED 18/18 null on both, because the adapter reads e.NAME/e.URL and the real keys are EVENT/OFFICIAL_URL. 18 of the 740 archive rows currently have a null title as a direct result. This 
  [REMOVE_SUBCOMPONENT] event .city and .region as separate fields @ portale.html:1752 {{ e.city }}, :1775 {{ e.city }} · {{ e.region }} :: Real data has one LOCATION string ('BolognaFiere, Bologna', 'Tenuta di Nozzole, Greve in Chianti (FI)'). Splitting it into city and region would be guessing. The two spans merge into one {{ e.location }}.
  [REMOVE_SUBCOMPONENT] event .historical[] (past exhibitor participation) @ portale.html:3173 evDeco (e.historical), :3183 :: The real records carry PARTICIPATION_LAW on all 18: future participation is never inferred from past participation, and a company with no line means NOT KNOWN. Rendering a 'historical participants' badge is exactly the i
  [SHOW_UNKNOWN] event .crops[] @ portale.html:3183 evd0.crops.includes(c.crop) :: Real data has CROP_RELEVANCE, a single descriptive string ('TRANSVERSAL'), not a crop array. The 'related cases for this event' cross-link that depends on crops[] cannot be rebuilt from it.
  [REMOVE_SUBCOMPONENT] opportunity dossier body — latin, hero, origin, happening, why, stage, signal, progress, evidence{}, actions[], departments[], ladder, readiness, scale, fieldCount, compCount, signals[], primary, products[], moreMatches, matchCount @ italy-app-model.js:260 Object.assign({}, c, …) copies all of them; portale.html: :: 26 of 29 radar dossiers are fixture end-to-end and every listed field is a §2-forbidden fact. Upstream supplies 3 dossiers with a DIFFERENT and richer field set (TITLE, WHAT_IS_HAPPENING, WHY_IT_MATTERS, CURRENT_EVIDENCE
  [REMOVE_SUBCOMPONENT] opportunity.isUpstreamReal / provenance REAL_SOURCE on IT-OPP-002 and IT-OPP-003 @ italy-app-model.js:256, :259, :262 :: Actively false today, not merely unsupported. Upstream IT-OPP-003 is the national ADAMA authorisation-expiry calendar; demo IT-OPP-003 is Durum Wheat / Fusarium Head Blight / Toscana. The model certifies the demo record 
  [SHOW_UNKNOWN] futureSignal .status, .confidence, .proves, .notProves, .evidenceIds @ italy-app-model.js:233-238; portale.html:3151 futureStatusChips, :3149 status co :: MEASURED 0 of 3 real future signals carry any of them — IG.FUTURE_SIGNALS has no STATUS, CONFIDENCE, WHAT_IT_PROVES, WHAT_IT_DOES_NOT_PROVE or EVIDENCE_RECORD_IDS field. The seven status chips (NEW SIGNAL … PREPARE) ther
  [KEEP_AS_LABELLED_DEMO] futureSignal .sourceType, .spark[], .who[], .independent, .observations, .trend, .trail[], .sources[] @ italy-app-model.js:243 (scenarios carry them); portale.html:2894-2900 signal det :: These exist only on the 56 DEMO_SCENARIO records. The signal-detail screen (sources fan-out, evidence trail, independence note, sparkline) is entirely a scenario feature — opened on a real signal it renders empty. That i
  [SHOW_UNKNOWN] researchers .org, .crops[], .issues[], .url @ italy-app-model.js:189-191 :: MEASURED 0/60 on all four. org is a wrong-key bug (real field is INSTITUTIONS) and is repairable. crops[], issues[] and url have NO real equivalent in IG.RESEARCHERS (whose real fields are CATEGORY, ORCID, OPENALEX_ID, T
  [SHOW_UNKNOWN] scienceRecords .date/.year, .studyArea, .affiliation @ italy-app-model.js:181-183; archive date column via :269 :: MEASURED 0/88 on all four. date/year is a wrong-key bug (real field is PUBLISHED_AT) and is repairable, which also fixes 88 archive rows that currently sort with a null date. studyArea and affiliation have no real equiva
  [SHOW_UNKNOWN] news .outlet, .url · channels .platform, .url @ italy-app-model.js:211-212, :223 :: MEASURED news.outlet 0/8, news.url 0/8, channels.platform 0/30, channels.url 0/30. Three of the four are wrong-key bugs (PUBLISHER, SOURCE_URL, CHANNEL_URL) and repairable. channels.platform has NO real field at all in I
  [KEEP_AS_LABELLED_DEMO] Field Sales inbound messages (all 18 + composer-generated) @ italy-app-model.js:280; portale.html:2603, :3401 simulateInbound, :3406 sendComp :: §5-compliant as it stands: stamped SYNTHETIC_DEMO, labelled 'simulated' in the data-state panel, excluded from nav counts. Two residual risks to close: sendComposer at 3406 attaches a real D.CASES caseObj and a futureSce

-- PLAN --
  1. STEP 0 — Freeze the measurement. Before touching anything, add a build-time report to italy-app-model.js that prints, per collection, count/real/demo plus per-field non-null counts, and commit the current numbers as the baseline (windows 29, opportunities 29/3 real, products 166, product links 80 across 32 products, sources 31 with 6 demo ids, events 18 with 18 null names, archive 740 with 18 null titles, searchIndex 309). Every later step is judged against this baseline. No behaviour change.
  2. STEP 1 — Rename the fixture binding. Line 23: const D -> const UIFX, with the comment '/* rank 4-5 ONLY: ui palette (uiOf) + futureScenarios + fieldMessages. Any other read is a §2 violation. */'. Update the 8 call sites mechanically. This makes every remaining fixture read grep-able in one word and is the cheapest guard against regression.
  3. STEP 2 — Fix window identity (line 73). id: w.WINDOW_ID. Rename dwByCase -> uiByLegacyCase (line 68-69). Keep line 71 unchanged. Verify: windows.records ids become IT-WIN-0001…IT-WIN-0029, ui still populated 29/29, and the 29 WINDOW rows in the archive change key. Also fix line 275 to print sourceIds or NOT_EXTERNALLY_OBSERVABLE instead of the literal 'CANONICAL'.
  4. STEP 3 — Delete line 98 (D.PRODUCTS injection). Verify products count stays 166 and no product has provenance SYNTHETIC_DEMO. This is a zero-risk deletion measured today; do it early so the next step operates on a clean product universe.
  5. STEP 4 — Read ITALY_LABEL_VERDICTS in the model for the first time. Add const LV = window.ITALY_LABEL_VERDICTS || {}; next to the other inputs at line 22-28. Build the new productRelationships collection from LV.VERIFIED (12) and LV.NOT_FOUND (7) anchored to ITALY_CANONICAL by CROP_NAME+ISSUE_NAME. Recompute products[].links / verifiedLinks / checkNeededLinks / relatedLinks / rejectedLinks from it, so the field names the views already bind do not change. THEN delete lines 102-105. Expected measured outcome: 80 links -> 32 (28 window-anchored + 4 orphan), 32 products with links -> 10, checkNeed
  6. STEP 5 — Adjust the two live consumers of relationships. portale.html:3001 relRow -> route via l.windowId with this.openWindow, and render l.region || T.prodNotObservable. portale.html:3002 wins -> read AM.collections.windows.records matched on l.windowId, state from w.status (CURRENT_STATUS), colour from status === 'WINDOW_OPEN'. Markup: delete the '· {{ p.check }} △' fragment at 1417 only; leave 1363-1376 structurally intact (the sc-ifs self-hide). Screenshot-compare the Product Intelligence and Portafoglio screens before/after — layout must be byte-identical except for the removed △ and the
  7. STEP 6 — Repair the events adapter (line 215-220): name from EVENT, url from OFFICIAL_URL, split DATE on ' a ', add sector/organizer/timeState/exhibitorListState/confirmedParticipation/participationLaw; delete the deByName lookup (214) and the ui line (216). Verify the 18 null archive titles disappear (740 rows, 0 null titles).
  8. STEP 7 — Repair the sources adapter (line 203-209): id from s.ID only, delete the dsByName lookup and the dead ui line, add role/frequency/latestObservation/accessStatus/limitations, add the derived linkedRecordCount, add a model-side SRC_TYPE_COLOR literal for the swatch. Verify the 6 hijacked ids revert to IT-SRC-*. Do NOT touch the sources SCREEN in this step — it still reads D.SOURCES and belongs to the sources-domain migration; this step only makes the model honest and ready.
  9. STEP 8 — Rebuild opportunities from upstream (lines 245-264). Delete upstreamOppIds and the false join at 253-259 entirely. Build ONE opportunities collection from IG.OPPORTUNITIES[3] with its own uppercase field set; keep upstreamOpportunities as an alias to the same object for one release. Expected: counts.opportunities 29 -> 3, real 3 / demo 0. IMPORTANT: land this as its own commit, because the radar screen still builds its cards from D.CASES at portale.html:2515 and is unaffected — the two must not be conflated.
  10. STEP 9 — Fix the search index (line 300): index windows and the 3 upstream opportunities instead of the 29 demo cases; drop the c.latin term. Expected searchIndex 309 -> ~283, with zero entries whose terms came from a fixture.
  11. STEP 10 — Repair the same-class wrong-key bugs found in the audit: channels.name<-CHANNEL, channels.url<-CHANNEL_URL, channels.platform -> NOT_EXTERNALLY_OBSERVABLE; news.outlet<-PUBLISHER, news.url<-SOURCE_URL; researchers.org<-INSTITUTIONS and drop crops/issues/url; scienceRecords.date<-PUBLISHED_AT and drop studyArea/affiliation. These fix 88 archive dates and 8 news links.
  12. STEP 11 — Add the new real collections that need no fixture: productRegisteredUses from IG.LINKS[219] and regulatoryFuture from IG.PRODUCTS.expiry[163/163]. Both are additive; no view reads them yet. They exist so the product page has truthful content for the 156 products that lost their relationship block in step 4.
  13. STEP 12 — Introduce the adapter/validator layer as a refactor, not a rewrite. Extract every per-family map() in italy-app-model.js into an adapter object with the signature described in modelChanges, run them through one loop, and publish ITALY_APP_MODEL.validationReport. Assert at build time: (a) demoLeaks[] is empty for windows.id, sources.id, productRelationships and opportunities; (b) no id appears in two families with different (crop, issue, region) — this is the assert that would have caught the IT-OPP-003 collision; (c) referenceDate is read once and no adapter calls new Date() without 
  14. STEP 13 — Create the 23 empty-valid collection slots (see the family map). Each slot is coll([], provenance, note) with emptyValid:true when there is no real source yet. An empty slot is a passing build. Nothing may be seeded with a placeholder record.
  15. STEP 14 — Publish the view contract (see openQuestions / risks): every record exposes exactly {id, provenance, sourceIds, ui, …domain fields} and NOTHING that names a mission, a package version, a scraper, a run date or a file. Grep the model for 'V2', 'HANDOFF', 'MISSION', 'INGEST' appearing in any emitted string; today ITALY_INGEST.BUILT and ITALY_INGEST.SOURCE exist at the blob root and must never be copied onto a record.
  16. STEP 15 — Re-run the STEP 0 report and diff it against the baseline. Every changed number must be explainable by one line of this spec. Any number that moved for a reason not listed here is a regression.

-- RISKS --
  ! The relationship shrink is severe and highly visible: 32 products with portfolio relationships become 10, and the amber LABEL_CHECK_NEEDED list disappears from every product page. Someone will ask to backfill it from IG.LINKS (219 registered uses). That would be inventing a verdict: IG.LINKS is keyed on Latin target (Agriotes sp, Cercospora beticola) and crop CODES (SUGARBEET, WHEAT_GENERIC), whil
  ! The 'Cereal Aphids · BYDV Risk' vs 'Cereal Aphids / BYDV Risk' separator mismatch means canonical window IT-WIN-0016 loses its MAVRIK EW verdict purely on punctuation. If the engineer normalises separators to fix it, the same normalisation will silently merge other pairs. The validator must REPORT near-misses and a human must resolve them one by one.
  ! Only 4 of 31 sources are cited by any record. Publishing linkedRecordCount honestly puts 27 zeros on the sources table. There will be pressure to hide the zeros or to count 'related' some other way. Hiding them would misrepresent the coverage of the source registry, which is exactly the number a decision-maker needs.
  ! Zero of the 29 canonical windows carry a SOURCE_ID. The windows collection is the highest-precedence layer in the whole model (CANONICAL) and it is the one with no citation. If a future screen prints 'canonical, audited' next to a window it is making a claim the record cannot support.
  ! opportunities dropping from 29 to 3 empties the radar, which is the portal's first screen. §3 says a smaller truthful screen beats a dense fake one and forbids redesigning to compensate — but the radar screen still reads D.CASES directly, so the model change alone will NOT empty it. That gap (model says 3, screen shows 29) is worse than either end state and must not be left standing across release
  ! The upstream feeds are tiny and structurally different from the fixture: 3 opportunities, 3 future signals, and the future signals have no STATUS at all. Rebuilding the future screen's seven status chips on real data yields seven zeros. Deriving a status from WHY_WATCH text would violate §7 (presentation may not invent an agronomic state).
  ! IG.CROP_WINDOWS uses ids IT-WIN-001…IT-WIN-007 while ITALY_CANONICAL uses IT-WIN-0001…IT-WIN-0029. Different strings today, but one zero away from a silent collision the moment either side renumbers. The cross-family id assert in STEP 12 must run on the union of ALL families, not per family.
  ! IG.CROP_WINDOWS (7 records, real, Italian-language, with REGULATORY_ACT 'DDR n. 13645 2026-05-14' and explicit COVERAGE_STATE) is real regulatory window evidence that neither the model nor the canonical file reads. Merging it with ITALY_CANONICAL is tempting and dangerous: its ISSUE strings are Italian ('Flavescenza dorata (vetor Scaphoideus titanus)'), its CROP strings are Italian codes (VITE, OL
  ! Several real records carry Portuguese working-notes as values (EXHIBITOR_LIST_STATE 'NAO CONSULTADA', OBSERVED_STAGE 'NAO SEI', timing 'NAO SEI — a coluna de epoca do rotulo nao foi extraida'). These are unknown-markers written in the research team's language, and if rendered raw they both break §11 (Italian default) and read as data. The normalize layer must map them to the model's NOT_EXTERNALLY
  ! sendComposer (portale.html:3406) attaches a real D.CASES object and a futureScenarios record to a demo field message, and fieldCases (3130) lets demo messages add a 'field evidence' badge to radar cards. A demo record is influencing how a core object renders — §9. Not in the model, but it is the fixture leak most likely to survive this migration unnoticed.
  ! IG.PRODUCTS coverage is uneven and the product page must not imply otherwise: crops 120/163, targets 99/163, hrac 52/163, frac 8/163, irac 10/163, catalogUrl 40/163. Any per-product panel that assumes these exist will render mostly-empty cards for the majority of the registry.

-- OPEN QUESTIONS --
  ? THE INGESTION BOUNDARY — adapter signature. Proposal for review: every family is registered as { family:'cropWindows', slot:'windows', rank:'CANONICAL', sourceKey:(pkg)=>pkg.CANONICAL_WINDOWS, required:['WINDOW_ID','CROP_NAME','ISSUE_NAME','CURRENT_STATUS'], enums:{CURRENT_STATUS:['WINDOW_OPEN','WINDOW_CLOSED','NEXT_CY
  ? PRECEDENCE MERGE — proposal for review. The merge is per FIELD, not per record, and it only ever fills a hole; it never overwrites. mergeField(slot, id, field, value, rank) accepts the value if the slot is empty for that field OR the incoming rank is strictly higher (CANONICAL 1 > REAL_SOURCE 2 > REAL_DERIVED 3 > SYNTH
  ? VIEW CONTRACT — proposal for review. A record leaving the model exposes exactly: id (stable, namespaced by family, never a fixture id), provenance (one of the five constants), sourceIds[] (possibly empty; emptiness is a fact the UI must render, not hide), ui{icon,color,order} (presentation only), and the family's norma
  ? THE CLOCK — proposal for review. APP.referenceDate / AM.REF stays the only clock (2026-09-02, read once at line 39-40 from CANON.meta.referenceDate with a literal fallback). Add a build assert that no adapter and no view calls new Date() with zero arguments; the only permitted date arithmetic is AM.daysFrom(iso). Note 
  ? V2.1 FAMILY -> SLOT MAP, for confirmation before any V2.1 data is ingested. EXISTS TODAY: productsRegulatory -> collections.regulatory (IG.PRODUCTS, 163); productsCommercial -> collections.commercial (ITALY_CATALOG, 44); cropWindows -> collections.windows (ITALY_CANONICAL, 29); marketObservations -> collections.marketO
  ? Is ITALY_LABEL_VERDICTS authoritative enough to be the ONLY relationship source? It is dated (AUDIT_DATE) and scoped (SCOPE_NOTE), covers 18 of 29 canonical windows, and gives 10 products. The 11 uncovered windows (Maize|Diabrotica Larvae ×2, Wheat|Cereal Aphids · BYDV Risk, Durum Wheat|Wheat Rusts, Maize|Mycotoxin Ris
  ? Should collections.upstreamOpportunities be deleted outright or kept as an alias? Keeping two collections over the same 3 records is what allowed the false join to be written in the first place.
## Gli strumenti dei portoni — e perche NON stanno in package.json

I portoni che aprono un navigatore vero (`mobile.mjs`, `browser.mjs`,
`cta-navigation.mjs`, `action-map-consistency.mjs`, `opportunity-hub.mjs`) e
quelli che leggono un PDF (`pdf-gate.mjs`, `rtv-gate.mjs`) hanno bisogno di
due pacchetti che il SITO non usa:

    npm install playwright playwright-core pdfjs-dist

Non sono dichiarati in `package.json`, e non e una dimenticanza. La radice del
repository contiene `vercel.json` con `outputDirectory: italia-portale/client`:
il deploy parte da QUI, e installa cio che trova in `package.json` — anche le
devDependencies. `playwright` in fase di installazione scarica i browser, e un
deploy statico da 800 KB si porterebbe dietro centinaia di MB per uno
strumento che il sito non carica mai.

    UNA DIPENDENZA DI PROVA DENTRO IL MANIFESTO DEL DEPLOY NON E UNA
    COMODITA: E UN RISCHIO DI BUILD IN CAMBIO DI UN COMANDO IN MENO.

Chromium e gia nell'ambiente (`PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers`):
NON eseguire `playwright install`. I portoni lo trovano da soli.
